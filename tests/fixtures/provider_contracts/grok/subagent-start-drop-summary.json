{
  "capture_type": "gobby-hooks-log-and-grok-discovery",
  "grok_version": "1.0.30",
  "debug_trace": "subagent-start-debug-trace.json",
  "note": "Grok ~/.grok/logs/hooks.log is discovery-only. Per-hook dispatch is in grok --debug --debug-file and Gobby ~/.gobby/logs/hooks.log, which records only lookup failures and TTY binds: a subagent_start that resolves to the parent leaves no row.",
  "discovery": {
    "source": "~/.grok/logs/hooks.log",
    "sample": "hooks: discovery complete total_hooks=23 subagent_start=1 subagent_stop=1"
  },
  "observed_child_hooks": {
    "source": "~/.gobby/logs/hooks.log",
    "event": "BEFORE_AGENT",
    "behavior": "Bound native subagent hook BEFORE_AGENT to live pane owner (inherited tmux pane, source=grok).",
    "process_identity": "Child rows share the parent's terminal_context.tmux_pane and parent_pid; an unrelated Grok session registered in the same minute had a different pane and pid."
  },
  "sanitized_child_payload": {
    "hookEventName": "user_prompt_submit",
    "sessionId": "01a04561-child",
    "cwd": "<WORKSPACE>",
    "terminal_context": {
      "tmux_pane": "%90",
      "tmux_socket_path": "/tmp/tmux-501/default",
      "parent_pid": 64881
    }
  },
  "observed_subagent_start": {
    "source": "~/.gobby/logs/hooks.log.1 and the sessions table",
    "older_build_2026-08-29": "SUBAGENT_START arrived with the child sessionId: 4 orphan rows, 1 TTY bind on 2026-08-30.",
    "grok_1.0.30_2026-09-14": "No SUBAGENT_START orphan or bind row while the parent's subagent_count went above zero, so the start carried the parent sessionId and resolved silently.",
    "undercount_2026-09-14": "One counted start for a batch of about fifteen parallel children; after the first subagent_stop the counter read zero and 14 running children auto-registered as standalone Grok terminal sessions within 34 seconds."
  }
}
