# gclient chrome refresh — council round 2 (refreshed hash)

Plan: `.gobby/plans/gclient-chrome-refresh.md` (registry row `31284fe4-3929-4eec-9c69-e4d2a94d9d1b`; epic #22698).
Planning task: #22720 (Fold chrome round 1 into the gclient-chrome-refresh plan and refresh its hash).
Reviewed hash: `4065ada943990c6383d98d2021aa472e354eba3e59e0e1fa47b0ddd89ce930a8` (writer commit `79191b6bea37957c73521882b7c8c61e5e9f0341`, the round-1 fold).
This is the single refreshed-hash council round the task buys. No second round.

Participants:
- Writer: gobby#14278 (`4f8e9af1-11cd-478a-ab6c-6260527355a1`), claude-fable-5-1.
- Enhancer: gobby#14285, Grok 4.7. Verdict relayed by the Program Director as message `0fd8fc75-5f4a-4da3-bfad-eba3210beab2`; full verdict stored as message `08b6abc4-75e1-4ee1-a3e7-f60a02feaf47`.
- Adversary: gobby#14287, Grok 4.7. Verdict relayed as message `39b362ac-64b1-4591-b780-a767bd6167c6` (`needs_review`, 3 blockers); full verdict stored as message `2089453c-28b3-4705-ad9d-dbeb3a085f3a`. That session expired before the fold was adjudicated, so the Program Director spawned a read-only response collaborator for the same round: adversary gobby#14297 (run `c0742a73-f760-40b7-aa23-e7ff4f191be8`, Grok 4.7), limited to WITHDRAW or HOLD on findings 1–3 against hash `2ad4684c…` and commit `63a5ed0299`; its disposition was relayed as message `2d14b6d1-44b5-4453-8ebb-5182a19401b2`.
- Program Director: parent session `32f43d2c-d090-4ca2-96b5-4128d22639aa`.

Priority rule applied throughout: a signed design artifact (the chrome canvas, the plan's Decision Record) outranks either reviewer. Every writer position below was verified against the tree at the reviewed hash before the move was recorded.

## Enhancer items (message 0fd8fc75) — writer positions and moves

### cr-1 enh-mod-order — FOLD
Enhancer position: drop 3.8 from 3.2b's dependencies (leaving 3.2a) and add 3.7 as a dependency of 3.8, so the shared `crates/gclient/src/ui/mod.rs` edits order menu_bar → status_segments → marks and menu/sidebar/startup work stops waiting on grids and theme roles. Evidence: 3.2b uses no marks; the edge existed only for the shared file.
Writer verification: 3.2b's body references no marks symbol and no theme role; the only shared target with 3.8 is `crates/gclient/src/ui/mod.rs`, where each of 3.2b, 3.7, 3.8 and 3.9 adds one `pub mod` line. New order 3.2b (depends 3.2a) → 3.7 (depends 3.2b, 3.3, 3.6) → 3.8 (depends 0.1, 3.7) → 3.9 (depends 3.2a, 3.7, 3.8, 3.10) has no cycle: nothing on the 3.2b..3.7 path depends on 3.8; 3.13 and 3.14 keep their 3.8 edges.
Move: 3.2b heading `(depends: 3.2a)`; 3.8 heading `(depends: 0.1, 3.7)`; a Research context bullet in 3.8 records the mod.rs declaration order.

### cr-2 enh-status-fixed-slots — FOLD
Enhancer position: `StatusSegment` should contain only `Focus, Model, Context, Tokens`; defaults left `[focus, model]`, right `[context, tokens]`; daemon connecting/unreachable and the hidden-attention count stay fixed left-slot writers emitted once; delete the `Daemon`/`Attention` variants and the `Option<u64>` placeholder accessor. Evidence: Decisions 9/17 make them fixed; the plan emitted both in the fixed slot and in the configurable list.
Writer verification: Decision 8 puts the hidden count on the left with click-opens-sidebar and `Daemon unreachable` leading the left while it holds; Decision 17 names exactly two fixed parts and makes the rest two named lists. 3.7 at the reviewed hash emitted `daemon` and `attention` both as fixed slots and as default list members (`["daemon", "attention", "focus", "model"]`), so a default prefs file drew each twice. 3.9 already targets `render_status_line` and reads `chrome.connection.retry_at`, so the countdown placeholder in 3.7 was dead mechanism (restraint rung 1).
Move: 3.7 enum and parse names reduced to the four list segments; defaults `status_left = ["focus", "model"]`, `status_right = ["context", "tokens"]`; the fixed left slot writes `Daemon unreachable` while `!ws.daemon_ready()` (3.9 extends that same slot with the stage and retry text) then `{n} need you` once; the placeholder accessor sentence and the matching assumption bullet are deleted. 3.7.3 keeps its meaning.

### cr-3 enh-shadow-unused — FOLD (signed source)
Enhancer position: 3.9 draws only `marks::wordmark()` in `MarkPalette::normal`; `wordmark_shadow()` is not called; the shadow stays saved unused data per Decision 19; the one-right/one-down placement is deleted (it also contradicted the signed half-row description).
Writer verification: Decision 19 verbatim: "The lowercase drop shadow (49 × 9, letters in `accent` over a copy one column right and half a row down in `dim`) is the saved alternate and ships as data, unused." The 3.9 splash paragraph at the reviewed hash drew the shadow under the wordmark, contradicting the signed decision.
Move: the 3.9 splash paragraph draws the wordmark alone and names Decision 19; 3.8 keeps `wordmark_shadow()` and `MarkPalette::shadow` as the data's parser and palette binding (3.8.3 still tests braille in `dim`) and notes that no renderer calls them.

### cr-4 enh-splash-mod — FOLD
Enhancer position: 3.9 creates `crates/gclient/src/ui/splash.rs` and calls `splash::render_splash` without declaring the module; add `crates/gclient/src/ui/mod.rs` to 3.9's Targets with `pub mod splash;` after `marks`.
Writer verification: `crates/gclient/src/ui/mod.rs` declares every submodule explicitly (`pub mod chrome; … pub mod widgets;`); an undeclared file does not compile. 3.9 depends on 3.7 and 3.8, the other editors of that file, so the shared-target ordering holds.
Move: `crates/gclient/src/ui/mod.rs` added to 3.9 Targets; body sentence declares `pub mod splash;` after `marks`.

### cr-5 enh-model-slug — FOLD (restraint rung 2: one helper, two callers)
Enhancer position: add `AgentEntry::model_slug` in 3.1 beside `definition_label` (`model_display_name` else `model`, lowercased, whitespace runs to hyphens, effort suffix); 3.4b and 3.7 call it; the row-only 17-cell truncation stays local.
Writer verification: `AgentEntry` (`crates/gclient/src/app/sidebar_model.rs`) already carries `model`, `model_display_name` and `effort`; at the reviewed hash 3.4b defined the slug rule inline and 3.7 pointed at "the `model_slug` rule from 3.4b", which is two implementations of Decision 5's slug.
Move: 3.1 adds `AgentEntry::model_slug(&self) -> String` and acceptance 3.1.6 (test `crates/gclient/tests/sidebar_model.rs::model_slug_lowercases_hyphenates_and_appends_effort`); 3.4b sets `model_slug` from `agent.model_slug()` truncated to 17 cells locally; 3.7's `model` segment is `agent.model_slug()` untruncated.

## Adversary items (message 39b362ac) — writer positions and moves

Named checks with no blocker (recorded as the adversary stated them): cr-5 menu.rs ceiling repaired (3.2b moves inline tests before regrouping, 3.11 moves item builders only, 3.2b.6 names source_size); cr-1 consumer compile gaps closed including the `task_title` default and `::*` scoping; signed seven-menu order and Edit contents correct; provider labels correct; project-dialog routing correct through `project_dialog_key` with 3.13 depending on 3.12; `workspace_attach` hold gate coherent; project/workspace split unconditional and correctly targeted; sidebar pin mechanism coherent once finding 1 is repaired.

### Finding 1 gcr-r2-retired-pref-keys (check_key retired-pref-keys-still-parse, section 3.2a, blocking) — FOLD
Adversary position: `crates/gclient/src/prefs.rs:37-38` `UiPrefs` has `#[serde(default, deny_unknown_fields)]` with fields `pane_borders`/`sidebar_collapsed`; `tests/startup.rs::prefs_round_trip_and_reject_unknown_keys` rejects unknown keys. 3.2a drops the two keys and falsely says old prefs still parse. Existing saved prefs would fail to load, so 3.3 never reads new sidebar prefs. Required repair: keep `pane_borders` and `sidebar_collapsed` on `UiPrefs` as `#[serde(default)]` fields; `ClientPrefs::from` ignores them; do not copy them into `ClientPrefs` or show settings rows; leave `deny_unknown_fields`; delete the false sentence; 3.3 adds `sidebar_side`/`sidebar_pinned` beside the retired fields.
Writer verification: confirmed. `prefs.rs:37` reads `#[serde(default, deny_unknown_fields)]` directly above `struct UiPrefs`; the typo case in `prefs_round_trip_and_reject_unknown_keys` (`mouse_captre`) proves an unknown key is a `PrefsError::Parse`. The plan sentence "an old `prefs.toml` carrying them still parses because `UiPrefs` does not set `deny_unknown_fields`; verify" was false.
Move: applied as required, with one refinement inside the repair's intent: the retired fields carry `#[serde(default, skip_serializing)]` so a fresh `save_prefs` stops emitting keys that nothing reads (otherwise every saved file would carry `pane_borders = false` and `sidebar_collapsed = false` forever). `ClientPrefs::from` ignores them, `ClientPrefs` does not carry them, no settings row shows them, `deny_unknown_fields` stays. 3.2a's tests paragraph covers `crates/gclient/tests/startup.rs`: `prefs_carry_sidebar_and_project_preferences` keeps `sidebar_collapsed = true` in its fixture text and drops its assertions on the field; `prefs_round_trip_and_reject_unknown_keys` gains a retired-keys case (a file naming both keys loads; a fresh save omits them; the typo case still fails). New acceptance 3.2a.7 names that test. 3.3's persistence paragraph places `sidebar_side`/`sidebar_pinned` beside the retired fields.

### Finding 2 gcr-r2-unnamed-pane-consumers (check_key unnamed-pane-const-deleted-with-live-imports, section 3.6, blocking) — FOLD
Adversary position: `UNNAMED_PANE` is defined at `crates/gclient/src/app/pane.rs:78`, re-exported by `crates/gclient/src/app/mod.rs:39`, and returned by `crates/gclient/src/ui/chrome/labels.rs::attention_label` (:105) and `terminal_label` (:137); 3.6 omits both consumer files. Deleting the const breaks compilation; leaving it keeps labels drawing `shell`. Required repair: add both files to 3.6 Targets; drop the re-export; both functions use `Pane::display_name`'s post-shell fallback (label, else command, else terminal id); acceptance asserts `UNNAMED_PANE` absent from both files.
Writer verification: confirmed with `gcode grep -w UNNAMED_PANE`: readers are `pane.rs:78/310`, `app/mod.rs:39` (`pub use` list), `sidebar_model.rs:17/210` (already targeted through `build_agents`), `labels.rs:11/105/137`. The two `labels.rs` branches that return the const are the no-pane branches, so "terminal id" is the concrete rung there: `crates/gclient/src/app/pane.rs::short_terminal_id` (line 84, the 8-character prefix `orphans.rs` already uses).
Move: 3.6 Targets gain `crates/gclient/src/app/mod.rs::*` (scope-reason: the `UNNAMED_PANE` re-export leaves the `pub use` list; the same wildcard form 3.10 uses for that file, and 3.10 → 3.7 → 3.6 orders the shared target) and `crates/gclient/src/ui/chrome/labels.rs::attention_label` / `::terminal_label`. Body: `display_name`'s last rung is `short_terminal_id(&self.terminal_id)`; `attention_label`'s no-pane branch returns `short_terminal_id(attention_subject(entry_id))` and `terminal_label`'s returns `short_terminal_id(terminal_id)`; the `UNNAMED_PANE` import and re-export go. Research context records the reader list. New acceptance 3.6.7 and 3.6.8 assert `UNNAMED_PANE` absent from `app/mod.rs` and `labels.rs`.
Validator consequence: `crates/gclient/src/app/mod.rs` is 927 lines, so the validator's `production-size-growth` rule refuses any deliverable that targets it without naming a same-extension split, a one-line deletion included. 3.6 therefore also moves the pane-access methods of `impl<D: Daemon> Workspace<D>` (about 114 lines) into the new `crates/gclient/src/app/workspace_panes.rs`, the extension-file pattern 3.2a and 3.10 already use; acceptance 3.6.9 covers it. The rule flagging a shrinking edit is noted for the Program Director as validator found-work outside this task's edit scope.

### Finding 3 gcr-r2-spawn-executor-ceiling (check_key spawn-executor-split-lands-under-850, section 1.2, blocking) — FOLD
Adversary position: `src/gobby/agents/spawn_executor.py` is 998 lines; `_spawn_codex_terminal` is only 32 lines and the shared `_runtime_spawn` stays in the original file, so moving 32 lines leaves about 970 and acceptance 1.2.5's under-850 claim cannot pass, though the repository under-1,000 ceiling does. Required repair: change 1.2.5 and the `wc` check to the repository ceiling (both files under 1,000; the old attribute path still resolves); do not claim 850; no additional codex-only helper exists.
Writer verification: confirmed. `wc -l` reports 998 (the plan said 988); `gcode outline` shows `_spawn_codex_terminal` at lines 228-259 (32 lines) and no other codex-only function; `_runtime_spawn` (330-587) is shared by every provider.
Move: 1.2 states the true line count, moves only `_spawn_codex_terminal`, and claims the repository ceiling; the planned `wc -l` check and 1.2.5 say "under 1,000" and 1.2.5 also asserts the old attribute path `spawn_executor._spawn_codex_terminal` still resolves (the `tests/agents/test_srt_spawn.py` consumer entry already depends on it). The split itself is kept: 1.2 only deletes lines in that file, but 998 lines leaves no headroom for the next edit, and the adversary's repair keeps it (restraint rung 6, the smallest complete split).

## Adversary moves
Protocol (prompt-council.md §4, the adversary's rule): "When the writer contests a finding, you rule on it: withdraw it, or hold it with your reasoning restated against theirs. A finding is resolved only when the writer folds it or you withdraw it." The writer folded all three findings and contested none, so each finding is resolved by its fold and no withdraw-or-hold ruling is owed on any of them. Nothing is open, so nothing goes to the Program Director's gate from this round.

Observable dispositions per finding, as of the writer's fold commit:
- Finding 1 gcr-r2-retired-pref-keys: writer FOLD (applied as required, with the `skip_serializing` refinement); resolved by fold. Adversary ruling: none owed (not contested); independent adjudication below: WITHDRAW.
- Finding 2 gcr-r2-unnamed-pane-consumers: writer FOLD (applied as required, plus the validator-driven `workspace_panes.rs` split); resolved by fold. Adversary ruling: none owed (not contested); independent adjudication below: WITHDRAW.
- Finding 3 gcr-r2-spawn-executor-ceiling: writer FOLD (applied as required); resolved by fold. Adversary ruling: none owed (not contested); independent adjudication below: WITHDRAW.

Relay: a spawned writer can message only its parent, so the adversary's adjudication of the folded hash reached this log through the Program Director (message `2d14b6d1-44b5-4453-8ebb-5182a19401b2`, sent 2026-09-22T22:51:55Z). The Program Director's framing and the adversary response follow verbatim.

> Real independent adversary response received for the existing round. Append the following disposition verbatim to the council-round2 artifact, replacing the relay placeholder; then run the direct plan validation, commit only the council log (plan bytes/hash should remain unchanged), link the commit, and report evidence. Do not retry close_task; parent will do that.
>
> Adversary response for the existing gclient-chrome-refresh council round 2. Not a new round. Reviewer gobby#14297. Plan `.gobby/plans/gclient-chrome-refresh.md` hash `2ad4684c87889daa5d05ec394697525dab4c8f4c5051b8c4c42ec741187d1186`. Log commit `63a5ed0299f7b61db4b0025c2f787a92e46c355c`. Independent adjudication of the three original findings after the writer's folds.
>
> 1. gcr-r2-retired-pref-keys — WITHDRAW
> The current plan fully resolves the finding. `deny_unknown_fields` stays on `UiPrefs`, and the retired keys remain parse-only fields omitted from fresh saves. Plan §3.2a (line 989) keeps `pane_borders` and `sidebar_collapsed` on `UiPrefs` as `#[serde(default, skip_serializing)]` because `prefs.rs` line 37 is `#[serde(default, deny_unknown_fields)]`; `ClientPrefs::from` ignores them, `ClientPrefs` does not carry them, no settings row shows them, and `save_prefs` never emits them. Line 991 and acceptance 3.2a.7 (line 1013) require `prefs_round_trip_and_reject_unknown_keys` to load a file naming both keys, omit both on a fresh save, and still fail the `mouse_captre` typo. Line 1001 records the same observation; line 1002 rejects dropping `deny_unknown_fields`. The false sentence that `UiPrefs` does not set `deny_unknown_fields` is absent. §3.3 (line 1103) adds `sidebar_side` and `sidebar_pinned` beside those two retired fields, parsed and ignored. Independent check: `crates/gclient/src/prefs.rs:37` is `#[serde(default, deny_unknown_fields)]` on `UiPrefs`, which still has both fields. Log finding 1 records the writer's FOLD, including the `skip_serializing` refinement that omits the keys on save.
>
> 2. gcr-r2-unnamed-pane-consumers — WITHDRAW
> The current plan fully resolves the finding. Deleting `UNNAMED_PANE` covers `app/mod.rs` and `chrome/labels.rs` with the label, command, then short-terminal-id fallback. §3.6 Targets include `crates/gclient/src/app/mod.rs::*` (line 1271) and `crates/gclient/src/ui/chrome/labels.rs::attention_label` / `::terminal_label` (lines 1273-1274). Line 1294 deletes the const and sets `display_name` to `label` → `command` → `short_terminal_id(&self.terminal_id)`; `app/mod.rs` drops the re-export; `attention_label`'s no-pane branch returns `short_terminal_id(attention_subject(entry_id))` and `terminal_label`'s no-pane, no-agent fallback returns `short_terminal_id(terminal_id)`. Acceptance 3.6.7 and 3.6.8 (lines 1320-1321) require `UNNAMED_PANE` absent from both files. Independent check: `gcode grep -w UNNAMED_PANE` readers are only `pane.rs:78/310`, `app/mod.rs:39`, `sidebar_model.rs:17/210`, and `labels.rs:11/105/137`, matching plan line 1306. The two `labels.rs` returns are the no-pane branches; pane-present paths already call `display_name`. `short_terminal_id` is the 8-character prefix at `pane.rs:84`. The `workspace_panes.rs` split (3.6.9) is the validator size-growth consequence of editing the 927-line `app/mod.rs`; it does not change this disposition.
>
> 3. gcr-r2-spawn-executor-ceiling — WITHDRAW
> The current plan fully resolves the finding. Acceptance enforces the repository under-1,000 ceiling and does not claim an under-850 result for this split. Independent check: `wc -l src/gobby/agents/spawn_executor.py` is 998; `gcode outline` places `_spawn_codex_terminal` at lines 228-259 (32 lines) and leaves shared `_runtime_spawn` at 330-587. Plan §1.2 (lines 400-407) states 998 lines, moves only that function, and says both files stay under the repository 1,000-line ceiling (about 970 and 40; no 850 figure is claimed). The planned `wc -l` check (line 461) requires both files under 1,000. Acceptance 1.2.5 (line 469) requires each file under 1,000 and `spawn_executor._spawn_codex_terminal` still resolving. Log finding 3 records the writer's FOLD of that repair.
>
> No finding is held. These three items send nothing to the Program Director gate.

Writer acknowledgment: all three findings are WITHDRAWN on the folded hash; none is held, so nothing goes to the Program Director gate. The council round is complete. No plan bytes changed for this entry, so the plan hash stays `2ad4684c87889daa5d05ec394697525dab4c8f4c5051b8c4c42ec741187d1186`.

## Resulting artifact
- Plan edits: sections 1.2, 3.1, 3.2a, 3.2b, 3.3, 3.4b, 3.6, 3.7, 3.8, 3.9 and V1 Verification (round-2 provenance).
- Validation: `uv run gobby plans validate .gobby/plans/gclient-chrome-refresh.md` exit 0, no warnings (three passes: the first two caught the `production-size-growth` rule on `app/mod.rs` and precedent file names in prose, both resolved as recorded above).
- Resulting hash: `2ad4684c87889daa5d05ec394697525dab4c8f4c5051b8c4c42ec741187d1186` (registry row `31284fe4-3929-4eec-9c69-e4d2a94d9d1b` refreshed by `gobby-plans:update_plan_hash` at 2026-09-22T22:26:34Z).
