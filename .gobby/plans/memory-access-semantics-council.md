# Memory access semantics — council round

Plan: `.gobby/plans/memory-access-semantics.md` (Plan ID: memory-access-semantics; epic #22641 "Memory access semantics, index dedupe, post-task review, and recall-signal retirement"; registry row `memory-access-semantics active implementation 22641`).
Planning task: #22816 "Memory access semantics plan council round: enhance, adversarial review and gate .gobby/plans/memory-access-semantics.md" (opened by the Program Director at 2026-09-24 01:0x CDT, second item in Josh's council queue after #22808).
Pre-round bytes: sha256 `69c0b97c4fef20fcad18eaf2bc98d0e694c03207e377782fc4b96783fd69e9eb`, the 2026-09-20 draft, untracked in the main checkout until this round's first commit; its coverage manifest `.gobby/plans/coverage/d45545c5-ded5-4335-b115-0245752edacf/22641/memory-access-semantics.coverage.yaml` was generated 2026-09-21T00:00:10Z against that hash with every row `missing` (no expansion yet).

Flow (Josh, via #22808 and restated in #22816): debate to consensus with no round cap; the adversary finalizes to the PD; the PD reviews and sends it to the assistant for Josh; expansion waits for Josh. Josh confirmed the 15-item Decision Record on 2026-09-20 (gobby#14037), so there is no elicitor: a finding that contradicts a confirmed decision goes to Josh through the PD rather than into the plan.

Participants:
- Writer: gobby#14425 (`912b3959-4924-4d93-a092-1a18636f2e31`), claude-fable-5-1; the only editor of the plan file and of this file.
- Enhancer: gobby#14422 (Codex gpt-5.6-sol).
- Adversary: gobby#14449 (`fa9b7651-ff74-47ff-b6d7-d848bd7b2d3e`, Grok 4.7 xhigh), the seat continued after gobby#14423 expired; it attacks only the post-enhancer hash.
- Program Director: gobby#14018 (`44738b98-9535-4ec5-8be5-57b345780be5`); gate and finalization recipient. Assistant gobby#14069 takes the final hash to Josh. Symbol lookup gobby#14332 (read-only).

Priority rule: a signed source outranks either reviewer: the #22641 description, the Decision Record in the plan, `ROADMAP.md`, the Golden Path rulings, `.impeccable.md`, the prompt book. Commits from this seat are by pathspec (`git commit --only -- <paths>`) with nothing left staged between steps (PD, 2026-09-24).

## Writer drift fold before round 1 (2026-09-24 01:1x-03:0x CDT)

The PD named three drift items in #22816; the writer verified every cited path (168 cited files exist except the seven the plan creates; 22 changed on 0.5.0 since the plan-time commit `1c126374`, 2026-09-20 18:48 CDT) and folded the following. Post-fold sha256 `395039f4a2e457c3627e67be7914773a9976b350b4739238d9566db0f218354c`; bare `uv run gobby plans validate .gobby/plans/memory-access-semantics.md` prints Phases 5 and nothing else; `git diff --check` clean.

- D1 Migration number (PD item 1). 445 is `445_drop_task_validation_system_prompt.sql`; HEAD's committed migrations end at `449_workspace_default_project.sql`; `450_drop_session_heuristic_title.sql` exists on #22740's branch (`b9303f8183`); 451 is the runbooks plan's (#22808). FOLD: every 445 of this plan's migration is 452 (2.1 title, the file name in Targets and 2.1.1, the `EmbeddedMigration` scope-reason, 4.1's "421, 427, 439, and now 452", 5.1's apply and `latest_version: 452`); 2.1's research context states the numbering with its sources.
- D2 Schema carriers on HEAD (PD item 2, citations). `crates/gcore/src/grant/bundle.rs` line 218 carries `latest_version: 449` in the no-postgres fallback (set by #22809, `8039cebc41`) and names no recall table (the nine tables appear only in `baseline.sql` and `catalog.manifest.json`), so the old scope-reason "drop grants for the retired tables" was wrong. `schema_contract.rs` literals are at lines 21 and 24, `cli_contract.rs` at 58; the manifest anchors are `memories.access_count` 2368, `memories.last_dreamed_at` 2420, `memories.source_type` 2444, with about 140 `recall_*` entries (not 150). FOLD: bundle.rs scope-reason and research context rewritten with those anchors; 2.1's verification adds `cargo test -p gobby-core --lib grant::tests` without `--features postgres` for the default-features identity guard (memory 24090e86).
- D3 1.1 anchors and test names (PD item 2). `memory.py` imports the judge at line 36 and registers `judge_shadow_relevance_tool` at 747-767 (the plan said 735-762). Neither cited test exists: `test_memory_rules_table_matches_bundled_rules` is `tests/docs/test_memory_guides.py::test_memory_guide_lists_every_bundled_lifecycle_rule` (line 43), and the reference-audit parity test is `tests/skills/test_reference_library.py::test_reference_contract_3_2_1` (line 60, through `coverage_errors` in `tests/skills/reference_library_helpers.py`). FOLD: 1.1.4 and the 1.1 research context name the real tests. `docs/reference-audit/memory.json` still lists the judge tool (324-332) and the seven recall-signals commands (591-675); `docs/guides/cli-commands.md` 882 and `docs/guides/sessions.md` 660 still mention the stack, so those targets stay.
- D4 1.2 anchor. `_record_delivered_hits` (52-84) is called at line 270 (the plan said 273). FOLD.
- D5 1.3 audit matrix (PD item 2). `docs/audits/configuration-audit.md` carries no row for any of the thirteen removed keys, at plan time (`1c126374`) or on HEAD (0 matches for `recall_signal`, `shadow_relevance`, `recall_drift`, `fitted_recall`, `memory_usefulness`); the half-life row is line 314, not "near 232" (232 is a `memory_recall.*` retired row). FOLD: the Constraints found-work bullet claiming the rows would go stale is dropped; 1.3 rewords only the half-life row and says why no retired rows are needed; 1.3.6 reads "the audit matrix's half-life row describes the new anchor"; the honesty test is named as `test_audit_registration_claims_match_runtime_contract`.
- D6 3.2 anchor. `get_memory` is at 423-457 (the plan said 421-455). FOLD.
- D7 3.3 on the #22708 recall path (PD item 2: "#22708 changed the hook recall path and late delivery under the 10 s cap"). `src/gobby/workflows/engine/mcp_injections.py` (`d92c3afd40`, `ba2373562a`, `d9bd4195fd`; #22807 `6cdbdd3213`) now caches `surface_memories` per session (`_CACHED_INJECTION_TOOLS` 30-37, `_INLINE_WAIT_CAP_SECONDS` 10 s, `_CACHE_TTL_SECONDS` 120 s) and delivers a timed-out result on the next hook (`_deliver_late_mcp_injections` 267-301). Inline (`_apply_cached_mcp_injection` 131-223), cached and late deliveries all call `_append_injected_mcp_result` (391-434), which runs `_format_memory_backed_result`, `_format_memory_index_result` (`delivery_formatting.py` 58-74) and `_filter_and_track_new_memories` (`injection_tracking.py` 14-61), so 3.3's stamp and sequence logic in the filter covers every path with one implementation. FOLD: that passage is in 3.3's research context, with the note that a late or cached delivery is a surfacing and advances the sequence, and that `_memory_surface_seq` is distinct from the existing once-per-parent-turn guard `_memory_surface_turn_seq` (`surface-memories-on-turn-start.yaml`, set `on_receipt`), which stays. Also refreshed: `hook_manager.py` is 947 lines (was 921), `_dedup_memory_results` is at 839-841 (was 813-815), the ingress helpers are lines 240-283 and 706-772 (about 110 lines); 3.3.7's under-850 target still holds (947 minus about 110).
- Verified unchanged (no edit): the five surfacing moments (`surface-memories-on-turn-start.yaml` plus the four rules in `surface-memories-on-tool-intent.yaml`); `parent_turn_seq` increments only for non-spawned sessions (`increment-parent-turn-seq.yaml`); `set_variable` is the only variable effect (`workflows/definitions.py` 173, 265, 370); `upsert_bounded_list_variable` at `state_manager.py` 340; `MemoryConfig` 437 with `access_debounce_seconds` 471, the stack fields 560-632 and validators 640-661; `MemoryUsefulnessConfig` `sessions.py` 95, `app.py` 157/287, `config_subscribers.py` 234; `rule_evaluator.py` 23-77, 310-339, 374-378; `memory_review.py` 74-87 and 129-246; `telemetry_loops.py` 159-197; `runner.py` 160; the judge cases in `test_rule_engine.py` (1908-1946), `test_mcp_dispatch_async.py` (97, 118) and `test_memory.py` (365-395); `GOBBY_TEST_GDAEMON=checkout` in `tests/fixtures/gdaemon_binary.py` 15. Files not touched on 0.5.0 since `1c126374` keep their line citations.
- Writer judgment calls for the council to weigh: (a) no retired audit rows for keys the matrix never carried (D5); (b) the no-postgres guard test added to 2.1's verification (D2); (c) the 3.3 passage rules that late and cached deliveries count as surfacings.

## Enhancer items (round 1)

Pending: the enhancer reviews the post-fold hash; each item is logged here with the enhancer's position, the writer's move (fold with the change named, or contest with reasoning) and the resolution.

## Adversary findings

Pending: the adversary attacks the post-enhancer hash and finalizes to gobby#14018.

## Program Director rulings

Pending.

## Resulting artifact

Post-fold (this entry): plan sha256 `395039f4a2e457c3627e67be7914773a9976b350b4739238d9566db0f218354c`; bare validate clean (Phases 5); `git diff --check` clean. Committed together with this file and the coverage manifest by pathspec; the commit sha is recorded in the next entry. The manifest still carries the pre-round `plan_hash` (`69c0b97c...`): `gobby plans validate` does not regenerate it, the daemon's `regenerate_coverage_manifest` does on the next registration or expansion event, so it is committed as found.
Pending: the enhancer round, the adversary round, the adversary's finalization with the final commit and hash, the PD's review and hand-off to the assistant (task criteria 1-5).
