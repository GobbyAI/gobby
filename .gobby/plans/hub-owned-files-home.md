Plan artifact: `.gobby/plans/hub-owned-files-home.md`

# Hub-Owned Files Home

**Plan ID:** hub-owned-files-home
**Epic:** #20330
**Contract:** `docs/architecture/hub-owned-files-home.md`

## Overview
`kind: framing`

Implement the reviewed architecture: one bind directory on the hub host holds `USER.md`, `_personal`, and the wiki home. The hub-local daemon (`datastore_mode: local`) is the only POSIX writer. Remote daemons never create `~/.gobby/personal` or `~/wiki`; they proxy file HTTP to `hub_daemon_url`.

## Constraints
`kind: framing`

Decision Record (settled in #20238 / `docs/architecture/hub-owned-files-home.md`; not reopened here):

1. One hub-host bind directory. Not `$GOBBY_HOME/personal`. Not a named Docker volume inside Postgres/Qdrant/Falkor.
2. Compose/pack/hub-backup may inventory that bind dir as `gobby_files` for lifecycle only.
3. Hub-local daemon is the only process that touches the POSIX tree.
4. No per-node mount, Syncthing, or replica. Nodes use hub HTTP.
5. `files_home` is required in `datastore_mode: local` when a bootstrap file is present. `hub_daemon_url` is required in `datastore_mode: remote` when a bootstrap file is present. The inverse is a hard `BootstrapConfigError`.
6. Tree: `<files_home>/{USER.md,_personal/{.gobby,notes,reminders,attachments},wiki/{wikis.json,personal,<topic>}}`.
7. Wiki home is not a vault. Flatten today’s `~/wiki/topics/<name>` to `<files_home>/wiki/<name>`. Personal vault is `wiki/personal`.
8. Reserved names: `USER.md`, `_personal`, `wiki` at files_home; reserved vault names `personal`, `_personal`, and `wiki`. Topic refusal is this plan. Project-vault refusal is inherited by #18779.
9. No dual-read of old daemon-home paths after a successful migrate.
10. `_personal` stays checkout-free. Do not `register` a `project_checkouts` row. Path helper is this plan’s; #19651 must not invent a per-machine personal root.
11. `<checkout>/wiki` stays until #18779. Fixture `gwiki --out` stays #19664.
12. `~/.gobby/comms_attachments` stays until Stage 3 / #17488.
13. No full pytest suite. `GOBBY_TEST_PROTECT=1` plus focused pytest, Ruff, Mypy, and the named gwiki/gcore tests. Every touched hand-maintained production source stays under 1,000 lines. Record current and projected counts in the owning leaf when a file is already above 800 lines.
14. Companion coverage ledger: `.gobby/plans/hub-owned-files-home.coverage-ledger.yaml`. It is required before expansion.
15. Cross-store transactional restore (one journal that atomically publishes Postgres plus files) is out of scope. Hub-backup already restores datastores sequentially. Files restore uses the same sequential model plus preflight and the existing confirm/`--force` collision policy. Operator rerun is the recovery path.
16. Missing bootstrap file still returns pre-DB port defaults via `_default_bootstrap_config`. That default is not a files owner: `require_files_home` and local owner writes fail until a present local bootstrap names an absolute `files_home`.
17. Local install that creates a bootstrap file takes an existing absolute `files_home` from the operator (`--files-home`). Writers persist that path; they do not create the root. That directory must not be a filesystem root and must be disjoint from `$GOBBY_HOME/personal`, `$GOBBY_HOME/projects`, and `~/wiki/topics` (no equality, ancestor, or descendant overlap).
18. `gobby files migrate`, `gobby pack` files-home snapshot, files-home unpack, backup snapshot, and restore run only while the hub daemon is stopped. They hold `claim_pid_file` with role `maintenance` for the whole campaign. `gobby start` acquires `claim_pid_file` with role `daemon` before managed-service startup, DB-backed runtime load, or `service_start`, and retains or atomically hands that claim to the direct or service-managed runner with no unlocked interval. `gobby stop` / restart refuse a `maintenance` claimant instead of signaling it; `_do_stop` performs that role check before `get_cli_runtime`, service status/stop, Docker handling, or `stop_daemon`. Direct `gwiki init` and `register_scope` refuse while role is `maintenance`; under a live `daemon` holder they proceed; when the lock is free they claim the singleton for the mutation and release after publication. The live daemon still claims role `daemon`. No live-daemon files-home reader-writer lock, no third claimant role, and no requirement that every gwiki CLI write go through the daemon.

## D1: Production project vault cutover
`kind: deferred`

```yaml
deferral:
  task_ref: "#18779"
  reason: "Empty-vault production activation writes project vaults into wiki/<project.name> after this home exists. That epic inherits refusal of reserved vault names personal, _personal, and wiki as project names. This plan only names the destination and refuses those names at the topic entry."
  owner: "repository-intelligence"
  original_acceptance_items:
    - D1.1
```

## D2: Fixture vaults and information model
`kind: deferred`

```yaml
deferral:
  task_ref: "#19664"
  reason: "Isolated gwiki --out fixture vaults and the redesigned information model stay in the wiki-output epic."
  owner: "repository-intelligence"
  original_acceptance_items:
    - D2.1
```

## D3: Comms attachments and machine-local execution
`kind: deferred`

```yaml
deferral:
  task_ref: "#17488"
  reason: "Telegram receive-side files and cron-vs-Telegram routing are Stage 3 machine-local execution, not hub user documents."
  owner: "remote-stack"
  original_acceptance_items:
    - D3.1
```

## P1: Bootstrap and path contract
`kind: framing`

**Goal:** Local-mode daemons resolve one absolute `files_home`. Remote-mode daemons have no files tree and know the hub owner URL. Both language projections carry the same typed fields.

### 1.1 Add files_home and hub_daemon_url resolution [category: code]
`kind: deliverable`

Targets:
- `src/gobby/config/bootstrap.py::BootstrapConfig`
- `src/gobby/config/bootstrap.py::BootstrapConfig.to_config_dict`
- `src/gobby/config/bootstrap.py::load_bootstrap`
- `src/gobby/config/bootstrap.py::_parse_datastore_mode`
- `src/gobby/config/bootstrap.py::_parse_optional_daemon_url`
- `src/gobby/config/bootstrap.py::_default_bootstrap_config`
- `src/gobby/config/bootstrap_io.py::write_bootstrap_yaml`
- `src/gobby/config/bootstrap_io.py::update_bootstrap_yaml`
- `src/gobby/utils/durable_file.py::exclusive_file_lock`
- `src/gobby/config/postgres_bootstrap.py::write_postgres_defaults`
- `src/gobby/config/app.py::DaemonConfig`
- `src/gobby/cli/install_setup.py::ensure_daemon_config`
- `src/gobby/cli/datastores.py::expose_datastores`
- `src/gobby/paths.py::*` — scope-reason: add get_files_home, require_files_home, held directory fd, no-follow descendant helpers, and typed files-home errors beside get_gobby_home
- `src/gobby/install/shared/config/bootstrap.yaml::*` — scope-reason: add files_home to the installed local-mode template
- `src/gobby/install/bundled_content_manifest.json::*` — scope-reason: regenerate after the installed bootstrap template gains files_home
- `crates/gcore/src/bootstrap.rs::*` — scope-reason: typed FilesHomeView for datastore_mode, files_home, and hub_daemon_url consumed by gwiki
- `tests/config/test_files_home.py`
- `tests/config/test_bootstrap.py::*` — scope-reason: every local/remote fixture must carry the newly required mode-specific field
- `tests/config/test_app_config.py::*` — scope-reason: replace missing/default local bootstrap expectations that omit files_home
- `tests/utils/test_daemon_url.py::*` — scope-reason: present-file daemon-url bootstraps carry the required mode-specific field
- `tests/config/test_bootstrap_postgres.py::*` — scope-reason: present-file postgres-bootstrap mappings carry files_home
- `tests/config/test_bootstrap_falkordb.py::*` — scope-reason: present-file falkordb-bootstrap mappings carry files_home
- `tests/cli/test_daemon_remote_mode.py::*` — scope-reason: present remote bootstrap fixtures carry hub_daemon_url
- `tests/test_ui_exposure.py::*` — scope-reason: present local bootstrap fixtures carry files_home
- `tests/cli/test_datastores_expose.py::*` — scope-reason: expose staging and rollback keep files_home
- `tests/cli/test_cli_install.py::*` — scope-reason: present local install fixtures carry files_home
- `tests/cli/test_runtime.py::*` — scope-reason: present runtime bootstraps carry files_home
- `tests/cli/test_daemon_coverage.py::*` — scope-reason: present daemon-status bootstraps carry files_home
- `tests/e2e/conftest.py::*` — scope-reason: present e2e local bootstraps carry files_home
- `tests/e2e/test_single_active_daemon.py::*` — scope-reason: present e2e daemon bootstraps carry files_home
- `src/gobby/cli/installers/postgres.py::_install_docker`
- `src/gobby/cli/installers/postgres.py::_write_bootstrap_defaults`
- `tests/cli/installers/test_postgres_installer.py::*` — scope-reason: present installer bootstraps carry files_home; compose work waits on a valid local mapping
- `tests/storage/hub/test_postgres_pool_atexit.py::*` — scope-reason: present pool-atexit bootstraps carry files_home
- `tests/e2e/test_runtime_boundary.py::*` — scope-reason: present runtime-boundary bootstraps carry files_home

Add bootstrap fields:

```python
files_home: str | None = None          # absolute path; local mode only
hub_daemon_url: str | None = None      # http(s) origin of the files-owner daemon
```

`load_bootstrap` rules when the bootstrap file exists:

- `datastore_mode: local`: `files_home` required, non-empty, absolute (no `~`, no relative). Missing/relative/`~` → `BootstrapConfigError`. `hub_daemon_url` if present → `BootstrapConfigError` (this process is the owner).
- `datastore_mode: remote`: `hub_daemon_url` required. Parse it as an HTTP(S) origin: scheme `http` or `https`, host required, optional port, no userinfo, no query, no fragment, path empty or `/` only. Store the origin with no trailing slash. Any other shape → `BootstrapConfigError`. `files_home` if present → `BootstrapConfigError`. Refuse an origin that equals this process’s own advertised daemon origin (`daemon_url` or `http(s)://{bind_host}:{daemon_port}`).
- Never default `files_home` to `$GOBBY_HOME` or `$HOME/.gobby/...`.

Missing bootstrap file: keep today’s `_default_bootstrap_config()` for pre-DB port readers. Do not invent `files_home`. `require_files_home` on that default raises `FilesHomeError`. Tests that construct a **present** local or remote bootstrap must include the required field; replace stale missing/default-is-owner expectations.

```python
def get_files_home() -> Path | None:
    """Local-mode files_home, or None on a remote-mode daemon."""

def require_files_home() -> Path:
    """Return files_home or raise FilesHomeNotOnThisDaemonError / FilesHomeError."""
```

`get_files_home` reads the already-loaded bootstrap (same source as `get_gobby_home` uses env/home). Do not mkdir. `require_files_home` opens the configured directory, records `(st_dev, st_ino)`, and holds that directory fd. Missing directory is `FilesHomeError`, not a silent create. A later publication that sees a different directory identity at the same path is also `FilesHomeError`. Python profile, identity, attachment, migrate, unpack, restore, pack emission, and files-home backup emission writers perform descendant mkdir/open/replace/unlink/archive-add through that held fd (no-follow; no pathname walk from a later re-resolved string). Do not add a separately named capability type. A root or ancestor swap between open and publication is `FilesHomeError`.

A present local `files_home` must not be a filesystem root (`/` or a path whose parent is itself).

Writers that emit or rewrite a local bootstrap mapping (`write_bootstrap_yaml`, `write_postgres_defaults`, `ensure_daemon_config`, installed template) must persist `files_home` when `datastore_mode` is local, and persist `hub_daemon_url` when remote. A writer must not persist a local mapping that would fail `load_bootstrap`. When `ensure_daemon_config` creates a local bootstrap, it takes a caller-supplied existing absolute `files_home` and writes that value into the template copy or generated fallback. It must not emit a placeholder that fails `load_bootstrap`. `to_config_dict` emits both fields; `DaemonConfig` carries them so bootstrap values are not stranded.

`write_bootstrap_yaml`, `update_bootstrap_yaml`, `write_postgres_defaults`, `ensure_daemon_config`, installed-template publication, and `expose_datastores` staging/rollback all publish through `exclusive_file_lock` and one durable validated replace path. `update_bootstrap_yaml` uses a private already-locked writer so it does not re-acquire the sidecar. After acquiring the lock, re-read the file. Do not add a second lock type. Tests cover two-process `update_bootstrap_yaml` versus direct `write_bootstrap_yaml` create and update versus exposure rollback.

Present-file local/remote fixtures in the existing bootstrap, app-config, daemon-url, postgres-bootstrap, falkordb-bootstrap, remote-daemon, UI-exposure, datastores-expose, CLI install, runtime, daemon-coverage, e2e, PostgreSQL installer, pool-atexit, and runtime-boundary suites include the required field. True missing-file default cases stay owner-less.

A present local bootstrap that predates `files_home` cannot pass `load_bootstrap`. The 1.1 helper is exclusive-lock raw-read of the existing mapping without `load_bootstrap`, preserve other keys, inject the validated existing absolute root, validate the completed mapping, and durably replace it. The public `install --files-home` front door that calls that helper before identity or services is § 2.1.

Standalone PostgreSQL Docker install (`_install_docker`) must not start compose, asset, or datastore work from a missing or legacy-invalid local bootstrap. Either a present valid local mapping already names `files_home`, or the installer persists an explicit existing absolute `files_home` first. Otherwise refuse before those effects with a typed instruction to run local `install --files-home`. `_write_bootstrap_defaults` / `write_postgres_defaults` still must not persist a local mapping that would fail `load_bootstrap`.

`expose_datastores` stages and rolls back through `write_bootstrap_yaml`. Successful exposure and rollback both keep `files_home` on a local mapping.

Rust `FilesHomeView` in `bootstrap.rs` is a validated typed view: `datastore_mode`, `files_home: Option<PathBuf>`, `hub_daemon_url: Option<String>`. Same present-file rules as Python (absolute owner path; remote origin; no tilde/relative; inverse field is an error). Endpoint parse (`DaemonEndpoint`) still falls back to host/port defaults when those keys are absent. Add `reads_bootstrap_with_files_home` covering owner, remote, missing file, relative, tilde, and endpoint-default cases. gwiki consumes this view in § 4.1; tolerance-only unknown-key ignore is not enough.

**Acceptance:**

- 1.1.1 - Local bootstrap file requires absolute `files_home` and refuses `hub_daemon_url`. test: `tests/config/test_files_home.py`.
- 1.1.2 - Remote bootstrap file requires `hub_daemon_url` and refuses `files_home`. test: `tests/config/test_files_home.py`.
- 1.1.3 - `require_files_home` returns the configured path in local mode, raises `FilesHomeNotOnThisDaemonError` in remote mode, and raises `FilesHomeError` when the directory is missing. test: `tests/config/test_files_home.py`.
- 1.1.4 - Installed bootstrap template documents `files_home` for local mode. file: `src/gobby/install/shared/config/bootstrap.yaml`.
- 1.1.5 - Rust `FilesHomeView` accepts owner and remote cases and refuses missing/relative/tilde owner paths; daemon endpoint defaults still load. test: `crates/gcore/src/bootstrap.rs::reads_bootstrap_with_files_home`.
- 1.1.6 - Remote `hub_daemon_url` accepts `http(s)://host[:port]`, refuses userinfo/query/fragment/non-root path, and refuses this process’s own origin. test: `tests/config/test_files_home.py`.
- 1.1.7 - Present-file local/remote fixtures in the existing bootstrap suites include the required field; missing-file defaults are not treated as a files owner. test: `tests/config/test_bootstrap.py`.
- 1.1.8 - Local bootstrap writers persist `files_home`; remote writers persist `hub_daemon_url`. test: `tests/config/test_files_home.py`.
- 1.1.9 - Creating a local bootstrap through `ensure_daemon_config` persists the caller-supplied absolute `files_home` and does not emit a mapping that fails `load_bootstrap`. test: `tests/config/test_files_home.py`.
- 1.1.10 - Present-file fixtures in the daemon-url, postgres-bootstrap, and falkordb-bootstrap suites include the required mode-specific field. test: `tests/utils/test_daemon_url.py`.
- 1.1.11 - Local and remote `to_config_dict` output includes the mode-specific field, and `DaemonConfig` carries both fields. test: `tests/config/test_app_config.py`.
- 1.1.12 - Present-file fixtures in the remote-daemon and UI-exposure suites include the required mode-specific field. test: `tests/cli/test_daemon_remote_mode.py`.
- 1.1.13 - Concurrent `update_bootstrap_yaml` writers hold `exclusive_file_lock` across re-read, update, validation, and replace; neither published mapping drops the other writer's owner field. test: `tests/config/test_files_home.py`.
- 1.1.14 - `expose_datastores` staging and rollback keep `files_home` on the published local mapping. test: `tests/cli/test_datastores_expose.py`.
- 1.1.15 - Present-file fixtures in the CLI install, runtime, and daemon-coverage suites include the required mode-specific field. test: `tests/cli/test_cli_install.py`.
- 1.1.16 - A present legacy local mapping without `files_home` upgrades via exclusive-lock raw-read, inject, validate, and durable replace without calling `load_bootstrap`; the published mapping then loads. test: `tests/config/test_files_home.py`.
- 1.1.17 - Standalone PostgreSQL Docker install refuses compose, asset, and datastore work unless a present valid local bootstrap already names `files_home`, or persists an explicit existing `files_home` first. test: `tests/cli/installers/test_postgres_installer.py`.
- 1.1.18 - Present-file fixtures in the PostgreSQL installer, pool-atexit, and runtime-boundary suites include the required mode-specific field. test: `tests/cli/installers/test_postgres_installer.py`.
- 1.1.19 - Direct `write_bootstrap_yaml` create, `update_bootstrap_yaml`, and `expose_datastores` rollback share `exclusive_file_lock` and one durable validated replace path; update-versus-create and update-versus-rollback races keep every owner field. test: `tests/config/test_files_home.py`.
- 1.1.20 - `require_files_home` holds the directory fd; a root or ancestor swap before descendant publication raises `FilesHomeError` and creates no new bind root. test: `tests/config/test_files_home.py`.
- 1.1.21 - A present local mapping whose `files_home` is a filesystem root is `BootstrapConfigError`. test: `tests/config/test_files_home.py`.

## P2: Hub-owner personal tree and USER.md
`kind: framing`

**Goal:** On the hub owner, personal identity and the working profile live under `files_home`. Nodes keep the shared checkout-free sentinel row and never materialize a personal directory.

### 2.1 Retarget personal identity and USER.md [category: code] (depends: 1.1)
`kind: deliverable`

Targets:
- `src/gobby/storage/projects.py::personal_project_path`
- `src/gobby/storage/projects.py::ensure_personal_project`
- `src/gobby/storage/projects.py::ensure_personal_project_identity`
- `src/gobby/hooks/event_handlers/_session_start/profile.py::*` — scope-reason: owner USER.md read/seed/write and remote raise; no daemon-home fallback
- `src/gobby/cli/install.py::*` — scope-reason: gate filesystem identity to local mode during install
- `src/gobby/storage/hub/runtime.py::*` — scope-reason: hub-open keeps the sentinel row and skips remote filesystem identity
- `src/gobby/cli/utils_config.py::*` — scope-reason: config open keeps the sentinel row and skips remote filesystem identity
- `tests/storage/test_project_manager.py::*` — scope-reason: personal identity helper, repair, preservation, corruption, and write-failure cases use configured files_home; unrelated project CRUD stays on existing fixtures
- `tests/hooks/test_session_user_profile.py::*` — scope-reason: retarget every profile fixture off $GOBBY_HOME/personal
- `tests/cli/test_install_coverage.py::*` — scope-reason: install identity stays local-only
- `tests/cli/test_install_front_door.py::*` — scope-reason: front-door install identity stays local-only
- `src/gobby/cli/installers/remote_preflight.py::_credential_errors`
- `src/gobby/cli/installers/remote_preflight.py::run_remote_preflight`
- `tests/cli/test_cli_utils.py::*` — scope-reason: config-open keeps the sentinel and skips remote filesystem identity
- `tests/cli/test_utils_coverage.py::*` — scope-reason: config-open coverage fixture is explicitly local; remote skips filesystem identity
- `tests/storage/test_personal_remote_init.py`

```python
def personal_project_path(gobby_home: Path | None = None) -> Path:
    """Hub-owner _personal directory. Raises FilesHomeNotOnThisDaemonError on a node."""
    return require_files_home() / "_personal"
```

`ensure_personal_project_identity` is local-owner only. It writes `<files_home>/_personal/.gobby/project.json` with `id=PERSONAL_PROJECT_ID`, `name=_personal`. It does not mkdir under `$GOBBY_HOME/personal`. It does not call checkout `register` / `rebind`. On remote it must not be invoked; install / hub-open / config-open call it only when `datastore_mode == "local"`.

Local full install takes `--files-home` naming an existing absolute directory (no root create). Refuse a filesystem root and refuse equality, ancestor, or descendant overlap with `$GOBBY_HOME/personal`, `$GOBBY_HOME/projects`, and `~/wiki/topics` before any bootstrap write. Persist that bootstrap via `ensure_daemon_config` before `ensure_personal_project_identity` and before managed-service startup. A bootstrap write failure leaves no personal identity marker and starts no managed services. When a present local bootstrap already exists without `files_home`, this leaf owns the public `install --files-home` front door (Click option, prompt, and coverage/front-door tests) and calls the § 1.1 helper: raw-read, inject, validate, and replace before identity or services. Remote install does not take `--files-home`. It still requires the existing hub `local_cli_token` already on disk (the installer does not generate or rotate it). `run_remote_preflight` probes `GET /api/files/user-md` on `hub_daemon_url` with that token and `X-Gobby-Files-Proxy-Hop: 1`. Success is a 200 JSON body from a local files owner within the existing DaemonClient timeout. Auth failure, network failure, missing root, hop refusal, and a remote-target probe are typed install failures and start no managed services. Do not add a second credential store or `--hub-token-stdin`.

`ensure_personal_project` always upserts the shared checkout-free sentinel row. It must not create a `project_checkouts` row. Filesystem marker creation is local-only. If `projects.repo_path` still exists (pre-#19651 P6), write the hub `_personal` path as unread campaign data on the hub owner only — never a per-machine `$GOBBY_HOME/personal`. After that column is gone, stop writing it. Do not add a compatibility reader for the old path.

`read_user_profile_content` on the hub owner reads `require_files_home() / "USER.md"`. Missing file → `""`. It does not read `$GOBBY_HOME/personal/USER.md`. On a node it raises `FilesHomeNotOnThisDaemonError` so § 5.1 can fetch. `seed_user_profile_content` propagates empty on that error until 5.1 wires the fetch.

Do not create `notes/` / `reminders/` here except as empty dirs from the migrate leaf.

**Acceptance:**

- 2.1.1 - `personal_project_path` is `<files_home>/_personal` and never `$GOBBY_HOME/personal`. test: `tests/storage/test_project_manager.py::TestConstants.test_personal_project_path`.
- 2.1.2 - Identity marker is written under files_home and checkout register is not called. test: `tests/storage/test_project_manager.py::TestPersonalProjectEnsure`.
- 2.1.3 - Hub-owner profile reads `<files_home>/USER.md` only. test: `tests/hooks/test_session_user_profile.py::test_read_user_profile_content_reads_personal_user_md`.
- 2.1.4 - Node `read_user_profile_content` does not read a daemon-home file. test: `tests/hooks/test_session_user_profile.py::test_read_user_profile_content_reads_personal_user_md`.
- 2.1.5 - Remote install, hub-open, and config-open upsert the sentinel row and do not create a node-local `_personal` tree. test: `tests/storage/test_personal_remote_init.py`.
- 2.1.6 - Local install still writes the files_home identity marker. test: `tests/cli/test_install_coverage.py`.
- 2.1.7 - Local install persists `files_home` before personal identity or managed-service startup; an injected bootstrap write failure performs neither. test: `tests/cli/test_install_coverage.py`.
- 2.1.8 - `--files-home` must name an existing absolute directory. test: `tests/cli/test_install_front_door.py`.
- 2.1.9 - Remote install refuses when the hub `local_cli_token` is missing and does not start managed services. test: `tests/cli/test_install_coverage.py`.
- 2.1.10 - Local `install --files-home` upgrades a present legacy bootstrap and then identity writes use that `files_home`. test: `tests/cli/test_install_coverage.py`.
- 2.1.11 - Local `--files-home` refuses a filesystem root and any equality, ancestor, or descendant overlap with `$GOBBY_HOME/personal`, `$GOBBY_HOME/projects`, or `~/wiki/topics` with zero bootstrap or identity writes. test: `tests/cli/test_install_front_door.py`.
- 2.1.12 - `tests/cli/test_utils_coverage.py` constructs config-open as explicit local and calls `ensure_personal_project`; a remote case upserts the sentinel without filesystem identity. test: `tests/cli/test_utils_coverage.py`.
- 2.1.13 - Remote install treats invalid-token or auth rejection, network or timeout failure, and a missing owner root as typed install errors and starts no managed services. test: `tests/cli/test_install_coverage.py`.

## P3: Chat attachments
`kind: framing`

**Goal:** Chat upload bytes reconstruct under `_personal/attachments` on the hub owner. Every reader, deleter, and cleanup job uses one owner-only resolver.

### 3.1 Reconstruct chat attachment paths [category: code] (depends: 2.1)
`kind: deliverable`

Targets:
- `src/gobby/servers/chat_attachment_files.py::*` — scope-reason: one owner-only resolver and unlink that never follows a stale absolute local_path
- `src/gobby/servers/routes/chat_attachments.py::_attachment_dir`
- `src/gobby/servers/routes/chat_attachments.py::upload_attachment`
- `src/gobby/servers/routes/chat_attachments.py::get_attachment_content`
- `src/gobby/servers/routes/chat_attachments.py::delete_attachment`
- `src/gobby/servers/routes/chat.py::*` — scope-reason: conversation delete uses the owner resolver
- `src/gobby/servers/websocket/handlers/session_lifecycle.py::*` — scope-reason: session cleanup uses the owner resolver
- `src/gobby/servers/websocket/chat_attachments.py::prepare_message_attachments`
- `src/gobby/servers/websocket/chat_attachments.py::_bind_attachments_sync`
- `src/gobby/storage/chat_attachments.py::*` — scope-reason: hub-owned rows are fetched by attachment id, then filtered by row project_id; tokenized deletion lease
- `src/gobby/runner_maintenance/storage_hygiene.py::cleanup_chat_attachments_loop`
- `src/gobby/runner_maintenance/storage_hygiene.py::_remove_stale_chat_attachment_file`
- `src/gobby/utils/durable_file.py::durable_replace`
- `crates/gcore/assets/schema/baseline.sql::*` — scope-reason: add chat_attachments deletion-lease columns on the hub schema
- `crates/gcore/assets/schema/migrations/386_chat_attachments_deletion_lease.sql`
- `tests/storage/test_chat_attachments.py::*` — scope-reason: hub rows are visible by project and attachment id; bind serializes against cleanup
- `tests/servers/websocket/test_chat_attachments.py::*` — scope-reason: retarget every attachment path assertion off $GOBBY_HOME/projects
- `tests/servers/routes/test_chat_attachments.py::*` — scope-reason: HTTP upload/download/delete use reconstructed files_home paths
- `tests/servers/routes/test_chat_routes.py::*` — scope-reason: conversation-delete fixtures use the reconstructed relative locator, not an absolute stored path
- `tests/test_runner_chat_attachments_cleanup.py::*` — scope-reason: stale-upload cleanup uses the owner resolver and skips remote unlink

```python
def resolve_attachment_dir(project_id: str, attachment_id: str) -> Path:
    root = require_files_home()  # re-stat; missing root is FilesHomeError
    return (
        root / "_personal" / "attachments"
        / _safe_path_part(project_id, "project")
        / attachment_id[:2]
        / attachment_id
    )
```

`_attachment_dir` delegates to that resolver. The column `local_path` stays required. New uploads persist exactly this files_home-relative locator and nothing else:

`_personal/attachments/<safe_project_id>/<attachment_id[:2]>/<attachment_id>/<filename>`

Every read reconstructs the same path from `(project_id, attachment_id, filename)` under the held `require_files_home` directory fd. Do not follow a stored absolute path. A stored value that is absolute or that disagrees with the reconstructed locator is ignored for resolution; § 6.1 rewrites every legacy row to the relative locator above. Conversation-delete, websocket cleanup, and stale-upload tests bind fixtures through the resolver, not an arbitrary absolute `local_path`.

HTTP GET `/{attachment_id}/content` and DELETE `/{attachment_id}` stay attachment_id-only. They load the hub row by attachment id, then reconstruct using that row's `project_id` and `filename`. After the load, storage predicates include both `project_id` and `id`. Remote proxy forwards the same attachment_id URL. WebSocket prepare/bind still send attachment ids and use `project_id` from the loaded row. Do not add a new route or message field.

Conversation delete, websocket lifecycle cleanup, and stale-upload hygiene call the same resolver/unlink. They must not `Path(record.local_path).unlink`. Stored-chat WebSocket prepare/bind (`prepare_message_attachments`) looks up and binds by attachment id; it must not require `machine_id == require_machine_id()`. Terminal-proxy session attachments stay machine-local and are not this resolver.

Remote-mode cleanup returns before any shared-row mutation. On the owner, cleanup takes a tokenized deletion lease: persist `claim_token` and `claimed_at` on the row. CAS claim succeeds only from an unclaimed or expired lease (expiry 60 seconds). Bind CAS accepts only `claim_token IS NULL`; expired-token reclaim is reserved for cleanup. Another owner worker may reclaim an expired lease and continue. Unlink through the reconstructed resolver first; `FileNotFoundError` counts as completed cleanup. Any other unlink failure keeps the row and releases the claim. Delete only the still-claimed row after unlink succeeds. Track and shield the unlink worker. While it runs, keep or renew the token so the claim stays exclusive even past 60 seconds. Cancellation awaits that worker's terminal result before CAS delete or release, then re-raises. A completed unlink plus missing file is success. After unlink succeeds and the worker dies before row delete, expiry must not make the surviving row bindable: cleanup reclaim treats `FileNotFoundError` as completed deletion, and bind proceeds only after a live-file unclaimed state or row deletion. Remote daemons never delete the only hub locator. Dual-worker, crash-after-claim, crash-after-unlink, bind-versus-cleanup, cancellation-during-unlink, and expiry-after-unlink barrier tests cover the lease.

Writes under files_home create descendant directories only through the held files_home directory fd. They must not `mkdir` the configured root itself. `durable_replace` under files_home uses that no-root-create, held-fd publication. Lost, replaced, or ancestor-swapped bind → `FilesHomeError`, not a new local directory of canonical data.

Remote-mode upload/download/delete is § 5.1. This leaf’s resolver raises `FilesHomeNotOnThisDaemonError` on a node.

Do not move `~/.gobby/comms_attachments`.

**Acceptance:**

- 3.1.1 - Upload writes under `<files_home>/_personal/attachments/<project>/<id[:2]>/<id>/`. test: `tests/servers/websocket/test_chat_attachments.py`.
- 3.1.2 - Download/delete resolve the same reconstructed path and do not require a stored absolute `local_path`. test: `tests/servers/websocket/test_chat_attachments.py`.
- 3.1.3 - Node resolver does not create `$GOBBY_HOME/projects/.../attachments`. test: `tests/servers/websocket/test_chat_attachments.py`.
- 3.1.4 - Conversation delete, session cleanup, and stale-upload hygiene unlink via the resolver, not `record.local_path`. test: `tests/servers/routes/test_chat_attachments.py`.
- 3.1.5 - Remote cleanup does not unlink a node-local file. test: `tests/test_runner_chat_attachments_cleanup.py`.
- 3.1.6 - A vanished files_home root during upload or USER.md write raises `FilesHomeError` and does not recreate the bind path. test: `tests/servers/routes/test_chat_attachments.py`.
- 3.1.7 - Stored-chat WebSocket prepare/bind finds a hub row by attachment id after a remote upload. test: `tests/servers/websocket/test_chat_attachments.py`.
- 3.1.8 - Remote cleanup mutates no shared attachment row; owner unlink failure keeps the row and a later run finishes the delete. test: `tests/test_runner_chat_attachments_cleanup.py`.
- 3.1.9 - A replaced directory at the configured files_home path raises `FilesHomeError` for upload and USER.md write. test: `tests/servers/routes/test_chat_attachments.py`.
- 3.1.10 - Storage-layer fetch, bind, and cleanup use project and attachment id visibility, not current-process machine equality. test: `tests/storage/test_chat_attachments.py`.
- 3.1.11 - Bind CAS accepts only `claim_token IS NULL`; owner unlink failure keeps the row and releases the claim. test: `tests/storage/test_chat_attachments.py`.
- 3.1.12 - New uploads persist the files_home-relative locator `_personal/attachments/<project>/<id[:2]>/<id>/<filename>` and never persist an absolute path; conversation-delete fixtures resolve through that locator. test: `tests/servers/routes/test_chat_attachments.py`.
- 3.1.13 - HTTP GET/DELETE remain attachment_id-only; they load the row by id and reconstruct from that row's `project_id` and `filename`. test: `tests/servers/routes/test_chat_attachments.py`.
- 3.1.14 - An expired deletion lease is reclaimable by cleanup; crash after claim, after unlink, and before row delete each converge; cancellation awaits the unlink worker before CAS release or delete. test: `tests/storage/test_chat_attachments.py`.
- 3.1.15 - An ancestor swap under `files_home` during upload or unlink raises `FilesHomeError` and does not publish or delete through the replaced path. test: `tests/servers/routes/test_chat_attachments.py`.
- 3.1.16 - Cancellation after the unlink worker starts, including a worker that outlives 60 seconds, keeps bind and reclaim excluded until that worker terminates. test: `tests/storage/test_chat_attachments.py`.
- 3.1.17 - After unlink succeeds and the worker crashes before row delete, lease expiry does not let bind attach a row whose bytes are gone. test: `tests/storage/test_chat_attachments.py`.

## P4: Wiki home
`kind: framing`

**Goal:** On the hub owner, topic and personal vaults are children of `<files_home>/wiki`. The home itself is not a vault. Ongoing registry writes keep the migrated relative shape.

### 4.1 Point gwiki and daemon wiki scope at files_home/wiki [category: code] (depends: 2.1)
`kind: deliverable`

Targets:
- `crates/gwiki/src/scope.rs::resolve_hub_path`
- `crates/gwiki/src/scope.rs::resolve_topic`
- `crates/gwiki/src/scope.rs::default_hub_path`
- `crates/gwiki/src/scope.rs::resolves_global_topic`
- `crates/gwiki/src/scope.rs::rejects_invalid_topic_names`
- `crates/gwiki/src/scope.rs::resolve_project_from_root`
- `crates/gwiki/src/models.rs::validate_topic_name`
- `crates/gwiki/src/registry.rs::register_scope`
- `crates/gwiki/src/registry.rs::register_overwrites_existing_entries`
- `crates/gwiki/src/vault.rs::initialize`
- `crates/gwiki/src/vault.rs::create_dir`
- `crates/gwiki/src/obsidian.rs::seed_app_json`
- `crates/gwiki/src/obsidian.rs::ensure_gitignore_obsidian`
- `crates/gwiki/src/commands/init.rs::*` — scope-reason: init uses relative registry writes after flatten
- `src/gobby/servers/routes/wiki.py::*` — scope-reason: topic/personal entry uses files_home wiki home and reserved-name mapping
- `src/gobby/wiki/scope_resolution.py::resolve_wiki_scope`
- `src/gobby/wiki/scope_resolution.py::resolve_scope_identity`
- `src/gobby/wiki/scope_resolution.py::_resolve_project_root_sync`
- `crates/gwiki/tests/common/mod.rs::*` — scope-reason: integration fixtures use wiki-home children, not hub/topics/<name>
- `crates/gwiki/tests/cli_init.rs::*` — scope-reason: init assertions use direct wiki-home children
- `crates/gwiki/tests/cli_export.rs::*` — scope-reason: export fixtures use direct wiki-home children
- `tests/wiki/test_wiki_files_home.py`
- `tests/wiki/test_scope_resolution.py::*` — scope-reason: reserved names, personal sentinel without repo_path, and relative registry at the shared resolver

When a present bootstrap says `datastore_mode: remote`, topic/personal filesystem resolution refuses before evaluating `GOBBY_WIKI_HUB` or `wiki.hub_path` / `gwiki.hub_path`. Those overrides are local-owner or missing-bootstrap fixture contexts only.

`resolve_hub_path` order on a local owner or missing-bootstrap fixture:

1. `GOBBY_WIKI_HUB` if set (tests/explicit override).
2. Else `wiki.hub_path` / `gwiki.hub_path` config if set.
3. Else, when Rust `FilesHomeView` says this process is the hub owner, `<files_home>/wiki`.
4. Else do **not** fall back to `~/wiki`. Error: configure `GOBBY_WIKI_HUB` or run on the files owner.

`resolve_topic` uses `hub.join(topic)` — not `hub.join("topics").join(topic)`. The wiki registry file stays at the wiki home. Reserved topic name `personal` is allowed only as the personal vault; creating a second topic named `personal` is a typed refusal. Refuse `_personal` and `wiki` as topic names. The refusal is the same typed `WikiError::InvalidScope` family `rejects_invalid_topic_names` already uses for `.` / `..` / separators. `validate_topic_name` is the shared guard. Daemon `_resolve_scope` maps that error at the topic entry point and does not construct a local gateway for a reserved name.

Table-driven cases for `personal`, `_personal`, and `wiki`: topic creation/resolution returns the reserved-name error at the Rust resolver, `_resolve_scope`, and the Python daemon topic entry. An explicit personal-scope request (project id `PERSONAL_PROJECT_ID` / `_personal`) still resolves exactly to `<files_home>/wiki/personal`.

Personal scope (project id `PERSONAL_PROJECT_ID` / `_personal`) resolves to `<files_home>/wiki/personal`, never `_personal/wiki`. `resolve_project_from_root` is the checkout-root branch: after verifying the project id is `PERSONAL_PROJECT_ID` and the project root is the configured `<files_home>/_personal`, return `<files_home>/wiki/personal` with the wiki-home registry. Every other project root keeps checkout-adjacent `resolve_vault_dir(project_root)` until #18779.

`register_scope` serializes child paths relative to the wiki-home registry parent and rejects escapes. It must not persist an absolute `scope.root()` or a `topics/` prefix. `register_overwrites_existing_entries` must construct and assert direct-child relative paths (no `topics/` prefix, no absolute stored path) and keep escape rejection.

`initialize`, `create_dir`, `seed_app_json`, and `ensure_gitignore_obsidian` take a verified owner-root identity (same `(st_dev, st_ino)` as `require_files_home` / Rust FilesHomeView). Create descendants relative to that handle. Do not `create_dir_all` a missing files_home root or walk through a symlink or non-directory ancestor. Removal or replacement of the owner root between verification and each descendant write is a typed refusal.

`gwiki init` and `register_scope` consult the same singleton record `claim_pid_file` writes and hold exclusion through the complete mutation, including every vault and registry publication. When the claimant role is `maintenance`, refuse before any vault or registry mutation. Under a live `daemon` holder they proceed, because that holder already excludes maintenance. When the lock is free they atomically claim the singleton for the direct mutation and release after publication. Do not add a third claimant role. A live daemon still does not require routing those writes through the daemon.

`resolve_wiki_scope` / `resolve_scope_identity` recognize `PERSONAL_PROJECT_ID` and `_personal` as a first-class owner scope and do not request `repo_path`. Reserved topic names `personal`, `_personal`, and `wiki` refuse in that shared resolver so HTTP, MCP, scheduled registration, and parked-row recovery share one check. Remote topic/personal owner dispatch runs before any local path-existence check.

Daemon `_resolve_scope` / `_gateway` must use the same hub path for topic and personal scopes. A topic request on the hub owner must not look in `~/wiki/topics`.

Wiki home must not carry vault metadata. Creating it is a bug. Init and `register_scope` fail when the owner `files_home` directory is missing; they do not create that root.

**Acceptance:**

- 4.1.1 - Topic `foo` resolves to `<files_home>/wiki/foo`, not `.../wiki/topics/foo`. test: `crates/gwiki/src/scope.rs::resolves_global_topic`.
- 4.1.2 - Default hub path is files_home/wiki on the owner and does not silently become `~/wiki`. test: `tests/wiki/test_wiki_files_home.py`.
- 4.1.3 - Personal wiki scope is `<files_home>/wiki/personal`. test: `tests/wiki/test_wiki_files_home.py`.
- 4.1.4 - Daemon topic/personal routes use that same home. test: `tests/wiki/test_wiki_files_home.py`.
- 4.1.5 - Topic names `personal`, `_personal`, and `wiki` are reserved-name errors at the Rust resolver. test: `crates/gwiki/src/scope.rs::rejects_invalid_topic_names`.
- 4.1.6 - The same three names refuse at the Python daemon topic entry; explicit personal scope still resolves to `<files_home>/wiki/personal`. test: `tests/wiki/test_wiki_files_home.py`.
- 4.1.7 - New topic registration writes wiki-home-relative child paths and rejects escapes. test: `crates/gwiki/src/registry.rs`.
- 4.1.8 - Remote-mode gwiki refuses topic/personal filesystem resolution even when `GOBBY_WIKI_HUB` or a config hub path is set. test: `tests/wiki/test_wiki_files_home.py`.
- 4.1.9 - Personal sentinel resolves without `repo_path`; reserved topic names refuse in the shared Python resolver used by HTTP, MCP, and scheduled registration. test: `tests/wiki/test_scope_resolution.py`.
- 4.1.10 - Rust init/`register_scope` and the Python wiki owner entry refuse a missing or replaced files_home root and do not recreate it. test: `tests/wiki/test_wiki_files_home.py`.
- 4.1.11 - Rust init and export integration fixtures treat topic vaults as direct wiki-home children. test: `crates/gwiki/tests/cli_init.rs`.
- 4.1.12 - After owner bootstrap and gwiki init, `<files_home>/wiki/_gwiki/scope.json` is absent. test: `tests/wiki/test_wiki_files_home.py`.
- 4.1.13 - `resolve_project_from_root` maps a verified `<files_home>/_personal` personal root to `<files_home>/wiki/personal` and leaves ordinary project roots checkout-adjacent. test: `tests/wiki/test_wiki_files_home.py`.
- 4.1.14 - Registry overwrite fixtures persist wiki-home-relative direct-child paths and reject escapes. test: `crates/gwiki/src/registry.rs::register_overwrites_existing_entries`.
- 4.1.15 - Rust init/Obsidian writes refuse a missing, replaced, symlink, or non-directory files_home root and do not recreate it. test: `tests/wiki/test_wiki_files_home.py`.
- 4.1.16 - `gwiki init` and `register_scope` refuse while the singleton record role is `maintenance` and mutate no vault or registry bytes. test: `tests/wiki/test_wiki_files_home.py`.
- 4.1.17 - When the singleton is free, init and `register_scope` hold a claim through publication; a concurrent migrate, pack, unpack, snapshot, or restore cannot mutate during that window, and a maintenance winner leaves vault and registry bytes unchanged. test: `tests/wiki/test_wiki_files_home.py`.

## P5: Node access
`kind: framing`

**Goal:** A remote-mode daemon never opens a local files tree. Every wiki, attachment, and profile surface proxies to the hub owner through one request-aware boundary.

### 5.1 Proxy file HTTP to hub_daemon_url [category: code] (depends: P3, 4.1)
`kind: deliverable`

Targets:
- `src/gobby/hooks/event_handlers/_session_start/profile.py::*` — scope-reason: remote GET/PUT USER.md through DaemonClient; no node-local tree
- `src/gobby/wiki/owner_dispatch.py`
- `src/gobby/servers/routes/wiki.py::*` — scope-reason: every topic/personal handler including trust/refresh/export/sync-sessions/upkeep/librarian/recap uses owner_dispatch
- `src/gobby/mcp_proxy/tools/wiki.py::gateway`
- `src/gobby/wiki/scheduled_jobs.py::_gateway_for_resolved`
- `src/gobby/wiki/scheduled_jobs.py::create_wiki_sync_sessions_handler`
- `src/gobby/runner_lifecycle_periodic.py::*` — scope-reason: topic/personal watchers owner-dispatch; checkout-project watchers stay local
- `src/gobby/runner_lifecycle_subsystems.py::*` — scope-reason: prune registration does not construct a node-local topic/personal gateway
- `src/gobby/wiki/prune_job.py::*` — scope-reason: prune_all_scopes uses owner_dispatch for topic/personal
- `src/gobby/servers/routes/chat.py::*` — scope-reason: conversation message-delete proxies complete attachment cleanup on remote
- `src/gobby/servers/websocket/handlers/session_lifecycle.py::*` — scope-reason: clear-chat and conversation cleanup proxy the complete owner attachment delete
- `src/gobby/servers/websocket/chat_attachments.py::prepare_message_attachments`
- `src/gobby/servers/websocket/chat_attachments.py::_bind_attachments_sync`
- `src/gobby/servers/routes/chat_attachments.py::upload_attachment`
- `src/gobby/servers/routes/chat_attachments.py::get_attachment_content`
- `src/gobby/servers/routes/chat_attachments.py::delete_attachment`
- `src/gobby/storage/chat_attachments.py::*` — scope-reason: bind/fetch/delete hub attachments by id, not current-process machine_id
- `src/gobby/utils/daemon_client.py::*` — scope-reason: async raw request sharing URL/auth/timeout/error mapping with call_http_api
- `src/gobby/utils/durable_file.py::durable_replace`
- `src/gobby/servers/routes/hub_files_proxy.py`
- `src/gobby/servers/routes/__init__.py`
- `src/gobby/servers/_app_routes.py::register_routes`
- `tests/servers/routes/test_hub_files_proxy.py`
- `tests/hooks/test_session_user_profile.py::*` — scope-reason: retarget every profile read off daemon-home and add the hub-fetch and hub-write cases
- `tests/servers/routes/test_wiki_routes.py::*` — scope-reason: status/attach/ingest take the shared dispatch path
- `tests/storage/test_chat_attachments.py::*` — scope-reason: hub visibility and bind-versus-cleanup hold on the proxied identity model
- `tests/wiki/test_scheduled_jobs.py::*` — scope-reason: scheduled topic/personal jobs do not construct a node-local gateway
- `tests/wiki/test_watcher_lifecycle.py::*` — scope-reason: remote-mode watcher construction stays local only for checkout projects
- `tests/test_runner_lifecycle_subsystems.py::*` — scope-reason: cron and prune registration do not construct a node-local topic/personal gateway
- `tests/wiki/test_prune_job.py::*` — scope-reason: prune_all_scopes uses owner_dispatch remotely
- `tests/utils/test_utils_daemon_client.py::*` — scope-reason: raw and streaming DaemonClient contract covers URL, auth, timeouts, cancellation, and errors

wiki.py is 605 lines. Extract owner resolution and remote dispatch into `src/gobby/wiki/owner_dispatch.py` (new). HTTP handlers (including `status`, `attach`, `ingest`), MCP `gateway`, and scheduled `_gateway_for_resolved` call that module. Checkout project scopes stay local until #18779. Do not add dispatch branches that grow `scheduled_jobs.py` (947 lines). `runner_lifecycle_subsystems.py` is 857 lines; keep prune/cron wiring there and put new owner-dispatch branches in `owner_dispatch.py`. Projected: wiki.py < 700, scheduled_jobs.py ≤ 947, owner_dispatch.py < 400, `runner_lifecycle_subsystems.py` < 920, `runner_lifecycle_periodic.py` (469) < 550, `prune_job.py` (143) < 250. If any projection or actual edit would reach 1,000 lines, decompose inside the implementation task.

On `datastore_mode: remote`, profile, wiki, and attachment consumers share one hub request path: `DaemonClient.from_url(hub_daemon_url)` plus an **async** raw method on `DaemonClient` that uses the same URL join, auth headers, timeout, and error types as `call_http_api`. File upload/download uses a streaming request on that same client (not a fully-buffered body) and `StreamingResponse` backpressure on the way back. Streaming calls do not reuse the scalar five-second timeout as a stall detector. Specify a bounded connect/pool timeout, an operation-appropriate read/write inactivity limit, and one overall cancellable deadline on that same client. Async routes must await that method (or offload the entire sync lifecycle to a worker thread). Do not call sync `call_http_api` on the event loop. Do not add a second HTTP client stack. Join as `{origin}{path}` after the stored origin (no trailing slash). Forward method, path, query, body, and auth. Preserve FileResponse semantics already used by attachment download: status, raw bytes, `Content-Type`, `Content-Disposition`, `Content-Length`, `Content-Range`, `Accept-Ranges`, `ETag`, and inbound `Range` / `If-None-Match` / `If-Modified-Since` when FileResponse already honors them. Enforce existing attachment size limits while streaming. Cancellation closes the client. On the owner, `CancelledError` is handled: cancellation before row commit removes temp and unpublished target bytes; cancellation after row commit leaves a valid row/file pair that stale cleanup can finish. Shield the replace-to-row-commit boundary so a cancelled route cannot delete a published file that already has a row, and cannot leave a row whose file was removed. Network/auth/timeout failures are typed errors, never a local-file fallback.

Hop bound: send `X-Gobby-Files-Proxy-Hop: 1` on every proxied files/wiki/attachment request. If that header is already present, refuse (no second hop). Combined with § 1.1 self-origin refusal this covers same-origin, alias, remote-to-remote, and two-node cycles. The install and first-use owner probe is authenticated `GET /api/files/user-md` with that hop header; success is a 200 JSON body from a local owner. A remote target is a typed error.

- `read_user_profile_content` GET `/api/files/user-md`. Only a 200 JSON body with a string `content` field, including `""`, is success. 404, malformed JSON or schema, remote-target, and every other non-2xx status are typed failures with no local-file fallback.
- `write_user_profile_content`: hub-local mode uses a no-root-create `durable_replace` on `require_files_home() / USER.md`; remote mode PUT `/api/files/user-md` with `{"content": "..."}`. Never mkdir a node-local personal tree. Decoded `content` has a 1,048,576-byte UTF-8 ceiling. The JSON wire body has a separate maximum of 6,291,470 bytes (`{"content":}` envelope plus worst-case `\uXXXX` expansion of a maximum-length content string). Reject `Content-Length` above the wire maximum before reading. Independently count `Request.stream()` bytes and refuse the first byte above 6,291,470 regardless of missing, chunked, or under-reported `Content-Length`. Parse only the bounded body, then enforce the decoded ceiling. Exact 1,048,576-byte content and exact wire-maximum bodies succeed; each limit plus one byte refuses. Direct-owner and proxied over-limit requests leave the destination unchanged.
- Wiki topic/personal operations go through `owner_dispatch` and use `/api/wiki/...` HTTP routes. Inventory every MCP and scheduled topic/personal command and map each to an existing route or a sibling hub route. Commands without a route today: trust, refresh, export, sync-sessions, upkeep, librarian, recap. Do not add a second RPC protocol. HTTP, MCP, and scheduled callers share `owner_dispatch`. Do not construct a local `GwikiGateway` for topic or personal scopes on a node. File-bearing attach/ingest send streamed bytes plus filename/mime metadata; the hub never interprets a node filesystem path. Remote `sync-sessions` is file-bearing: `wiki_sync_sessions` and `create_wiki_sync_sessions_handler` stream the node's archive file bytes (explicit `archive_dir` and the scheduled default from the node's archive root). The hub ingest uses those transferred bytes and never interprets a node path as a local directory.
- Chat attachment upload/download/delete proxy to the matching hub routes, including multipart upload and binary download. Do not call `require_files_home` on the node. Remote file-bearing attachment and wiki branches forward `Request.stream` before multipart binding and keep the original multipart `Content-Type`. Only the owner parses `UploadFile` data.
- Topic/personal wiki watchers and `prune_all_scopes` go through `owner_dispatch`. Checkout-project watchers stay local. Remote mode creates no node-local topic or personal wiki state through those registrations.
- Conversation message-delete (`DELETE /{conversation_id}/messages`) and WebSocket `clear_chat` proxy the complete attachment cleanup plus message delete to the hub owner. Remote nodes do not bulk-delete shared attachment rows or skip hub bytes.

Attachment identity: hub-owned rows are created, bound, fetched, and deleted by `(project_id, attachment_id)`. HTTP and remote proxy URLs stay attachment_id-only; the owner loads `project_id` from the row. `machine_id` may be stored as the authenticated caller for audit. Bind/fetch/delete must not require `machine_id == require_machine_id()`. A remote upload then WebSocket bind on the node must find the hub row.

Hub-owner surfaces in `hub_files_proxy.py` (keep this module and `files.py` separate; `files.py` is checkout browsing):

- `GET /api/files/user-md` → `{"content": "<profile text or empty>"}` from `require_files_home()` plus the profile filename.
- `PUT /api/files/user-md` authenticated; body `{"content": "..."}`; atomic replace via no-root-create `durable_replace`. Empty content is a valid write (clears the working profile).

Export `create_hub_files_proxy_router` from `src/gobby/servers/routes/__init__.py` and include it in `register_routes`. Add an assembled FastAPI app test that both endpoints exist.

No caching of the working profile or vault bytes on the node disk.

**Acceptance:**

- 5.1.1 - Remote profile seed fetches hub USER.md and never reads `$GOBBY_HOME/personal/USER.md`. test: `tests/hooks/test_session_user_profile.py::test_seed_user_profile_content_merges_profile`.
- 5.1.2 - Remote wiki topic/personal requests, including status/attach/ingest, are proxied and do not create `~/wiki`. test: `tests/servers/routes/test_hub_files_proxy.py`.
- 5.1.3 - Remote attachment upload/download/delete proxy and do not write `$GOBBY_HOME/projects`. test: `tests/servers/routes/test_hub_files_proxy.py`.
- 5.1.4 - Hub `GET /api/files/user-md` returns the files_home profile. test: `tests/servers/routes/test_hub_files_proxy.py`.
- 5.1.5 - Hub PUT writes USER.md atomically; remote `write_user_profile_content` updates the hub copy and creates no node-local USER.md or personal directory. test: `tests/hooks/test_session_user_profile.py`.
- 5.1.6 - Origin join, timeout/auth errors, multipart upload, and binary download preserve status, bytes, Content-Type, and Content-Disposition. test: `tests/servers/routes/test_hub_files_proxy.py`.
- 5.1.7 - MCP and scheduled topic/personal jobs use `owner_dispatch` and do not construct a node-local gateway. test: `tests/wiki/test_scheduled_jobs.py`.
- 5.1.8 - Assembled FastAPI app exposes GET and PUT `/api/files/user-md`. test: `tests/servers/routes/test_hub_files_proxy.py`.
- 5.1.9 - Async hub requests yield the event loop; cancellation cleans up the client. test: `tests/servers/routes/test_hub_files_proxy.py`.
- 5.1.10 - Repeated hop, self-origin, remote-to-remote, and two-node cycles refuse with a typed error. test: `tests/servers/routes/test_hub_files_proxy.py`.
- 5.1.11 - Remote upload then WebSocket bind finds the hub attachment row. test: `tests/servers/routes/test_hub_files_proxy.py`.
- 5.1.12 - wiki.py and scheduled_jobs.py stay under 1,000 lines after dispatch extraction. behavior: "touched production sources stay under 1,000 lines" in `docs/architecture/hub-owned-files-home.md`.
- 5.1.13 - Every inventoried MCP and scheduled topic/personal command has a hub `/api/wiki/...` route and uses `owner_dispatch` remotely. test: `tests/servers/routes/test_wiki_routes.py`.
- 5.1.14 - Remote attach/ingest send streamed bytes plus metadata and do not forward a node filesystem path. test: `tests/servers/routes/test_hub_files_proxy.py`.
- 5.1.15 - Attachment proxy streams with backpressure; range, slow-consumer, cancellation, and maximum-size cases hold. test: `tests/servers/routes/test_hub_files_proxy.py`.
- 5.1.16 - Topic and personal watchers and prune use `owner_dispatch`; checkout-project watchers stay local; remote mode creates no node-local wiki state. test: `tests/wiki/test_watcher_lifecycle.py`.
- 5.1.17 - Streaming calls use a bounded connect timeout, read/write inactivity limit, and overall cancellable deadline; delayed first byte and stalled peer fail typed. test: `tests/servers/routes/test_hub_files_proxy.py`.
- 5.1.18 - Remote conversation message-delete and clear-chat send the complete attachment cleanup plus message delete to the hub owner; rows, bytes, and messages converge. test: `tests/servers/routes/test_hub_files_proxy.py`.
- 5.1.19 - Attachment and wiki proxies forward inbound `Range`, `If-None-Match`, and `If-Modified-Since` and return `Content-Length`, `Content-Range`, `Accept-Ranges`, and `ETag` with 206 and 304 when FileResponse already honors them. test: `tests/servers/routes/test_hub_files_proxy.py`.
- 5.1.20 - DaemonClient raw and streaming methods share URL join, auth headers, connect/inactivity/deadline timeouts, cancellation cleanup, and non-2xx error types. test: `tests/utils/test_utils_daemon_client.py`.
- 5.1.21 - Owner upload cancellation before row commit leaves no orphan bytes; cancellation after replace-and-row leaves a valid row/file pair. test: `tests/servers/routes/test_chat_attachments.py`.
- 5.1.22 - A successful `GET /api/files/user-md` probe with hop `1` from a local owner lets remote install continue; invalid-token or auth rejection, network or timeout failure, missing owner root, hop refusal, and a remote-target probe are typed install errors and start no managed services. test: `tests/cli/test_install_coverage.py`.
- 5.1.23 - Remote topic/personal `owner_dispatch` runs before any local path-existence or override resolution. test: `tests/wiki/test_scope_resolution.py`.
- 5.1.24 - Remote profile read treats only a 200 JSON body with string `content`, including `""`, as success; 404 and other non-2xx statuses are typed failures. test: `tests/hooks/test_session_user_profile.py`.
- 5.1.25 - Remote file-bearing attachment and wiki requests consume the inbound body via `Request.stream` before multipart binding; the node does not spool a complete UploadFile first. test: `tests/servers/routes/test_hub_files_proxy.py`.
- 5.1.26 - Remote `sync-sessions` with an explicit archive directory and with the scheduled default streams node archive bytes; the hub never treats a node path as a readable directory. test: `tests/servers/routes/test_hub_files_proxy.py`.
- 5.1.27 - Owner and proxied PUT `/api/files/user-md` accept exactly 1,048,576 decoded UTF-8 content bytes and refuse 1,048,577 before publication, leaving the destination unchanged. test: `tests/servers/routes/test_hub_files_proxy.py`.
- 5.1.28 - Owner and proxied PUT `/api/files/user-md` accept a wire body of exactly 6,291,470 bytes and refuse `Content-Length` 6,291,471 before parsing, leaving the destination unchanged. test: `tests/servers/routes/test_hub_files_proxy.py`.
- 5.1.29 - Owner and proxied PUT `/api/files/user-md` refuse the first streamed byte above 6,291,470 when `Content-Length` is absent, chunked, or under-reported, and leave the destination unchanged. test: `tests/servers/routes/test_hub_files_proxy.py`.

## P6: Migrate and inventory
`kind: framing`

**Goal:** One hub-local move of existing bytes with resume-on-rerun; pack/backup see the bind dir; operator docs match the new paths.

### 6.1 Add hub-local files migrate [category: code] (depends: 3.1, 4.1)
`kind: deliverable`

Targets:
- `src/gobby/cli/files.py`
- `src/gobby/files_migrate.py`
- `src/gobby/cli/__init__.py::cli`
- `src/gobby/runner_pid_file.py::claim_pid_file`
- `src/gobby/storage/chat_attachments.py::*` — scope-reason: after verified attachment publication, rewrite legacy local_path to files_home-relative locators
- `tests/cli/test_files_migrate.py`
- `tests/test_runner_pid_file.py::*` — scope-reason: migrate holds the daemon pid lock so start cannot enter

Hub-local only (`require_files_home`). Command: `gobby files migrate`, registered on the root Click command in `cli`. No dual-write. No start-time implicit migrate. Refuse unless `claim_pid_file` succeeds with role `maintenance` (daemon stopped). Hold that claim from discovery through publication. A concurrent `gobby start` cannot enter while the claim is held. File writers live in the daemon; the pid-file claim is the exclusive fence. Do not add a live-daemon files-home reader-writer lock.

After validating the pre-provisioned `files_home` root (do not create it), refuse a filesystem-root `files_home` and refuse equality, ancestor, or descendant overlap between `files_home` and every resolved legacy personal, project-attachment, and wiki-topic source. Those refusals mutate nothing. Then complete preflight of every present source/destination pair first. Publish each present source into an absent destination. Seed only still-missing baseline artifacts after those publications: empty profile file, personal marker and notes/reminders/attachments directories, wiki home directory, and a baseline wiki-home registry. Missing sources skip; empty-source hubs still get the baseline. Do not create a destination collision by seeding before a source that will occupy the same child.

Source classes (skip missing; each is independently resumable):

1. `$GOBBY_HOME/personal/USER.md` → `<files_home>/USER.md`
2. `$GOBBY_HOME/personal/.gobby` → `<files_home>/_personal/.gobby`
3. `$GOBBY_HOME/personal/wiki` → `<files_home>/wiki/personal`
4. leftover `$GOBBY_HOME/personal/{notes,reminders}` and any other leftover children (except already-handled USER.md / `.gobby` / `wiki`) → `<files_home>/_personal/<name>`
5. each `~/wiki/topics/<name>` → `<files_home>/wiki/<name>`
6. `$GOBBY_HOME/projects/<id>/attachments/**` → `<files_home>/_personal/attachments/<id>/...`
7. the existing hub wiki registry beside `~/wiki/topics/` → transform every registration to wiki-home-relative children; refuse malformed or unknown entries before any source mutation; publish the registry only after verified vault moves

There is no separate persisted notes/reminders subsystem in production code today. If those directories exist under the old personal tree, class 4 preserves their bytes. If they do not exist, skip. Checkout vaults and comms attachment storage stay put.

Preflight: discover every present source/destination pair first and build the complete normalized destination graph. Refuse with zero source mutations when destinations collide, when one planned destination is a prefix of another, or when a legacy topic name fails `validate_topic_name` (including `personal` as a topic source that is not the personal vault). Refuse when a destination exists and is not a recognized migrate destination (unexpected extra content). A recognized partial destination (only expected children, some sources still present) is resume, not refuse.

Both-present pairs after an interrupted EXDEV copy: if destination bytes and required metadata equal the remaining source, treat the pair as published-but-not-retired and unlink the source only after the full graph preflight succeeds. If they differ, refuse with zero mutations.

Apply the architecture Migration steps for each remaining source: copy or rename onto the same filesystem when possible; on EXDEV copy then verify then delete the source. Delete a source only after the destination bytes verify. After each published class, rewrite that class’s vault metadata / wiki registry to wiki-home-relative child paths (no absolute laptop paths). Refuse remote mode.

After verified publication of attachment bytes (class 6), rewrite every legacy `chat_attachments.local_path` to `_personal/attachments/<safe_project_id>/<attachment_id[:2]>/<attachment_id>/<filename>`. That rewrite is its own resumable step. An interruption between publication and rewrite leaves sources gone or present as already specified; the next run finishes the locator rewrite. After a successful full run, no migrated attachment row contains an absolute or canonical-source path.

Personal and topic scope-metadata rewrite is an independent post-preflight normalization whenever the destination exists, including source-absent and equal-both-present states. A crash after source retirement and before metadata rewrite does not skip that rewrite on rerun.

After a successful full run, personal and topic vault metadata `root` values point at the new wiki home children. The wiki registry contains only wiki-home-relative child paths. The wiki home itself is not a vault.

Idempotent: a second run is success when remaining sources are gone and the destination is the complete expected layout. A crash after the first class leaves a recognized partial; the next run resumes remaining classes. Partial failure must not invent a reader for the old locations.

**Acceptance:**

- 6.1.1 - First migrate moves every present source class, including leftover personal notes/reminders when they exist. test: `tests/cli/test_files_migrate.py`.
- 6.1.2 - Second migrate after a complete destination is a no-op success. test: `tests/cli/test_files_migrate.py`.
- 6.1.3 - Remote mode refuses. test: `tests/cli/test_files_migrate.py`.
- 6.1.4 - Checkout wiki and comms_attachments are untouched. test: `tests/cli/test_files_migrate.py`.
- 6.1.5 - Unrecognized destination content refuses before any source mutation. test: `tests/cli/test_files_migrate.py`.
- 6.1.6 - After a successful migrate, personal and topic scope metadata name the new wiki-home roots. test: `tests/cli/test_files_migrate.py`.
- 6.1.7 - After a successful migrate, the wiki registry holds only wiki-home-relative child paths. test: `tests/cli/test_files_migrate.py`.
- 6.1.8 - A recognized partial destination resumes remaining sources instead of refusing. test: `tests/cli/test_files_migrate.py`.
- 6.1.9 - Injected failure after the first published class leaves the remaining source intact; the next run finishes it. test: `tests/cli/test_files_migrate.py`.
- 6.1.10 - `gobby files migrate --help` is reachable from the root command. test: `tests/cli/test_files_migrate.py`.
- 6.1.11 - The existing hub wiki registry is an explicit source; malformed entries refuse before mutation; a crash before registry publication leaves remaining sources intact. test: `tests/cli/test_files_migrate.py`.
- 6.1.12 - Empty-source migrate still creates the baseline profile file, personal children, wiki directory, and registry without creating the files_home root. test: `tests/cli/test_files_migrate.py`.
- 6.1.13 - Migrate refuses while the hub daemon is running. test: `tests/cli/test_files_migrate.py`.
- 6.1.14 - Cross-device copy removes the source only after destination bytes verify; verification or truncation failure preserves the source and leaves no authoritative partial destination. test: `tests/cli/test_files_migrate.py`.
- 6.1.15 - Preflight finishes before any publication; a present legacy profile or populated personal directory publishes before baseline seeding and does not collide with a manufactured destination. test: `tests/cli/test_files_migrate.py`.
- 6.1.16 - After verified attachment publication, every migrated attachment locator is files_home-relative; interruption between publication and rewrite converges on rerun. test: `tests/cli/test_files_migrate.py`.
- 6.1.17 - Cross-class destination collisions, prefix overlaps, and reserved legacy topic names refuse before any source mutation. test: `tests/cli/test_files_migrate.py`.
- 6.1.18 - An equal both-present EXDEV pair unlinks the source after preflight; a divergent both-present pair refuses with zero mutations. test: `tests/cli/test_files_migrate.py`.
- 6.1.19 - Migrate holds `claim_pid_file` for the campaign; a concurrent daemon start cannot enter. test: `tests/cli/test_files_migrate.py`.
- 6.1.20 - After migrate and an idempotent rerun, `<files_home>/wiki/_gwiki/scope.json` is absent. test: `tests/cli/test_files_migrate.py`.
- 6.1.21 - Scope-metadata rewrite runs whenever the destination exists, including after source retirement; an injected crash before rewrite converges on rerun. test: `tests/cli/test_files_migrate.py`.
- 6.1.22 - Filesystem-root `files_home` and equality, ancestor, or descendant overlap with any resolved legacy personal, project-attachment, or wiki-topic source refuse with zero mutations. test: `tests/cli/test_files_migrate.py`.

### 6.2 Inventory bind dir in pack and hub-backup [category: code] (depends: 6.1)
`kind: deliverable`

Targets:
- `src/gobby/cli/pack.py::pack`
- `src/gobby/cli/pack.py::_do_pack`
- `src/gobby/cli/pack.py::unpack`
- `src/gobby/cli/pack.py::_archive_would_overwrite`
- `src/gobby/cli/pack.py::_safe_archive_target`
- `src/gobby/cli/pack.py::_write_restricted_archive`
- `src/gobby/runner_pid_file.py::claim_pid_file`
- `src/gobby/runner_pid_file.py::probe_daemon_lock`
- `src/gobby/cli/daemon.py::start`
- `src/gobby/cli/daemon.py::_do_stop`
- `src/gobby/cli/daemon.py::restart`
- `src/gobby/cli/utils_shutdown.py::stop_daemon`
- `src/gobby/runner.py::run_gobby`
- `src/gobby/runner.py::main`
- `src/gobby/cli/hub_backup/files_home.py`
- `tests/test_runner_pid_file.py::*` — scope-reason: claimant role, start-before-side-effects, and stop-during-maintenance barriers
- `src/gobby/cli/hub_backup/cli.py::_run_backup`
- `src/gobby/cli/hub_backup/cli.py::_archive_volumes`
- `src/gobby/cli/hub_backup/cli.py::restore_hub_backup`
- `src/gobby/cli/hub_backup/_stores.py::tar_volumes`
- `src/gobby/data/docker-compose.services.yml::*` — scope-reason: declare the files_home bind as a lifecycle entry
- `src/gobby/cli/installers/compose_env.py::resolve_compose_runtime`
- `tests/cli/test_hub_files_restore.py`
- `tests/cli/test_pack.py::*` — scope-reason: exact pack inventories, summaries, overwrite policy, and bootstrap fixtures
- `tests/cli/hub_backup/test_cli.py::*` — scope-reason: exact backup artifacts, restore order, summaries, and bootstrap fixtures
- `tests/cli/test_compose_bind_address.py::*` — scope-reason: compose environment projects GOBBY_FILES_HOME; drop deleted gcore asset comparison
- `tests/cli/installers/test_compose_env.py::*` — scope-reason: compose environment fixtures include files_home
- `tests/cli/installers/test_postgres_compose_template.py::*` — scope-reason: Python compose asset is the only template; drop deleted gcore copy
- `tests/install/test_daemon_runtime_pins.py::*` — scope-reason: runtime pins do not require the deleted gcore compose asset

`hub_backup/cli.py` is 930 lines. Do not add archive/restore bodies there. Put files-home archive, restore, collision, and confirm/`--force` in `src/gobby/cli/hub_backup/files_home.py`. `cli.py` only calls it. Projected: `cli.py` ≤ 950, `files_home.py` < 300, `pack.py` (714) < 850. `daemon.py` is 830 lines; moving the singleton probe and adding a claimant-role check must keep it under 900. If the projection or actual edit would reach 1,000 lines, decompose inside the implementation task.

Declare `gobby_files` as a **bind** of the configured `files_home` (not a named volume attached to postgres/qdrant/falkor). No service in compose consumes it except backup/pack helpers that archive the bind path. `resolve_compose_runtime` projects a validated `GOBBY_FILES_HOME` from the local bootstrap. Datastore volume enumeration stays unchanged. The Python compose asset is the only template. The gcore compose copy remains absent; compose-template and runtime-pin suites treat that absence as required.

`pack` includes the bind directory as `gobby/files/` in the tarball when `files_home` is set. Do not add `personal/` from `$GOBBY_HOME` to `PACK_FILES`. `unpack` extracts `gobby/files/` members into configured `files_home` (via `_safe_archive_target`), not under `$GOBBY_HOME/files`. Files-home members use fixed-size chunk copy into same-filesystem temporary files, then fsync and atomically replace; they must not `read()` a whole member into memory. Copied byte count must equal the member's declared `TarInfo.size`; a mismatch refuses before publication. Tests inject a declared-versus-actual mismatch without allocating a 100 GiB payload. Historical project-gobby, ordinary `$GOBBY_HOME`, nested Docker-volume, and PostgreSQL members stay on their current extract paths; this leaf does not rewrite those branches into 100 GiB streaming. Archive-wide preflight, completed before any files_home or datastore mutation: at most 100,000 members, at most 100 GiB total declared uncompressed bytes, checked summation, reject duplicate normalized names and file/directory prefix conflicts, and check free space. Insufficient destination space refuses before either files_home or datastore mutation, including confirmation and `--force` paths. Count every archive member. `_write_restricted_archive` and the files-home backup helper prewalk the source tree into the same normalized member graph, checked-sum regular-file sizes, and refuse more than 100,000 members or more than 100 GiB before creating any output. Exactly 100,000 members and exactly 100 GiB are accepted and round-trip. 100,001 members or 100 GiB plus one byte refuse before any output exists and leave no archive. They also `lstat` and refuse symlinks, FIFOs, sockets, device nodes, and regular files with `st_nlink > 1` before emitting an archive. Regular singly-linked files and directories round-trip. After prewalk, both producers emit that graph relative to the held `files_home` directory fd: open each descendant no-follow, `fstat` and compare identity, type, link count, and size at emission, feed `addfile` from the already-open stream, and remove unpublished temp output on mismatch. Leaf- and ancestor-swap after prewalk must not enter the archive. Before stopping services or creating output, pack and files-home hub-backup refuse when the resolved output path or its temporary publication path equals or is contained in `files_home` or any other archived source root. Temporary archives are created outside every source root, fsynced, and atomically published to the validated destination. Injected failure during temp write, file or directory fsync, atomic replace, or mid-member publication leaves no partial authoritative archive, preserves any prior destination, defines temp cleanup, and converges on rerun. `_archive_would_overwrite` treats those members as files_home collisions and keeps the existing confirm/`--force` policy: a valid collision aborts when confirmation is declined and proceeds only after complete preflight when confirmed or `--force` is supplied. A late invalid member always refuses before mutation in every mode. An interrupted member is not authoritative; operator rerun uses that collision policy. No cross-store journal.

`claim_pid_file` records a claimant role beside the PID: `daemon` for the live runner (default, so existing runner callers stay valid) and `maintenance` for migrate, pack, unpack, backup snapshot, and restore. `probe_daemon_lock` returns that role. `gobby start` acquires `claim_pid_file` with role `daemon` before `_services_start`, `get_cli_runtime` / DB-backed config, and `service_start`. It retains that claim or atomically hands it to the direct or service-managed runner with no unlocked interval. A held `maintenance` claim performs none of those operations. Reciprocal barriers against migrate, pack, unpack, snapshot, and restore prove exactly one exclusive entrant. `_do_stop` inspects the role-bearing singleton as its first operation and refuses `maintenance` before `get_cli_runtime`, service status/stop, Docker handling, or `stop_daemon`. `stop_daemon` and `restart` detect role `maintenance` and refuse with a typed error; they do not SIGTERM/SIGKILL the campaign PID. `run_gobby` and `main` destructure that role the same way: either held role exits before initialization and is not described as a live daemon.

`pack` acquires `claim_pid_file(..., role="maintenance")` after any stop, holds it through files_home discovery and final archive publication, and releases it before an intentional restart. Unpack, backup snapshot, and restore each hold that same `maintenance` claim from preflight through final publication so `gobby start` cannot enter and `gobby stop` cannot kill the campaign.

Files-member unpack and hub-backup restore begin with a present destination-local bootstrap that already names an existing absolute `files_home`. Capture that destination `files_home` before mutation, restore files there, and merge archived bootstrap data while preserving the destination `files_home`. A missing destination bootstrap is a typed refusal before mutation.

`HUB_VOLUMES` stays datastore named volumes. `_run_backup` / `_archive_volumes` take a separate bind-archive step for `files_home`. `restore_hub_backup` recognizes that archive member and restores it into configured `files_home` with the same confirm/`--force` collision policy unpack uses. Do not append `gobby_files` to `HUB_VOLUMES`. Restore stays sequential with the existing datastore restores (no cross-store journal). Preflight all artifacts and collisions, including a late invalid files member, before mutating either domain; operator rerun is the recovery path.

Update existing pack and hub-backup suites so exact inventories, JSON summaries, restore order, overwrite behavior, and bootstrap fixtures include the files bind. Keep the new end-to-end round trip.

**Acceptance:**

- 6.2.1 - Pack archives `files_home` and not `$GOBBY_HOME/personal`. file: `src/gobby/cli/pack.py`.
- 6.2.2 - Compose/hub-backup treat `gobby_files` as a bind-dir lifecycle entry, not a datastore named volume. file: `src/gobby/data/docker-compose.services.yml`.
- 6.2.3 - Pack unpack and hub-backup restore put USER.md, a `_personal` attachment, and a wiki file back under configured `files_home` and apply the existing overwrite policy. test: `tests/cli/test_hub_files_restore.py`.
- 6.2.4 - Existing pack inventory/summary/overwrite contracts include the files bind. test: `tests/cli/test_pack.py`.
- 6.2.5 - Existing hub-backup artifact/order/summary contracts include the files bind archive. test: `tests/cli/hub_backup/test_cli.py`.
- 6.2.6 - `hub_backup/cli.py` stays under 1,000 lines; files-home archive/restore lives in the extracted module. behavior: "touched production sources stay under 1,000 lines" in `docs/architecture/hub-owned-files-home.md`.
- 6.2.7 - `resolve_compose_runtime` exports validated `GOBBY_FILES_HOME`; datastore volume enumeration is unchanged. test: `tests/cli/test_compose_bind_address.py`.
- 6.2.8 - files_home unpack and restore use chunked temp-file publication and preflight member-count plus aggregate size limits. test: `tests/cli/test_hub_files_restore.py`.
- 6.2.9 - Archive preflight refuses more than 100,000 members, more than 100 GiB declared uncompressed bytes, duplicate normalized names, and file/directory prefix conflicts before mutation. test: `tests/cli/test_hub_files_restore.py`.
- 6.2.10 - A late invalid files archive member refuses before any datastore row or files_home path changes, in both confirm and `--force` modes. test: `tests/cli/test_hub_files_restore.py`.
- 6.2.11 - A valid destination collision aborts when confirmation is declined and proceeds only after complete preflight when confirmed or `--force` is supplied. test: `tests/cli/test_hub_files_restore.py`.
- 6.2.12 - Pack and hub-backup refuse to emit an archive when `files_home` contains a symlink or other special file; regular files and directories round-trip. test: `tests/cli/test_pack.py`.
- 6.2.13 - Pack holds `claim_pid_file` from files_home discovery through archive publication; a concurrent daemon start cannot enter. test: `tests/cli/test_pack.py`.
- 6.2.14 - Pack and hub-backup refuse to emit an archive when `files_home` contains a regular file with `st_nlink > 1`. test: `tests/cli/test_pack.py`.
- 6.2.15 - Unpack and hub-backup restore require a present destination-local bootstrap, publish files under that `files_home`, and keep the destination `files_home` after merging archived bootstrap data. test: `tests/cli/test_hub_files_restore.py`.
- 6.2.16 - Unpack holds `claim_pid_file` with role `maintenance` from preflight through publication; a concurrent daemon start cannot enter. test: `tests/cli/test_pack.py`.
- 6.2.17 - Hub-backup snapshot holds `claim_pid_file` with role `maintenance` from preflight through publication; a concurrent daemon start cannot enter. test: `tests/cli/hub_backup/test_cli.py`.
- 6.2.18 - Hub-backup restore holds `claim_pid_file` with role `maintenance` from preflight through publication; a concurrent daemon start cannot enter. test: `tests/cli/test_hub_files_restore.py`.
- 6.2.19 - `gobby start` acquires a `daemon` claim before managed-service startup, DB-backed runtime load, and `service_start`, hands it to the runner with no unlocked interval, and performs none of those operations while `maintenance` is held. test: `tests/test_runner_pid_file.py`.
- 6.2.20 - A `maintenance` claimant is not treated as the daemon; `_do_stop`, `gobby stop`, and restart refuse before runtime, service-manager, Docker, or signaling effects. test: `tests/test_runner_pid_file.py`.
- 6.2.21 - Pack and files-home hub-backup accept exactly 100,000 members and exactly 100 GiB declared regular-file bytes and round-trip those archives. test: `tests/cli/test_pack.py`.
- 6.2.22 - Files-home unpack and restore refuse when copied bytes disagree with the declared member size, before publication, without allocating a full oversized payload. test: `tests/cli/test_hub_files_restore.py`.
- 6.2.23 - Pack and files-home hub-backup refuse 100,001 members or 100 GiB plus one byte before creating output and leave no archive. test: `tests/cli/test_pack.py`.
- 6.2.24 - `run_gobby` and `main` treat a held `daemon` or `maintenance` claim as contention and exit before initialization. test: `tests/test_runner_pid_file.py`.
- 6.2.25 - Pack and files-home hub-backup refuse when the output or temporary publication path equals or is contained in `files_home` or another archived source root, before stopping services or creating output. test: `tests/cli/test_pack.py`.
- 6.2.26 - Files-home unpack and hub-backup restore refuse insufficient destination space before either domain mutates, including confirmation and `--force` paths. test: `tests/cli/test_hub_files_restore.py`.
- 6.2.27 - Injected failure during temporary archive write, fsync, atomic replace, or files-member publication leaves no partial authoritative output, preserves any prior destination, and converges on rerun. test: `tests/cli/test_pack.py`.
- 6.2.28 - Direct `gobby stop` and restart refuse a `maintenance` claim inside `_do_stop` with zero `get_cli_runtime`, service-manager, Docker, or `stop_daemon` effects. test: `tests/test_runner_pid_file.py`.
- 6.2.29 - Pack and files-home hub-backup emit the prewalk graph through the held `files_home` directory fd; a leaf or ancestor swap after prewalk refuses and leaves no unpublished archive. test: `tests/cli/test_pack.py`.
- 6.2.30 - Start versus migrate, pack, unpack, snapshot, and restore barriers admit exactly one exclusive claimant; a maintenance winner leaves start with zero service, runtime, or runner effects. test: `tests/test_runner_pid_file.py`.

### 6.3 Update operator docs for files_home [category: docs] (depends: 6.2)
`kind: deliverable`

Targets:
- `src/gobby/install/shared/skills/intro/SKILL.md`
- `docs/architecture/hub-owned-files-home.md`
- `docs/guides/system-requirements.md`
- `src/gobby/install/bundled_content_manifest.json::*` — scope-reason: regenerate after the intro skill is the last bundled-content edit
- `tests/install/test_bundled_content_manifest.py::*` — scope-reason: integrity digest matches the intro edit

Intro skill and system-requirements: hub-local profile path is `<files_home>/USER.md`. Operator docs provision that hub bind directory first, then run local `install --files-home <existing-abs-dir>`, then `gobby files migrate` before `gobby start`. Remote intro uses the § 5.1 PUT path, not `~/.gobby/personal/USER.md`. Remote install requires `hub_daemon_url` and the existing hub `local_cli_token`; it does not generate one.

Architecture doc is the governing contract and must match this plan: remote owner URL is `hub_daemon_url` (not `daemon_url`); local install and migrate never create the `files_home` root; migrate, pack, unpack, backup, and restore hold `claim_pid_file` with role `maintenance` while the daemon is stopped; `gobby start` probes that claim before managed services; `gobby stop` refuses a maintenance claimant; direct gwiki init/register_scope refuse while role is `maintenance` and still do not have to route through the daemon. Path examples that still show `$GOBBY_HOME/personal` as live must match this plan. § 1.1 may regenerate the bundled-content digest for the installed bootstrap template; this leaf is the last bundled-content edit, so it owns the final digest after the intro change.

**Acceptance:**

- 6.3.1 - Intro skill writes `<files_home>/USER.md` on the hub owner and documents the remote PUT path. file: `src/gobby/install/shared/skills/intro/SKILL.md`.
- 6.3.2 - System-requirements documents the hub-local files_home profile path. file: `docs/guides/system-requirements.md`.
- 6.3.3 - Bundled-content digest matches the intro skill after this leaf. test: `tests/install/test_bundled_content_manifest.py`.
- 6.3.4 - Architecture doc names `hub_daemon_url` as the remote owner URL, states that writers never create the `files_home` root, and matches the stopped-daemon `claim_pid_file` fence including start-before-side-effects, maintenance role, stop refusal, and gwiki maintenance refusal. file: `docs/architecture/hub-owned-files-home.md`.
- 6.3.5 - Intro skill and system-requirements document provisioning the existing hub bind directory, local `install --files-home`, remote `hub_daemon_url` plus existing-token prerequisite, and migrate-before-start. file: `docs/guides/system-requirements.md`.

## V2 End-to-end verification
`kind: verification`

Focused proof, hub-owner plus remote-mode:

- Local bootstrap + `require_files_home`; present-file fixtures carry the required field, including daemon-url, postgres/falkordb, remote-daemon, UI-exposure, CLI install, runtime, daemon-coverage, PostgreSQL installer, pool-atexit, and runtime-boundary suites. `to_config_dict` and `DaemonConfig` carry the mode-specific fields. Concurrent bootstrap writers, including direct create and exposure rollback, serialize through `exclusive_file_lock`. The held directory fd refuses root and ancestor swaps. Filesystem-root `files_home` refuses. The § 1.1 helper upgrades a legacy present mapping without `load_bootstrap`; the § 2.1 front door then runs identity and refuses overlap with legacy sources. Standalone PostgreSQL Docker install does not start compose work from a missing or legacy-invalid local mapping.
- Personal marker and USER.md under files_home; remote startup keeps the sentinel and skips filesystem identity. Config-open coverage is explicit-local plus a remote no-identity case.
- Attachments persist and reconstruct the files_home-relative locator from the loaded row's project_id; HTTP stays attachment_id-only. Cleanup uses a 60-second tokenized lease, unlinks on the owner before row delete, and is a no-op remotely. Bind accepts only an unclaimed row. Cancellation awaits the unlink worker; expiry after unlink does not bind missing bytes. Storage-layer visibility is by project and attachment id.
- Topic/personal wiki paths under `files_home/wiki`; registry writes stay relative; personal sentinel and `resolve_project_from_root` map `_personal` to `wiki/personal`. Init, register_scope, and Obsidian helpers refuse a missing or replaced owner root. Direct gwiki mutators refuse a `maintenance` claim, proceed under a live daemon, and claim the singleton when free. Rust integration fixtures use wiki-home children.
- Remote daemon proxies HTTP, MCP, jobs, topic/personal watchers, prune, conversation clear/delete, and sync-sessions archive bytes, and writes no daemon-home copies. Streaming uses connect, inactivity, and overall deadlines. Range and conditional headers keep FileResponse 206/304 behavior. Remote install authenticates the existing hub token against `hub_daemon_url` before services start; auth, network, timeout, missing-root, hop, and remote-target probe failures start no services. USER.md PUT enforces the 1,048,576-byte decoded ceiling and the 6,291,470-byte raw-wire ceiling, including missing, chunked, and under-reported `Content-Length`.
- Reserved topic names refuse at Rust and Python; explicit personal scope still resolves.
- Remote USER.md write updates the hub copy only.
- Remote upload then WebSocket bind finds the hub row.
- Hop/self-origin/cycles refuse.
- Pack/hub-backup restore round-trips files_home bytes with chunked publication and declared-versus-actual size checks; pack, unpack, snapshot, and restore hold `claim_pid_file` with role `maintenance`; start acquires a daemon claim before side effects and hands it to the runner; `_do_stop` and runner entry paths refuse a maintenance claimant before runtime or service effects; exact 100,000-member and 100 GiB archives round-trip and over-limit cases leave no output; output paths stay outside archived roots; producer and restore preflight refuse oversized, duplicate, prefix-conflict, hardlink, special-file, overlap, insufficient-space, and late-invalid members; producers emit through the held directory fd; injected publication failures leave no partial archive; destination `files_home` survives bootstrap merge; existing inventory contracts stay green. Operator docs cover bind-directory provisioning, `install --files-home`, remote token prerequisite, and migrate-before-start.
- Compose runtime exports `GOBBY_FILES_HOME`; the gcore compose copy stays absent.
- Migrate runs after attachment and wiki cutover. It inventories leftover personal notes/reminders and the hub wiki registry, publishes present sources before baseline seeding, rewrites attachment locators after verified publication, verifies cross-device copies before source delete, resumes a recognized partial, refuses while the daemon is running, and leaves checkout wiki / comms attachments alone. Wiki home is not a vault.

```bash
GOBBY_TEST_PROTECT=1 uv run pytest tests/config/test_files_home.py tests/config/test_bootstrap.py tests/config/test_app_config.py tests/utils/test_daemon_url.py tests/config/test_bootstrap_postgres.py tests/config/test_bootstrap_falkordb.py tests/cli/test_daemon_remote_mode.py tests/test_ui_exposure.py tests/cli/test_datastores_expose.py tests/cli/test_cli_install.py tests/cli/test_runtime.py tests/cli/test_daemon_coverage.py tests/cli/installers/test_postgres_installer.py tests/storage/hub/test_postgres_pool_atexit.py tests/storage/test_project_manager.py tests/storage/test_personal_remote_init.py tests/storage/test_chat_attachments.py tests/hooks/test_session_user_profile.py tests/servers/websocket/test_chat_attachments.py tests/servers/routes/test_chat_attachments.py tests/servers/routes/test_chat_routes.py tests/test_runner_chat_attachments_cleanup.py tests/wiki/test_wiki_files_home.py tests/wiki/test_scope_resolution.py tests/wiki/test_scheduled_jobs.py tests/wiki/test_watcher_lifecycle.py tests/test_runner_lifecycle_subsystems.py tests/wiki/test_prune_job.py tests/utils/test_utils_daemon_client.py tests/servers/routes/test_hub_files_proxy.py tests/servers/routes/test_wiki_routes.py tests/cli/test_files_migrate.py tests/cli/test_hub_files_restore.py tests/cli/test_pack.py tests/cli/hub_backup/test_cli.py tests/cli/test_install_coverage.py tests/cli/test_install_front_door.py tests/cli/test_utils_coverage.py tests/cli/test_compose_bind_address.py tests/cli/installers/test_compose_env.py tests/cli/installers/test_postgres_compose_template.py tests/install/test_bundled_content_manifest.py tests/install/test_daemon_runtime_pins.py tests/test_runner_pid_file.py -v
cargo test -p gobby-wiki --lib scope -- --nocapture
cargo test -p gobby-wiki --lib registry -- --nocapture
cargo test -p gobby-wiki --test cli_init -- --nocapture
cargo test -p gobby-wiki --test cli_export -- --nocapture
cargo test -p gobby-core reads_bootstrap_with_files_home -- --nocapture
uv run ruff check src/gobby/
uv run mypy src/gobby/
```

## V1 Plan Changelog
`kind: verification`

**Round 1** `kind: enhancement`

- enhancer_run: 53cbc46d-f860-4ddc-ab47-b25947518fa2
- enhancer_session: 73f8b6c0-cca8-402c-b959-2e33a02fbf66
- converged: false
- suggestions_presented: 5
- accepted:
  - E1 / better / testability — migrate preflight, zero-mutation collision, metadata rewrite, relative wikis.json, complete-layout idempotency
  - E2 / better / clarity — one DaemonClient hub-request path with origin, timeout, and header/byte fidelity
  - E3 / better / clarity — pack unpack and hub-backup restore of the gobby_files bind
  - E4 / better / clarity — authenticated USER.md write plus remote intro path
  - E5 / better / testability — reserved topic-name refusal plus personal-scope positive control
- declined: none
- resolution_notes: Folded all five into § 1.1, § 4.1, § 5.1, § 6.1, and § 6.2. Hub HTTP reuses DaemonClient; atomic USER.md write reuses durable_replace; restore uses unpack and restore_hub_backup rather than a new archive format.

**Round 1** `kind: verification`

- reviewer_run: 819d849e-f595-49ad-8578-c5b14b13be72
- reviewer_session: 355cda97-8d5f-4edd-bf6e-802bed06ae0b
- verdict: needs_review
- findings:
- F1-coverage-ledger / blocking / companion coverage-ledger YAML absent
- F2-section-6-2-routing / blocking / 6.2 docs category routes executable pack/backup work to tech-writer
- F3-rust-files-home-data-flow / blocking / gwiki has no typed Rust files-home view
- F4-reserved-vault-names / blocking / project-vault refusal omitted; Python reserved-name test missing
- F5-notes-reminders-migration / blocking / notes/reminders source inventory undefined
- F6-bootstrap-consumers / blocking / writers and existing fixtures omit the new required fields
- F7-remote-personal-initializers / blocking / remote startup calls owner-only filesystem identity
- F8-attachment-path-consumers / blocking / cleanup still unlinks record.local_path
- F9-attachment-machine-identity / blocking / remote bind keys on node machine_id
- F10-wiki-remote-dispatch / blocking / status/attach/ingest, MCP, and jobs bypass proxy
- F11-wiki-registry-writer / blocking / register_scope reintroduces absolute topics/ paths
- F12-files-router-registration / blocking / hub_files_proxy not exported or included
- F13-files-cli-registration / blocking / files group not on root CLI
- F14-hub-backup-decomposition / blocking / cli.py 930 lines would exceed 1,000
- F15-backup-test-inventory / blocking / existing pack/backup contract suites omitted
- F16-async-proxy-boundary / blocking / sync DaemonClient on async routes
- F17-proxy-loop / blocking / no hop/self-origin/cycle bound
- F18-files-root-recreation / blocking / mkdir can recreate a lost bind
- F19-migration-recovery / blocking / partial dest refused with no resume path
- F20-restore-recovery / blocking / mid-restore can split DB and files generations
- resolution_notes: Accepted F1–F19; declined F20. F19 repaired as exclusive lock plus resume-on-rerun (no journal). F20 remains sequential restore with preflight/collision and operator rerun. 6.2 is now backend code; 6.3 is docs. 1.1 gained a typed Rust FilesHomeView, writer/fixture inventory, and self-origin refuse. 2.1 gates filesystem identity to local mode. 3.1 adds one owner resolver and no-root-create writes. 4.1 adds Python reserved-name tests and relative register_scope. 5.1 extracts owner_dispatch, async DaemonClient, hop bound, router registration, and id-based attachment bind. 6.1 inventories leftover personal notes/reminders, registers the CLI, and resumes recognized partials. Companion ledger path is named in Constraints; YAML write is still required before expansion.

```json plan-review-round
{"evidence_id":"de7c9274-c7c0-423b-aa71-5e89f39ead13","plan_hash":"98627d1fc1843f30bf6c7645b4392fa60009b7c8f74e0adb37db2567ba2d73e7","round_number":1,"round_result":{"coverage_attestation":{"adjacent_variant_complete":true,"attestation_digest":"01a62860d6f3ec60726d6c4be8f75bc74a337b7be8c651ae7dc8a0a13894bcb2","cross_lane_interaction_complete":true,"disposition_counts":{"dismissed":5,"emitted_findings":20,"total":25},"evidence_id":"de7c9274-c7c0-423b-aa71-5e89f39ead13","lanes":[{"candidate_count":7,"lane_id":"requirements_traceability","status":"completed"},{"candidate_count":11,"lane_id":"repository_blast_radius","status":"completed"},{"candidate_count":7,"lane_id":"runtime_invariants","status":"completed"}],"shadow_manifest_status":{"entry_count":7,"manifest_digest":"8f22f5df455dea4d90e4f35ecb2a9f67bf4904453eeca07220bb0ce28f7f25fa","status":"valid"},"source_digest":"5b654cb485191784e5fc4531b69276e125b64fede53bb8e5892e4862f4f28e6b","version":1},"findings":[{"category":"gobby-format","check_key":"bootstrap-coverage-ledger","description":"The required `.coverage-ledger.yaml` companion is missing, so close-time coverage cannot compare the eventual leaves with this reviewed plan.","finding_id":"F1-coverage-ledger","fix":"Add `.gobby/plans/hub-owned-files-home.coverage-ledger.yaml` with all 36 acceptance items mapped to the seven expected leaves and pin it to the revised plan hash.","location":"Plan preamble / whole-plan coverage","prevention":"Before resubmission, compare every deliverable and acceptance item against the required companion-ledger artifact.","principle":"Every new epic plan must carry the adversary-reviewed companion coverage ledger required before expansion.","root_cause":"The plan defines seven leaves and 36 acceptance items, while `.gobby/plans/hub-owned-files-home.coverage-ledger.yaml` is absent.","section_id":"__preamble__","severity":"blocking"},{"category":"gobby-format","check_key":"implementation-category-alignment","description":"The current category sends substantial backend implementation to a documentation route and suppresses the code leaf's TDD contract.","finding_id":"F2-section-6-2-routing","fix":"Change 6.2 to `[category: code]` with `implementation_domain: backend` and TDD, or split documentation-only edits into a docs leaf while retaining all executable lifecycle work in a backend code leaf.","location":"P6 / § 6.2","prevention":"For each deliverable, compare its production targets and behavioral acceptance with its category, implementation domain, and TDD route.","principle":"Deliverable category must match the executable work and deterministic agent route it creates.","root_cause":"Section 6.2 is marked `docs` although it changes pack, unpack, backup, restore, compose behavior, and executable tests; shadow derivation routes it to `tech-writer` with `tdd: false`.","section_id":"6.2","severity":"blocking"},{"category":"missing-requirement","check_key":"rust-files-home-data-flow","description":"No Rust API carries `datastore_mode`, `files_home`, and `hub_daemon_url` into gwiki, so the owner default and remote refusal cannot be implemented from the planned tolerance-only change.","finding_id":"F3-rust-files-home-data-flow","fix":"Specify a validated typed Rust files-home bootstrap view, make gwiki consume it, and change 1.1.5 to `reads_bootstrap_with_files_home` covering owner, remote, missing, relative, tilde, and endpoint-default cases.","location":"P1 / § 1.1 and P4 / § 4.1","prevention":"Trace every new configuration field from parser through each language projection to its consuming symbol and acceptance test.","principle":"A consumer that derives behavior from configuration needs a typed data path to the required fields; ignoring unknown keys is insufficient.","root_cause":"P1 asks gcore only to tolerate `files_home`, while P4 requires gwiki to distinguish local versus remote mode and derive `<files_home>/wiki`; acceptance 1.1.5 also cites the old datastore-mode test instead of the new regression named by prose and V2.","section_id":"1.1","severity":"blocking"},{"category":"missing-requirement","check_key":"reserved-vault-name-coverage","description":"A deferred project vault may collide with the personal or reserved home entries, and the Python daemon refusal has no named acceptance test.","finding_id":"F4-reserved-vault-names","fix":"Carry project-name refusal into D1's inherited obligation for #18779, and add a concrete Python test in 4.1 for all three reserved names plus explicit personal-scope success.","location":"D1 and P4 / § 4.1","prevention":"For namespace reservations, enumerate topic, project, personal, Rust, and Python entry points and require a negative test plus the explicit-personal positive control.","principle":"Every reserved-name rule must be assigned to each producer that can create the colliding namespace and to a concrete test at each entry point.","root_cause":"The architecture reserves `personal`, `_personal`, and `wiki` for topic and project vault names; 4.1 covers topics, while D1/#18779 omits project-vault refusal, and 4.1.5 cites only the Rust test for a requirement that also names the Python daemon.","section_id":"4.1","severity":"blocking"},{"category":"missing-requirement","check_key":"legacy-content-source-inventory","description":"Existing note or reminder bytes can be omitted silently, and acceptance 6.1.1 has no deterministic source inventory.","finding_id":"F5-notes-reminders-migration","fix":"Discover and enumerate their current persisted sources, add them to preflight/migration and byte-preservation tests, or state that no legacy files exist and correct the source-class count.","location":"P2 / § 2.1 and P6 / § 6.1","prevention":"Reconcile every destination class with a named legacy source, absence proof, and byte-preservation acceptance before designing moves.","principle":"A migration must enumerate every legacy content source or explicitly prove that no persisted legacy source exists.","root_cause":"Notes and reminders are named as existing hub-semantic content, but neither architecture nor plan identifies source paths or preservation behavior; 6.1 then claims an undefined six source classes.","section_id":"6.1","severity":"blocking"},{"category":"traceability","check_key":"mandatory-bootstrap-consumer-inventory","description":"Existing configuration generation and tests will fail the new hard-error contract outside the planned target and validation inventory.","finding_id":"F6-bootstrap-consumers","fix":"Add every bootstrap writer and current local/remote fixture to 1.1, including install fallback generation and existing bootstrap/app-config tests, then replace stale missing/default expectations explicitly.","location":"P1 / § 1.1","prevention":"Run constructor/writer/fixture blast radius for every newly mandatory field and add each changed file to Targets and focused validation.","principle":"Making a configuration field mandatory requires updating every writer, constructor, fallback, fixture, and existing expectation in the same leaf.","root_cause":"The target list covers the parser, a new test, and installed template, while current install fallbacks and existing bootstrap suites still create local or remote bootstrap data without the newly required mode-specific field.","section_id":"1.1","severity":"blocking"},{"category":"unhandled-edge","check_key":"remote-personal-initializer-gating","description":"Remote daemon startup has no specified branch that preserves the sentinel row while skipping `_personal` filesystem identity.","finding_id":"F7-remote-personal-initializers","fix":"Target `install.py`, `storage/hub/runtime.py`, `cli/utils_config.py`, and their tests; gate filesystem marker creation to local mode while preserving the shared checkout-free sentinel database row.","location":"P2 / § 2.1 and P5 / § 5.1","prevention":"For every owner-only path helper, inspect all startup and initialization callers and define local-owner and remote branches.","principle":"Remote mode must establish shared database identity without invoking an owner-only filesystem path.","root_cause":"Install, hub-database opening, and local-storage initialization call personal identity/project setup unconditionally; changing `personal_project_path` to raise remotely makes these paths fail or recreate node-local state.","section_id":"2.1","severity":"blocking"},{"category":"traceability","check_key":"attachment-path-consumer-inventory","description":"Non-route cleanup paths can unlink the wrong location or fail after absolute `local_path` stops being canonical.","finding_id":"F8-attachment-path-consumers","fix":"Introduce one owner-only resolver from project ID, attachment ID, and filename; retarget chat deletion, session lifecycle, storage hygiene, storage helpers, and existing HTTP route tests, with remote cleanup prohibited from local unlink.","location":"P3 / § 3.1 and P5 / § 5.1","prevention":"For path-shape changes, enumerate create/read/delete/bulk-delete/maintenance consumers and both HTTP and WebSocket test seams.","principle":"Changing a persisted path from canonical absolute storage to reconstruction requires every reader, deleter, cleanup job, and test to use the new resolver.","root_cause":"Conversation deletion, WebSocket lifecycle cleanup, and stale-upload maintenance still pass `record.local_path` to unlink helpers, and the existing HTTP route tests are absent from Targets.","section_id":"3.1","severity":"blocking"},{"category":"unhandled-edge","check_key":"proxied-attachment-machine-ownership","description":"A remote upload can succeed and then become invisible to message binding because its row is keyed to a different machine.","finding_id":"F9-attachment-machine-identity","fix":"Define one authoritative attachment ownership model: propagate authenticated caller machine identity to all hub operations or proxy the complete bind/fetch/delete lifecycle to the hub; add a remote upload-then-WebSocket-bind integration test.","location":"P3 / § 3.1 and P5 / § 5.1","prevention":"Trace identity keys across upload, persistence, bind, fetch, delete, and proxy authentication, then test the complete remote lifecycle.","principle":"A proxied lifecycle must preserve the ownership identity used by every later fetch, bind, and delete operation.","root_cause":"Hub upload storage uses the hub process machine ID, while remote WebSocket binding queries with the node machine ID; current daemon auth headers do not transmit an authoritative caller machine identity.","section_id":"5.1","severity":"blocking"},{"category":"traceability","check_key":"wiki-remote-entrypoint-inventory","description":"Implementing only the named route helpers leaves reachable remote operations that touch a node-local vault or fail outside the intended proxy path.","finding_id":"F10-wiki-remote-dispatch","fix":"Move owner resolution and remote dispatch into a request-aware shared scope/gateway boundary used by all HTTP handlers, MCP tools, and scheduled jobs; keep checkout project scopes local until #18779 and add refusal/proxy tests for every adjacent path.","location":"P4 / § 4.1 and P5 / § 5.1","prevention":"Inventory all gateway constructors and callers across HTTP, MCP, jobs, and reconciliation, then prove remote topic/personal calls cannot reach local gateway creation.","principle":"Remote topic and personal wiki access must cross one dispatch boundary before any local gateway is constructed, across every HTTP and non-HTTP entry point.","root_cause":"HTTP `status`, `attach`, and `ingest` bypass `_read`/`_write_call`, while MCP registry and scheduled jobs use shared scope helpers that still construct local gateways; these symbols and tests are absent from the plan.","section_id":"5.1","severity":"blocking"},{"category":"missing-requirement","check_key":"relative-registry-write-invariant","description":"The first topic registration after migration can immediately reintroduce absolute registry paths and the removed `topics/` prefix.","finding_id":"F11-wiki-registry-writer","fix":"Target `crates/gwiki/src/registry.rs` and affected integration fixtures; serialize child paths relative to the wiki-home registry parent, reject escapes, update fixtures, and run the affected gwiki integration tests.","location":"P4 / § 4.1 and P6 / § 6.1","prevention":"For every migration normalization, locate all ongoing writers and fixtures that can recreate the old shape.","principle":"A migrated serialization invariant must also be enforced by every future writer.","root_cause":"Migration rewrites `wikis.json` to relative paths once, while current `register_scope` persists absolute `scope.root()` values and existing fixtures retain `hub/topics/<name>`.","section_id":"6.1","severity":"blocking"},{"category":"traceability","check_key":"new-router-registration","description":"`GET` and `PUT /api/files/user-md` can remain unreachable even if their handler module is implemented.","finding_id":"F12-files-router-registration","fix":"Add `src/gobby/servers/routes/__init__.py` and `src/gobby/servers/_app_routes.py` exact targets, export/include the router, and add an assembled FastAPI application test for both endpoints.","location":"P5 / § 5.1","prevention":"For every new router module, inspect package exports, application include registries, and an assembled-app test.","principle":"A new route module is incomplete until exported, registered in the assembled application, and exercised through that assembly.","root_cause":"The plan adds `hub_files_proxy.py` but omits explicit router export and `_app_routes.py` inclusion inventories.","section_id":"5.1","severity":"blocking"},{"category":"traceability","check_key":"new-cli-registration","description":"`gobby files migrate` will not exist from the root CLI unless registration is added.","finding_id":"F13-files-cli-registration","fix":"Add `src/gobby/cli/__init__.py` as a target, register the files group, and include a root-command smoke/invocation test.","location":"P6 / § 6.1","prevention":"For every new CLI module, inspect root imports, `add_command` inventory, help exposure, and top-level invocation.","principle":"A new Click command module is incomplete until imported and registered by the root command.","root_cause":"The plan creates `src/gobby/cli/files.py` but omits the explicit root CLI registry.","section_id":"6.1","severity":"blocking"},{"category":"gobby-format","check_key":"projected-source-size-ceiling","description":"The lifecycle implementation has only 69 available lines before violating the enforced monolith ceiling.","finding_id":"F14-hub-backup-decomposition","fix":"Plan a focused hub-backup files-home archive/restore module before editing `cli.py`; keep `cli.py` orchestration-only and record projected post-change counts for all touched production sources.","location":"P6 / § 6.2","prevention":"Record current and projected line counts for each touched production file and plan decomposition whenever the projection reaches the ceiling.","principle":"Every touched hand-maintained production source must remain below 1,000 lines after the planned edit.","root_cause":"`src/gobby/cli/hub_backup/cli.py` is already 930 lines, while 6.2 adds archive creation, manifest integration, restore extraction, collision policy, and confirmation behavior to three functions without a decomposition.","section_id":"6.2","severity":"blocking"},{"category":"weak-testability","check_key":"existing-lifecycle-test-contracts","description":"The new test can pass while established pack and backup contracts fail or retain stale expectations.","finding_id":"F15-backup-test-inventory","fix":"Add `tests/cli/test_pack.py` and `tests/cli/hub_backup/test_cli.py` to 6.2; update their bootstrap fixtures, exact inventories, summaries, overwrite behavior, and restore order while retaining the new end-to-end round trip.","location":"P6 / § 6.2","prevention":"Blast-radius every exact inventory/order assertion when adding a lifecycle artifact and include those suites in Targets and validation.","principle":"Behavior that changes exact artifact inventories, restore order, summaries, and bootstrap prerequisites must update the existing contract suites that encode them.","root_cause":"Section 6.2 targets only a new round-trip test, while current pack and hub-backup tests assert exact stores/artifacts, JSON summaries, restore sequencing, and bootstrap fixtures.","section_id":"6.2","severity":"blocking"},{"category":"unhandled-edge","check_key":"async-http-proxy-boundary","description":"A slow hub request can block unrelated daemon requests for the full timeout.","finding_id":"F16-async-proxy-boundary","fix":"Specify an async raw method within the existing DaemonClient abstraction using the same URL/auth/error logic, or offload the complete synchronous request/stream lifecycle to a worker thread; add concurrency and cancellation tests.","location":"P5 / § 5.1","prevention":"At every sync/async boundary, test delayed dependency response, unrelated event-loop progress, cancellation, and cleanup.","principle":"Network I/O invoked from async routes must yield the event loop through timeout and cancellation.","root_cause":"The planned reuse of `DaemonClient.call_http_api` is synchronous, while wiki and attachment handlers are async; no async or worker-thread boundary is specified.","section_id":"5.1","severity":"blocking"},{"category":"unhandled-edge","check_key":"hub-proxy-loop-prevention","description":"Misconfigured or aliased hub origins have no typed terminal outcome before timeout/resource exhaustion.","finding_id":"F17-proxy-loop","fix":"Add a files-proxy hop marker rejected on repeated remote forwarding, verify the authenticated target reports local files ownership before use, reject obvious self-origin cases, and test same-origin, alias, remote-to-remote, and two-node cycles.","location":"P1 / § 1.1 and P5 / § 5.1","prevention":"For every proxy-to-same-route design, validate target ownership and carry a bounded hop marker with cycle tests.","principle":"A same-path proxy must bound and reject self or remote-to-remote forwarding cycles.","root_cause":"Bootstrap validates only URL syntax, so a node can point `hub_daemon_url` to itself, another remote daemon, or a two-node cycle and recursively forward identical file routes until exhaustion.","section_id":"5.1","severity":"blocking"},{"category":"unhandled-edge","check_key":"files-home-bind-loss","description":"A bind-loss race can silently create a new local directory rather than raising `FilesHomeError`.","finding_id":"F18-files-root-recreation","fix":"Add no-root-create files-home write primitives or equivalent root-held/revalidated mutation, use them for USER.md and attachment descendants, and test lost/replaced root behavior.","location":"P1 / § 1.1, P3 / § 3.1, and P5 / § 5.1","prevention":"For bind-owned roots, review every parent-creating write and inject root deletion/replacement between resolution and mutation.","principle":"Missing or lost `files_home` must remain a typed failure; descendant writes may create content only beneath a still-present owner root.","root_cause":"`durable_replace` and attachment upload use `mkdir(parents=True)`, so removal or unmount after `require_files_home` can recreate the configured mount path and write canonical data outside the bind.","section_id":"5.1","severity":"blocking"},{"category":"unhandled-edge","check_key":"migration-crash-recovery","description":"The one-shot command can strand user data across old and new roots with no permitted recovery path.","finding_id":"F19-migration-recovery","fix":"Require a daemon-stopped or maintenance-fenced epoch and migration lock; stage, fsync, verify, and publish each source with a durable journal, delete sources only after publication, make reruns resume or roll back recognized states, and test failure after every source class.","location":"P6 / § 6.1","prevention":"Enumerate failure after every migration step and cross-filesystem copy, then specify exclusive ownership, durable state, resume/rollback, and injected-failure tests.","principle":"A multi-source destructive migration must have exclusive ownership and a durable resume or rollback path for every partial state.","root_cause":"Preflight prevents initial conflicts, while a crash, EXDEV copy failure, or concurrent daemon write after the first move leaves an incomplete destination that the specified second run refuses; no journal or maintenance exclusion exists.","section_id":"6.1","severity":"blocking"},{"category":"unhandled-edge","check_key":"cross-store-restore-recovery","description":"A mid-restore failure can produce a durable cross-store split with no journal and allow restart into inconsistent state.","finding_id":"F20-restore-recovery","fix":"Preflight all artifacts and collisions, extract files into same-filesystem staging, run DB plus file publication under a durable maintenance journal, atomically publish only after prerequisites succeed, block daemon restart until completion, and add injected-failure tests around every boundary.","location":"P6 / § 6.2","prevention":"For each multi-store restore, test failure before and after every publication boundary and require durable completion/resume/rollback state.","principle":"Restoring related database and file state must preflight and recover as one maintenance campaign so generations cannot diverge silently.","root_cause":"Section 6.2 specifies collisions and a successful round trip, while current DB restore and unpack mutate final destinations sequentially; failure after either commits can leave attachment rows, wiki metadata, and files from different backup generations.","section_id":"6.2","severity":"blocking"}],"reviewer_session":"355cda97-8d5f-4edd-bf6e-802bed06ae0b","round":1,"verdict":"needs_review"},"session_id":"e18533a2-3d7e-4fa4-bdc4-df7028759faf"}
```

**Round 2** `kind: verification`

- reviewer_run:  cbb81bc5-0658-4642-a854-bcb3aa9df307
- reviewer_session:  df649f5a-2010-4ed0-8dd9-5a3075f6f372
- verdict: needs_review
- findings:
- F21-bootstrap-authoring / blocking / fresh install has no files_home acquisition or persist-before-identity order
- F22-bootstrap-consumer-fixtures / blocking / daemon-url and postgres/falkordb present-file fixtures omit files_home
- F23-websocket-chat-attachments-target / blocking / stored-chat bind lives in websocket/chat_attachments.py
- F24-personal-wiki-shared-resolver / blocking / scope_resolution.py requires repo_path for personal
- F25-compose-bind-runtime / blocking / resolve_compose_runtime does not project files_home
- F26-bundled-manifest-order / blocking / 6.3 intro edit leaves bundled digest stale
- F27-remote-wiki-command-protocol / blocking / MCP/scheduled commands lack hub HTTP routes; file paths must not cross machines
- F28-attachment-cleanup-ownership / blocking / remote row delete orphans hub bytes; unlink-after-delete is not retryable
- F29-proxy-streaming-bounds / blocking / buffered proxy cannot carry 100MB attachments or FileResponse ranges
- F30-files-home-root-capability / blocking / re-stat of the path string cannot detect a replaced bind
- F31-migration-writer-exclusion / blocking / exclusive lock does not exclude file-only writers
- F32-migration-registry-inventory / blocking / hub wiki registry omitted from migrate sources
- F33-archive-extraction-bounds / blocking / unpack reads whole members and can leave torn files
- F34-migration-baseline-tree / blocking / empty-source migrate never initializes the destination skeleton
- F35-gcore-compose-asset / blocking / gcore compose copy has no consumer and is already deleted
- resolution_notes: Accepted F21–F30 and F32–F35. Declined F31's live-daemon reader-writer lock; migrate requires the hub daemon stopped (Constraint 18). F27 repaired as additional `/api/wiki/...` routes plus streamed attach/ingest bytes, not a second RPC protocol. F29 preserves existing FileResponse headers rather than inventing a cache protocol. F30 is one verified-root device/inode helper. F35 removes the gcore compose target. Companion ledger must be re-pinned to the repaired plan hash before expansion.

```json plan-review-round
{"evidence_id":"a8305788-e82d-4db9-9ffe-57d4f12097fc","plan_hash":"d75f573ac262d871ea94fc470a352f7831689e3c764f7c66972810ece9fb4810","round_number":2,"round_result":{"coverage_attestation":{"adjacent_variant_complete":true,"attestation_digest":"3dafedd79b608f68cbe9ab28b991bf22fd2f7fa55e04f601719b4900e452e8e2","cross_lane_interaction_complete":true,"disposition_counts":{"dismissed":4,"emitted_findings":15,"total":19},"evidence_id":"a8305788-e82d-4db9-9ffe-57d4f12097fc","lanes":[{"candidate_count":2,"lane_id":"requirements_traceability","status":"completed"},{"candidate_count":6,"lane_id":"repository_blast_radius","status":"completed"},{"candidate_count":11,"lane_id":"runtime_invariants","status":"completed"}],"shadow_manifest_status":{"entry_count":8,"manifest_digest":"9f4b58146e7c36fd1d365625f89f950c3544e5e01a870af42adf516204a9ed34","status":"valid"},"source_digest":"ae695b6156abdda73412ad75dc9d446dba921ae0bb8b78838ec03adef93e4580","version":1},"findings":[{"category":"missing-requirement","check_key":"fresh-install-files-home-provisioning","description":"A fresh local install has no specified way to obtain or persist the required absolute files_home before identity writes or managed PostgreSQL startup, so the repaired required-field contract makes the supported creation paths fail after partial side effects.","finding_id":"F21-bootstrap-authoring","fix":"Add a required local-install files_home input, validate that it is an existing absolute directory, target ensure_daemon_config and the PostgreSQL installer caller, render the value into template and fallback bootstraps, and reorder install so bootstrap persistence succeeds before personal identity or service startup. Add fresh-install and injected-bootstrap-write-failure acceptance tests.","location":"P1 / § 1.1 and P2 / § 2.1","prevention":"For every required bootstrap field, trace command input through every template, fallback writer, installer caller, and first runtime consumer, including the missing-file path.","principle":"A new required bootstrap field needs an operator acquisition path and must be persisted before its first consumer or any managed-service side effect.","root_cause":"Fresh install currently reaches personal identity before daemon config creation, while ensure_daemon_config and the PostgreSQL installer writer have no files_home input and are absent from Targets.","section_id":"1.1","severity":"blocking"},{"category":"traceability","check_key":"required-bootstrap-fixture-inventory","description":"Known indirect load_bootstrap consumers will violate the new required local field even if the named § 1.1 tests pass, leaving predictable failures outside the plan's target and validation surface.","finding_id":"F22-bootstrap-consumer-fixtures","fix":"Add tests/utils/test_daemon_url.py, tests/config/test_bootstrap_postgres.py, tests/config/test_bootstrap_falkordb.py, and any other present-file fixtures found by the class-wide sweep to § 1.1 Targets and V2; give present local fixtures absolute files_home values while preserving true missing-file default cases.","location":"P1 / § 1.1","prevention":"Run caller and fixture inventory for every shared parser change, then list every file that must change and every focused suite in validation.","principle":"Tightening a shared configuration contract requires every present-file fixture and focused suite that consumes it to be in the implementation inventory.","root_cause":"Daemon-URL, PostgreSQL-bootstrap, and FalkorDB-bootstrap tests create present local bootstraps without files_home, while § 1.1 Targets and V2 name only a subset of bootstrap suites.","section_id":"1.1","severity":"blocking"},{"category":"traceability","check_key":"stored-chat-websocket-target","description":"The remote upload-to-WebSocket bind repair is aimed at the wrong attachment module, so the actual stored-chat path can retain machine_id filtering and fail after a successful remote upload.","finding_id":"F23-websocket-chat-attachments-target","fix":"Replace or supplement the current WebSocket Target with src/gobby/servers/websocket/chat_attachments.py and its prepare/bind callers, keep the terminal-proxy attachment module explicitly machine-local, and make the remote upload-then-prepare_message_attachments bind path an acceptance test.","location":"P3 / § 3.1 and P5 / § 5.1","prevention":"Resolve similarly named subsystems through their callers before adding Targets, then test the end-to-end entry point that reaches the affected storage method.","principle":"A machine-identity repair must target the actual persisted-chat binder that performs the affected lookup and bind operations.","root_cause":"The plan targets src/gobby/servers/websocket/attachments.py, which handles daemon-local terminal attachments, while src/gobby/servers/websocket/chat_attachments.py calls get_attachments_by_ids and bind_attachments for stored chat uploads.","section_id":"3.1","severity":"blocking"},{"category":"unhandled-edge","check_key":"checkout-free-personal-scope-resolution","description":"The checkout-free personal sentinel still cannot traverse the shared Python resolver used by routes, MCP, and scheduled jobs, and route-only reserved-name checks leave adjacent entry points inconsistent.","finding_id":"F24-personal-wiki-shared-resolver","fix":"Target src/gobby/wiki/scope_resolution.py, recognize PERSONAL_PROJECT_ID and reserved topic names there, return a first-class personal owner scope without requesting repo_path, and dispatch remote personal/topic scopes before local path existence checks. Cover HTTP, MCP, fresh scheduled registration, and parked-row recovery.","location":"P2 / § 2.1, P4 / § 4.1, and P5 / § 5.1","prevention":"Trace each semantic scope from HTTP, MCP, and scheduled registration through resolution, path lookup, dispatch, and gateway construction; test sentinels with repo_path absent.","principle":"A checkout-free semantic scope must be recognized before any shared resolver requires a checkout path.","root_cause":"Current scope_resolution.py resolves every project scope through LocalProjectManager.repo_path, so the personal sentinel can be rejected or parked before the planned downstream owner_dispatch and _gateway_for_resolved seams run.","section_id":"4.1","severity":"blocking"},{"category":"traceability","check_key":"compose-bind-config-projection","description":"The plan cannot materialize the configured files_home in Compose as written; adding gobby_files to the existing volume map either creates the wrong named-volume shape or leaves the host path unavailable to lifecycle tooling.","finding_id":"F25-compose-bind-runtime","fix":"Define gobby_files as lifecycle metadata in the canonical Python Compose asset, project a validated GOBBY_FILES_HOME value from bootstrap through src/gobby/cli/installers/compose_env.py, target that resolver and both Compose environment/template suites, and keep datastore volume enumeration unchanged.","location":"P6 / § 6.2","prevention":"For every new Compose interpolation, trace bootstrap value to rendered environment and template parsing, then include both environment and template contract suites.","principle":"A Compose lifecycle entry backed by runtime configuration needs an explicit configuration projection and contract tests.","root_cause":"The canonical Compose environment builder exports no files_home value, and § 6.2 targets neither resolve_compose_runtime nor its tests while requiring a configured host bind that is distinct from datastore named volumes.","section_id":"6.2","severity":"blocking"},{"category":"bad-sequencing","check_key":"bundled-content-manifest-finalization","description":"Executing the current leaf order leaves the intro skill's recorded bundled-content digest stale after § 6.3, even though § 1.1 previously regenerated the same manifest.","finding_id":"F26-bundled-manifest-order","fix":"Add src/gobby/install/bundled_content_manifest.json and tests/install/test_bundled_content_manifest.py to § 6.3, regenerate after the intro edit, and include that integrity test in V2.","location":"P1 / § 1.1 and P6 / § 6.3","prevention":"Inventory every bundled-content edit across all leaves and place manifest regeneration and its validation after the last dependent edit.","principle":"A generated digest manifest must be regenerated after the final bundled-content edit in the dependency order.","root_cause":"Section 1.1 regenerates bundled_content_manifest.json for bootstrap.yaml, then dependent § 6.3 edits the bundled intro skill without targeting the manifest or its integrity test.","section_id":"6.3","severity":"blocking"},{"category":"missing-requirement","check_key":"remote-wiki-command-matrix","description":"MCP and scheduled topic/personal work can reach commands that the proposed HTTP proxy cannot execute, and forwarding file paths would make the hub read paths from the wrong machine.","finding_id":"F27-remote-wiki-command-protocol","fix":"Specify one authenticated typed hub command endpoint covering the full GwikiGateway command matrix, wire HTTP, MCP, and scheduled callers through it, transmit file inputs as streamed bytes plus metadata, and add table-driven remote tests for every command and file-input variant.","location":"P5 / § 5.1","prevention":"Build a table of every gateway command and caller, then prove each argument and result is serializable across the owner boundary without interpreting a node path on the hub.","principle":"Every non-local operation needs a complete serializable owner protocol, including non-HTTP callers and file-bearing arguments.","root_cause":"Generic inbound method/path forwarding covers existing route calls, while MCP and scheduled GwikiGateway commands such as trust, refresh, export, sync-sessions, upkeep, librarian, and recap have no matching hub endpoints; attach and ingest can carry node-local paths.","section_id":"5.1","severity":"blocking"},{"category":"unhandled-edge","check_key":"attachment-cleanup-owner-gating","description":"A remote maintenance loop can delete the only hub attachment locator and skip unlink, permanently orphaning canonical bytes; the same row-first order loses retryability when owner unlink fails.","finding_id":"F28-attachment-cleanup-ownership","fix":"Target cleanup_chat_attachments_loop and every bulk-delete caller, return before shared-row mutation on remote daemons, and on the owner unlink through the reconstructed resolver before deleting the row, treating FileNotFound as completed cleanup and retaining the row on other failures. Add remote-no-op and owner retry tests.","location":"P3 / § 3.1 and P5 / § 5.1","prevention":"For every DB-row-plus-file deletion, trace operation order, daemon ownership, retry state, FileNotFound behavior, and failure after each step.","principle":"Destructive metadata cleanup must retain the canonical locator until the storage owner has successfully removed the corresponding bytes.","root_cause":"Current cleanup deletes stale rows before unlinking, and the plan explicitly permits remote cleanup to delete or mark the shared row while skipping node-local unlink.","section_id":"3.1","severity":"blocking"},{"category":"unhandled-edge","check_key":"bounded-proxy-streaming","description":"Remote attachments can be 100 MB each, so buffered proxying multiplies memory by concurrency and diverges from the local FileResponse contract even though the event loop remains technically asynchronous.","finding_id":"F29-proxy-streaming-bounds","fix":"Require DaemonClient streaming requests and StreamingResponse backpressure for upload/download, forward an explicit allowlist of Range and conditional request headers plus Content-Length, Content-Range, Accept-Ranges, and ETag responses, enforce upload limits while streaming, and add slow-consumer, range, cancellation, and maximum-size tests.","location":"P5 / § 5.1","prevention":"Test maximum-size concurrent transfers, ranges, slow consumers, disconnect cancellation, header parity, and client-resource closure for every file proxy.","principle":"A proxy for large file responses must preserve streaming, backpressure, cancellation, and representation semantics across the added hop.","root_cause":"The planned async raw DaemonClient method preserves final bytes and two headers, while ordinary httpx request handling buffers responses and the plan omits Range, conditional, length, and entity headers.","section_id":"5.1","severity":"blocking"},{"category":"unhandled-edge","check_key":"files-home-root-identity","description":"The stated lost-or-replaced-bind failure rule is enforceable for neither replacement nor all writers; several planned paths can recreate the mountpoint or publish into a substituted directory.","finding_id":"F30-files-home-root-capability","fix":"Specify one verified-root contract that records the owner directory device/inode, revalidates it at publication, performs descendant operations without creating the configured root, and is used by Python profile/attachment/migrate/restore paths and Rust wiki initialization/registry writes. Add removal and replacement tests for each writer class.","location":"P3 / § 3.1 through P6 / § 6.2","prevention":"Inventory every files_home writer in each language and inject root removal plus device/inode replacement immediately before publication.","principle":"A missing-or-replaced bind invariant requires stable root identity and descendant-only writes across every writer class.","root_cause":"require_files_home re-stats only the configured path, while durable_replace, Rust registry writes, migration, unpack, and restore can recursively create parents or write after a replacement race without comparing directory identity.","section_id":"3.1","severity":"blocking"},{"category":"unhandled-edge","check_key":"migration-writer-fence","description":"The accepted exclusive-lock repair does not currently exclude file-only owner writes, so migration preflight can become stale and a concurrent write can be moved, overwritten, or omitted.","finding_id":"F31-migration-writer-exclusion","fix":"Define a cross-process files-home lock outside the bind: profile, attachment, and wiki owner writes acquire it shared for their whole filesystem operation, while migrate acquires it exclusive from source discovery through registry publication. Target every acquisition site and add concurrent-write tests. Keep resume-on-rerun and the no-journal decision unchanged.","location":"P6 / § 6.1","prevention":"For any exclusive maintenance operation, enumerate every writer and test that each blocks from discovery through final publication.","principle":"An exclusive migration lock is effective only when every concurrent writer participates in the same lock protocol.","root_cause":"The existing maintenance epoch fences database connections and has no files migration campaign; profile, attachment, and wiki writes can proceed without it while § 6.1 assumes an exclusive maintenance lock prevents tree mutation.","section_id":"6.1","severity":"blocking"},{"category":"missing-requirement","check_key":"migration-registry-source-inventory","description":"Existing topic registrations can be lost or transformed nondeterministically because their persisted registry is absent from preflight and the resume state matrix.","finding_id":"F32-migration-registry-inventory","fix":"Add ~/wiki/wikis.json as an explicit source, define deterministic transformation of every registration to wiki-home-relative children, refuse malformed or unknown entries before source mutation, publish the registry after verified vault moves, and test multiple registrations plus a crash before registry publication.","location":"P6 / § 6.1","prevention":"For each migrated subsystem, inventory data files plus indexes/registries/metadata and test present, missing, malformed, partial-publication, and rerun states.","principle":"Migration preflight must inventory every persisted source whose contents affect post-migration behavior.","root_cause":"The source-class list includes ~/wiki/topics/<name> while omitting the sibling ~/wiki/wikis.json registry, and 'rewrite the registry' does not define preservation, filtering, malformed-entry handling, publication order, or rerun state.","section_id":"6.1","severity":"blocking"},{"category":"unhandled-edge","check_key":"bounded-archive-extraction","description":"A large or compressed files_home member can exhaust memory, and interruption can leave a torn final file that the existing collision policy treats as authoritative on rerun.","finding_id":"F33-archive-extraction-bounds","fix":"Require fixed-size chunk extraction into same-filesystem temporary files, fsync and atomically replace each verified member, preflight explicit member-count and aggregate uncompressed-size limits before mutation, and add maximum-size, compressed-member, truncation, and rerun tests. Preserve the declined cross-store-journal decision.","location":"P6 / § 6.2","prevention":"Test maximum allowed members, aggregate declared size, compressed large members, truncation during copy, publication failure, and operator rerun.","principle":"Restoring an unbounded file tree requires bounded-memory extraction and atomic per-file publication.","root_cause":"Current unpack reads each tar member fully into memory and writes final paths directly, while § 6.2 adds large attachment/wiki members without chunk bounds, declared-size/member-count policy, or interrupted-member handling.","section_id":"6.2","severity":"blocking"},{"category":"missing-requirement","check_key":"migration-empty-source-baseline","description":"A clean hub with no legacy personal or wiki data can finish migration without creating the required USER.md, _personal subdirectories, wiki directory, and registry baseline.","finding_id":"F34-migration-baseline-tree","fix":"After validating the pre-provisioned files_home root, add a source-independent initialization step that creates empty USER.md, _personal/{notes,reminders,attachments}, and wiki plus its baseline registry without creating the root itself; add an empty-source acceptance test and keep later source moves collision-safe.","location":"P6 / § 6.1","prevention":"Test migrations with zero, partial, and complete legacy inputs, and separate destination initialization from source movement.","principle":"Baseline destination initialization is a source-independent migration obligation and needs an empty-input acceptance case.","root_cause":"The architecture creates the files-home skeleton before optional legacy moves, while § 6.1 skips missing sources and only asserts movement of present source classes.","section_id":"6.1","severity":"blocking"},{"category":"over-engineering","check_key":"single-canonical-compose-asset","description":"The new gcore Compose copy adds an unsynchronized surface, has no plan consumer, and guarantees failure of the existing standalone-removal regression.","finding_id":"F35-gcore-compose-asset","fix":"Remove crates/gcore/assets/docker-compose.services.yml from Targets and update only src/gobby/data/docker-compose.services.yml plus its Python lifecycle consumers and tests.","location":"P6 / § 6.2","prevention":"Before adding a duplicate asset, search consumers and deleted-surface regression contracts, then retain the canonical source unless a second runtime consumer is proven.","principle":"A duplicate generated or deployed asset needs a concrete consumer; one canonical asset is the proportional form when no second consumer exists.","root_cause":"Section 6.2 creates crates/gcore/assets/docker-compose.services.yml without naming a Rust consumer, while standalone_modules_and_assets_are_deleted explicitly requires that path to remain absent.","section_id":"6.2","severity":"blocking"}],"reviewer_session":"df649f5a-2010-4ed0-8dd9-5a3075f6f372","round":2,"verdict":"needs_review"},"session_id":"e18533a2-3d7e-4fa4-bdc4-df7028759faf"}
```

**Round 3** `kind: verification`

- reviewer_run: 940c249b-4791-4e75-bbf3-b45be9e818fc
- reviewer_session: 08650f7b-fd4e-4450-8732-f7408d834ab3
- verdict: needs_review
- findings:
- F36-config-projection-acceptance / blocking / to_config_dict and DaemonConfig lack acceptance
- F37-wiki-root-loss-acceptance / blocking / 4.1 omits missing/replaced owner-root refusal
- F38-migration-exdev-acceptance / blocking / EXDEV verify-then-delete untested
- F39-migration-after-owner-cutover / blocking / 6.1 can run before attachment cutover
- F40-bootstrap-fixture-blast-radius / blocking / remote-daemon and UI-exposure fixtures omitted
- F41-personal-project-test-surface / blocking / TestPersonalProjectEnsure cases omitted
- F42-attachment-storage-contract-tests / blocking / storage-layer attachment suite omitted
- F43-wiki-flat-topic-integration-fixtures / blocking / Rust init/export fixtures still nest topics/
- F44-background-wiki-owner-dispatch / blocking / watchers and prune construct local gateways
- F45-attachment-bind-cleanup-serialization / blocking / bind can race unlink-before-delete
- F46-migration-baseline-before-sources / blocking / baseline seeding can collide with later sources
- F47-bootstrap-read-modify-write-serialization / blocking / update_bootstrap_yaml is unlocked RMW
- F48-proxy-stream-timeout-policy / blocking / streaming reuses the five-second scalar timeout
- F49-remote-conversation-cleanup-owner-dispatch / blocking / conversation clear/delete is not proxied
- resolution_notes: Accepted F36–F38 and F40–F49. Accepted F39's sequencing gap but declined a P5 dependency; 6.1 now depends on 3.1 and 4.1 so local attachment and wiki consumers exist before bytes move. F47 reuses exclusive_file_lock. F45 is a per-row deleting claim. F48 specifies connect/inactivity/deadline on the existing streaming DaemonClient. Companion ledger must be re-pinned to the repaired plan hash before expansion.

```json plan-review-round
{"evidence_id":"a9e17db4-b56f-4a3e-be2f-1753a56cbad3","plan_hash":"0f8126ef8efa8b911038b731071fcea5ebfa43612ef9fac022082f6e92574cdb","round_number":3,"round_result":{"coverage_attestation":{"adjacent_variant_complete":true,"attestation_digest":"127a318f89373a584e68994b79bbd0c64a28dbcb7930775e05de47bbcae0ca86","cross_lane_interaction_complete":true,"disposition_counts":{"dismissed":2,"emitted_findings":14,"total":16},"evidence_id":"a9e17db4-b56f-4a3e-be2f-1753a56cbad3","lanes":[{"candidate_count":4,"lane_id":"requirements_traceability","status":"completed"},{"candidate_count":5,"lane_id":"repository_blast_radius","status":"completed"},{"candidate_count":7,"lane_id":"runtime_invariants","status":"completed"}],"shadow_manifest_status":{"entry_count":8,"manifest_digest":"f2c8fc34d6597ce6f3ef86a6748d77557c3c19693639a8c878b5cc2115ac00f0","status":"valid"},"source_digest":"393f882ef25f17ba3e22dfdfbfa6057eab3c1480299cf3efd9666fd6d9f1814d","version":1},"findings":[{"category":"weak-testability","check_key":"config-projection-acceptance","description":"The plan can satisfy every listed acceptance item while leaving files_home and hub_daemon_url stranded before application configuration reaches consumers.","finding_id":"F36","fix":"Add a 1.1 acceptance item and focused app-config tests proving local and remote BootstrapConfig.to_config_dict output plus DaemonConfig propagation; add the ledger row and covers label.","location":"P1 / § 1.1","prevention":"Map each imperative deliverable-body clause to a focused acceptance item, test seam, ledger row, and eventual covers label.","principle":"Every required configuration projection must be represented by acceptance coverage and the coverage ledger.","root_cause":"Section 1.1 requires BootstrapConfig.to_config_dict and DaemonConfig propagation, while acceptance items stop at parsing and persistence.","section_id":"1.1","severity":"blocking"},{"category":"weak-testability","check_key":"wiki-root-loss-acceptance","description":"Wiki init or register_scope could recreate or write through a missing/replaced owner root without failing any 4.1 acceptance item.","finding_id":"F37","fix":"Add a 4.1 item covering Rust init/register_scope and the Python wiki owner entry against missing and replaced files_home roots, asserting typed refusal and zero root recreation; mirror it in the ledger and covers labels.","location":"P4 / § 4.1","prevention":"For every owner-root consumer, include missing, replaced, and no-recreation cases in its own acceptance inventory.","principle":"A required typed refusal path needs direct acceptance coverage at each owning subsystem boundary.","root_cause":"The wiki body requires missing-root refusal and no root recreation, while 4.1.1-4.1.9 contain no missing or replaced files_home case.","section_id":"4.1","severity":"blocking"},{"category":"weak-testability","check_key":"migration-exdev-acceptance","description":"The migration could truncate or delete a legacy source during a cross-device move while all thirteen listed acceptance items still pass.","finding_id":"F38","fix":"Add injected-EXDEV tests proving verified copy success removes the source and verification/truncation failure preserves the source with no authoritative partial destination; add the ledger row and covers label.","location":"P6 / § 6.1","prevention":"Enumerate each non-atomic migration fallback and test its last safe recovery point.","principle":"Failure-sensitive migration fallbacks require acceptance tests for success, verification failure, and source-preservation ordering.","root_cause":"The body specifies EXDEV copy, verification, and delete-after-verify semantics without a matching acceptance item.","section_id":"6.1","severity":"blocking"},{"category":"bad-sequencing","check_key":"migration-after-owner-cutover","description":"The typed manifest can schedule migration before 5.1, allowing attachment bytes to move before remote owner dispatch exists.","finding_id":"F39","fix":"Change 6.1 to depend on P5; 5.1 already depends on 3.1 and 4.1, so migration becomes runnable after local attachment/wiki and remote-owner cutovers.","location":"P6 / § 6.1","prevention":"Derive migration dependencies from every resource class moved and every reader/writer switched.","principle":"A one-shot migration that retires legacy reads must run only after every destination consumer and remote-owner path is cut over.","root_cause":"Section 6.1 depends on P4, while attachment cutover is in P3 and remote owner dispatch is in P5.","section_id":"6.1","severity":"blocking"},{"category":"weak-testability","check_key":"bootstrap-fixture-blast-radius","description":"Existing remote-daemon and UI-exposure tests will encode invalid bootstrap files after 1.1, yet the plan neither targets nor validates them.","finding_id":"F40","fix":"Add tests/cli/test_daemon_remote_mode.py and tests/test_ui_exposure.py to 1.1 Targets and V2; supply hub_daemon_url to remote fixtures and an existing absolute files_home to implicit-local present-file fixtures.","location":"P1 / § 1.1","prevention":"Inventory every load_bootstrap test fixture whenever present-file validation gains a required key.","principle":"A stricter present-file contract must update every reachable fixture and validate those suites explicitly.","root_cause":"Remote-daemon and UI-exposure fixtures create present bootstrap files without the new mode-specific field and are absent from Targets and V2.","section_id":"1.1","severity":"blocking"},{"category":"weak-testability","check_key":"personal-project-test-surface","description":"Unlisted personal-project behaviors can retain legacy-tree assumptions or fail after files_home becomes authoritative.","finding_id":"F41","fix":"Expand the target to tests/storage/test_project_manager.py::TestPersonalProjectEnsure::* and retarget every helper, repair, preservation, corruption, and failure case to configured files_home while keeping remote checkout-free sentinel coverage.","location":"P2 / § 2.1","prevention":"Sweep the complete test class for every changed path resolver and identity helper.","principle":"When a shared path seam changes, the plan must retarget the full behavior class that exercises it.","root_cause":"Section 2.1 names two tests while TestPersonalProjectEnsure covers constructors, repair, preservation, corruption, and write-failure paths using the old gobby_home seam.","section_id":"2.1","severity":"blocking"},{"category":"weak-testability","check_key":"attachment-storage-contract-tests","description":"Route tests alone can miss incorrect hub-row visibility, bind conflicts, audit machine_id behavior, or cleanup retry ordering.","finding_id":"F42","fix":"Add tests/storage/test_chat_attachments.py::* to 3.1, 5.1, and V2; replace machine-isolation expectations with project-and-attachment-ID hub visibility and retain bind, unlink-before-delete, remote no-op, and unlink-failure retry cases.","location":"P3 / § 3.1 and P5 / § 5.1","prevention":"Trace every changed storage method to its direct contract tests before relying on route-level coverage.","principle":"Changing row visibility and ownership semantics requires preserving and updating the storage-layer contract suite.","root_cause":"The existing attachment storage suite asserts machine isolation and bulk cleanup semantics, while sections 3.1 and 5.1 omit it from Targets and V2.","section_id":"3.1","severity":"blocking"},{"category":"weak-testability","check_key":"wiki-flat-topic-integration-fixtures","description":"The unit-level scope changes can pass while Rust integration binaries continue asserting or producing the retired nested topic layout.","finding_id":"F43","fix":"Add crates/gwiki/tests/common/mod.rs, crates/gwiki/tests/cli_init.rs, and crates/gwiki/tests/cli_export.rs to Targets; update fixtures to direct wiki-home children and run both integration binaries in V2.","location":"P4 / § 4.1","prevention":"Search construction and assertion sites for every removed path component, including shared test helpers.","principle":"A filesystem layout change must update shared integration fixtures and CLI assertions that materialize that layout.","root_cause":"Rust common, cli_init, and cli_export fixtures still construct wiki/topics/<name>, outside section 4.1 Targets and validation.","section_id":"4.1","severity":"blocking"},{"category":"unhandled-edge","check_key":"background-wiki-owner-dispatch","description":"A remote daemon can access or mutate node-local topic/personal wiki state through watcher or prune paths despite route-level owner dispatch.","finding_id":"F44","fix":"Target runner_lifecycle_periodic.py, runner_lifecycle_subsystems.py, wiki/prune_job.py, and their tests; keep checkout-project watchers local, owner-gate or proxy topic/personal watcher and prune work, and prove remote mode creates no node-local wiki state.","location":"P5 / § 5.1","prevention":"Inventory constructors and background registrations, including factories and cron callbacks, for each owner-dispatched subsystem.","principle":"Every remote-reachable consumer of hub-owned state must cross the same owner boundary before constructing a local gateway.","root_cause":"The proxy inventory covers routes and scheduled jobs while periodic topic watchers and prune_all_scopes startup registration still receive local GwikiGateway instances.","section_id":"5.1","severity":"blocking"},{"category":"unhandled-edge","check_key":"attachment-bind-cleanup-serialization","description":"A conversation can successfully bind an attachment whose canonical bytes are then removed by an already-running stale cleanup.","finding_id":"F45","fix":"Add a serialized per-row cleanup claim or deleting state that bind waits on or rejects; unlink through the owner resolver, delete only the still-claimed row, release the claim on non-FileNotFound failure, and add bind-versus-cleanup plus dual-worker barrier tests.","location":"P3 / § 3.1 and P5 / § 5.1","prevention":"For destructive background cleanup, test state-transition races with barriers and multiple workers.","principle":"Eligibility checks and destructive cleanup must be serialized against state transitions that revoke eligibility.","root_cause":"Unlink-before-row-delete preserves retryability but leaves a window where bind_attachments can bind the row before cleanup unlinks its bytes.","section_id":"3.1","severity":"blocking"},{"category":"unhandled-edge","check_key":"migration-baseline-before-sources","description":"First migration can manufacture destination collisions from its own baseline setup, causing rename failure or implicit overwrite/merge behavior.","finding_id":"F46","fix":"Complete preflight first, publish each present source into an absent destination, then seed only still-missing baseline artifacts; define per-child equality/collision rules if merging is required and test legacy marker, populated directories, existing registry, empty source, and interrupted rerun.","location":"P6 / § 6.1","prevention":"Preflight all sources and destinations, then order source publication before default seeding.","principle":"Migration must not create destination artifacts that collide with sources scheduled later in the same transaction plan.","root_cause":"Baseline personal markers, notes/reminders directories, and registry initialization can run before legacy sources are moved, leaving directory merge and equality behavior unspecified.","section_id":"6.1","severity":"blocking"},{"category":"unhandled-edge","check_key":"bootstrap-read-modify-write-serialization","description":"Concurrent bootstrap writers can each pass validation while the later replace silently drops the other writer's owner field or unrelated keys.","finding_id":"F47","fix":"Use an existing sidecar-lock pattern across read, update, present-file validation, and durable replace; re-read after acquisition and add a two-process test combining database-default persistence with local or remote owner-field persistence.","location":"P1 / § 1.1","prevention":"Review configuration writers for serialization across the complete read-update-validate-publish interval.","principle":"Atomic replacement does not make a read-modify-write sequence safe against concurrent writers.","root_cause":"update_bootstrap_yaml reads outside any lock, so two valid writers can publish stale mappings and lose files_home or hub_daemon_url.","section_id":"1.1","severity":"blocking"},{"category":"unhandled-edge","check_key":"proxy-stream-timeout-policy","description":"Valid 100 MB transfers or long wiki commands can fail after five seconds of quiet, while disabling the timeout would permit indefinite hangs.","finding_id":"F48","fix":"Define bounded connect/pool timeouts, operation-appropriate read/write inactivity limits, and an overall cancellable deadline; add delayed-first-byte, slow producer, slow consumer, stalled peer, max-size transfer, and cancellation cleanup tests.","location":"P5 / § 5.1","prevention":"For every streamed or long-running proxy, specify connect, read/write inactivity, cancellation, and total deadline semantics.","principle":"Streaming proxies need bounded phase-aware timeouts that distinguish progress from a stalled peer.","root_cause":"The planned streaming calls reuse DaemonClient's scalar five-second timeout without defining connect, inactivity, or overall-deadline behavior.","section_id":"5.1","severity":"blocking"},{"category":"unhandled-edge","check_key":"remote-conversation-cleanup-owner-dispatch","description":"Remote conversation cleanup must either violate the owner boundary, orphan hub-owned attachment files/rows, or abort message clearing.","finding_id":"F49","fix":"Proxy the complete conversation attachment cleanup and message-delete operation to the hub owner, add the HTTP chat route and WebSocket lifecycle handler to 5.1 Targets, and add remote HTTP plus clear-chat tests proving rows, bytes, and messages converge.","location":"P5 / § 5.1 with P3 / § 3.1","prevention":"Trace owner dispatch through aggregate delete and clear operations, including HTTP and WebSocket entry points.","principle":"Cross-owner cleanup must move the complete multi-row operation to the owner, preserving both file/row cleanup and message deletion.","root_cause":"Remote cleanup is forbidden from mutating shared attachment rows, while 5.1 proxies only single-attachment operations and existing HTTP/WebSocket clear flows bulk-delete attachment rows before messages.","section_id":"5.1","severity":"blocking"}],"reviewer_session":"08650f7b-fd4e-4450-8732-f7408d834ab3","round":3,"verdict":"needs_review"},"session_id":"e18533a2-3d7e-4fa4-bdc4-df7028759faf"}
```

**Round 4** `kind: verification`

- reviewer_run:  f19dfa1f-f1f5-4411-83c2-04b48ea6a14d
- reviewer_session:  1c190523-38de-4143-b53f-c39d43e2753e
- verdict: needs_review
- findings:
- F50-attachment-metadata-migration / blocking / migrate does not rewrite chat_attachments.local_path
- F51-wiki-home-nonvault-acceptance / blocking / wiki home vault-marker absence untested
- F52-proxy-conditional-header-acceptance / blocking / Range/conditional/206/304 untested
- F53-restore-preflight-zero-mutation / blocking / late restore preflight failure can mutate
- F54-files-archive-global-preflight / blocking / no member-count, size, duplicate, or prefix-conflict bounds
- F55-remote-hub-auth-provisioning / blocking / remote install has no authenticated owner probe
- F56-datastore-bootstrap-writer-inventory / blocking / expose_datastores omitted from 1.1
- F57-background-wiki-dispatch-test-seams / blocking / lifecycle watcher/prune tests omitted
- F58-source-size-accounting / blocking / runner_lifecycle_subsystems.py size unrecorded
- F59-daemon-client-direct-contract-tests / blocking / DaemonClient suite omitted
- resolution_notes: Accepted F50–F54 and F56–F59. Accepted F55's auth gap but declined --hub-token-stdin; remote install keeps the existing hub local_cli_token copy contract and adds an authenticated owner probe against hub_daemon_url before managed services. F54 specifies 100,000 members and 100 GiB plus duplicate/prefix-conflict preflight. Companion ledger must be re-pinned to the repaired plan hash before expansion.

```json plan-review-round
{"evidence_id":"a5bac0df-6858-4b51-8196-8e8ef78ebe0d","plan_hash":"a63d2fe9525dac500dbb87da641e8312ee349a309e2998cc5b2207f728691ca4","round_number":4,"round_result":{"coverage_attestation":{"adjacent_variant_complete":true,"attestation_digest":"2c283009c1e747aa54313c3659abfa23d398ddb300df24346d486aabf9c09856","cross_lane_interaction_complete":true,"disposition_counts":{"dismissed":4,"emitted_findings":10,"total":14},"evidence_id":"a5bac0df-6858-4b51-8196-8e8ef78ebe0d","lanes":[{"candidate_count":5,"lane_id":"requirements_traceability","status":"completed"},{"candidate_count":5,"lane_id":"repository_blast_radius","status":"completed"},{"candidate_count":4,"lane_id":"runtime_invariants","status":"completed"}],"shadow_manifest_status":{"entry_count":8,"manifest_digest":"605faf401e59866a61df5df12957ff9e8e43b064c41c8a771f90e6032429dabf","status":"valid"},"source_digest":"6d13184e5054824b19b25302d4b8633c57aa9f890564794b9469a97968fad4d8","version":1},"findings":[{"category":"missing-requirement","check_key":"attachment-metadata-migration","description":"The plan can finish migration with attachment rows still carrying canonical or absolute source paths. Once the source tree is retired, those rows remain ambiguous or unusable despite successful byte publication.","finding_id":"F50","fix":"Add src/gobby/storage/chat_attachments.py to §6.1 targets. After verified publication, rewrite every legacy local_path to the deterministic files_home-relative locator in a resumable step; acceptance must prove no migrated row contains an absolute or canonical-source path and that interruption between publication and rewrite converges on rerun.","location":"P3 / §3.1 and P6 / §6.1","prevention":"Inventory persistent locators alongside each migrated byte class and test the crash boundary between publication and metadata rewrite.","principle":"Migration must convert both file bytes and every persisted locator that resolves those bytes.","root_cause":"Section 6.1 publishes legacy attachment bytes into files_home but never rewrites existing chat_attachments.local_path rows; section 3.1 only defines future interpretation and cleanup.","section_id":"6.1","severity":"blocking"},{"category":"weak-testability","check_key":"wiki-home-nonvault-acceptance","description":"An implementation could create files_home/wiki/_gwiki/scope.json and still satisfy all current acceptance items, silently turning the shared wiki home into a scoped vault.","finding_id":"F51","fix":"Add acceptance proving files_home/wiki/_gwiki/scope.json does not exist after bootstrap, gwiki init, legacy migration, and an idempotent rerun.","location":"P4 / §4.1 acceptance","prevention":"Translate every negative filesystem invariant into an explicit path-existence assertion across creation and migration paths.","principle":"A stated filesystem boundary is not enforceable unless acceptance observes its forbidden state directly.","root_cause":"Section 4.1 says the wiki home is an ordinary directory rather than a vault, but none of its acceptance items checks that the root vault marker is absent.","section_id":"4.1","severity":"blocking"},{"category":"weak-testability","check_key":"proxy-conditional-header-acceptance","description":"The proxy may drop conditional headers or Content-Length, Content-Range, Accept-Ranges, and ETag while still passing the current acceptance criteria.","finding_id":"F52","fix":"Add explicit tests for inbound Range, If-None-Match, and If-Modified-Since and outbound Content-Length, Content-Range, Accept-Ranges, and ETag, including correct 206 and 304 behavior for attachment and wiki paths.","location":"P5 / §5.1 acceptance","prevention":"Enumerate transport-contract inputs and outputs in acceptance instead of collapsing them into a generic proxy assertion.","principle":"A byte-transparent proxy contract must verify request conditionals, response metadata, and partial/not-modified status semantics.","root_cause":"The implementation text requires range and conditional forwarding plus representation headers, while acceptance checks only generic bytes/status/type/disposition and a generic range case.","section_id":"5.1","severity":"blocking"},{"category":"weak-testability","check_key":"restore-preflight-zero-mutation","description":"A restore could modify database state or files_home before discovering an invalid archive member or collision and still satisfy the present tests.","finding_id":"F53","fix":"Add normal- and force-mode acceptance cases with a late invalid files archive member and a destination collision; assert refusal occurs before any datastore row or files_home path changes.","location":"P6 / §6.2 acceptance","prevention":"For multi-domain restore operations, test a failure discovered at the end of preflight and compare both domains byte-for-byte before and after.","principle":"Restore preflight is atomic only when every refusal condition is established before either persistence domain mutates.","root_cause":"Section 6.2 requires preflight-before-mutation in prose, but acceptance does not inject a late archive failure or collision and verify both datastore and filesystem remain unchanged.","section_id":"6.2","severity":"blocking"},{"category":"missing-requirement","check_key":"files-archive-global-preflight","description":"A syntactically valid archive can exhaust resources or contain ambiguous members such as duplicate normalized names and file/directory prefix conflicts, making restore order-dependent.","finding_id":"F54","fix":"Define shared preflight limits of 100,000 members and 100 GiB total declared uncompressed bytes, count every archive member, sum sizes with checked arithmetic, reject duplicate normalized names and file/directory prefix conflicts, and complete these checks plus free-space validation before mutation. Add boundary, overflow, duplicate, and prefix-conflict tests.","location":"P6 / §6.2 files archive preflight","prevention":"Specify archive-wide limits and graph invariants before extraction, then test exact boundaries and overflow paths.","principle":"Untrusted archive preflight requires deterministic global resource bounds and a conflict-free normalized member graph.","root_cause":"Section 6.2 checks members individually but defines no member-count bound, aggregate-size bound or counting semantics, checked summation, duplicate normalized-name rejection, or file/directory prefix-conflict rejection.","section_id":"6.2","severity":"blocking"},{"category":"missing-requirement","check_key":"remote-hub-auth-provisioning","description":"A correctly configured remote-mode installation can start but receive 401 responses for every owner-scoped API and streaming request.","finding_id":"F55","fix":"Add a remote install --hub-token-stdin input, write the credential to the existing owner-only local_cli_token path with mode 0600, perform an authenticated owner probe against hub_daemon_url before managed service startup, return a typed installation failure on rejection, and cover the flow in install tests and operator documentation.","location":"P1 / §1.1, P2 / §2.1, P5 / §5.1, and P6 / §6.3","prevention":"Treat endpoint and credential provisioning as one bootstrap contract and authenticate before installing managed startup.","principle":"Remote mode is installable only if the daemon receives a hub-accepted credential before its first authenticated request.","root_cause":"The plan provisions hub_daemon_url but never provisions the remote owner credential; the existing DaemonClient authorization path uses local token state that a remote hub will not accept.","section_id":"2.1","severity":"blocking"},{"category":"traceability","check_key":"datastore-bootstrap-writer-inventory","description":"expose_datastores can silently omit or lose files_home on successful staging or rollback even if the primary bootstrap tests pass.","finding_id":"F56","fix":"Add src/gobby/cli/datastores.py::expose_datastores and tests/cli/test_datastores_expose.py to §1.1 targets and require files_home preservation on both successful exposure and rollback.","location":"P1 / §1.1 targets","prevention":"Blast-radius bootstrap schema changes through all read, write, staging, and rollback consumers.","principle":"Every writer and rollback path for an extended bootstrap document must be in the implementation and test inventory.","root_cause":"src/gobby/cli/datastores.py::expose_datastores stages and rolls back bootstrap mappings through write_bootstrap_yaml, but §1.1 omits that consumer and its direct tests.","section_id":"1.1","severity":"blocking"},{"category":"weak-testability","check_key":"background-wiki-dispatch-test-seams","description":"Remote-mode watcher suppression or cron/prune routing can regress in lifecycle construction while endpoint-level proxy tests remain green.","finding_id":"F57","fix":"Add tests/wiki/test_watcher_lifecycle.py, tests/test_runner_lifecycle_subsystems.py, and tests/wiki/test_prune_job.py to §5.1 targets with direct remote-mode assertions for watcher construction, cron dispatch, prune dispatch, and shutdown.","location":"P5 / §5.1 targets","prevention":"Map every changed constructor and dispatcher to its nearest direct test before relying on higher-level integration coverage.","principle":"Mode-dependent background dispatch must be verified at each direct lifecycle seam that constructs or invokes it.","root_cause":"Section 5.1 changes watcher, cron, and prune dispatch in runner lifecycle code but targets only scheduled/integration coverage, omitting the existing direct lifecycle suites.","section_id":"5.1","severity":"blocking"},{"category":"traceability","check_key":"source-size-accounting","description":"The planned background-dispatch edits can push runner_lifecycle_subsystems.py toward or past the mandatory 1,000-line ceiling without triggering decomposition during planning.","finding_id":"F58","fix":"Record the current 857-line count and a concrete projected upper bound in §5.1; require decomposition within the implementation task if the projection or actual edit would reach 1,000 lines.","location":"P5 / §5.1 source-size accounting","prevention":"Run size accounting on every touched production target and record the projection whenever the configured warning threshold is crossed.","principle":"A plan touching a production file near the monolith ceiling must record current and projected size before implementation.","root_cause":"src/gobby/runner_lifecycle_subsystems.py is 857 lines, above the plan's 800-line accounting trigger, but §5.1 omits current and projected line counts.","section_id":"5.1","severity":"blocking"},{"category":"weak-testability","check_key":"daemon-client-direct-contract-tests","description":"URL construction, auth forwarding, header propagation, connect/inactivity/deadline handling, cancellation, or error parity can regress while proxy integration tests cover only selected paths.","finding_id":"F59","fix":"Add tests/utils/test_utils_daemon_client.py to §5.1 targets and directly test raw and streaming URL construction, authorization and header forwarding, connect/inactivity/deadline timeouts, cancellation cleanup, and non-2xx error parity.","location":"P5 / §5.1 targets","prevention":"Target the lowest reusable abstraction's contract suite whenever its public transport surface changes.","principle":"Reusable transport primitives require direct contract tests independent of the first endpoint that consumes them.","root_cause":"Section 5.1 adds async raw-response and streaming behavior to DaemonClient but omits its direct test module, leaving proxy tests to cover transport behavior indirectly.","section_id":"5.1","severity":"blocking"}],"reviewer_session":"1c190523-38de-4143-b53f-c39d43e2753e","round":4,"verdict":"needs_review"},"session_id":"e18533a2-3d7e-4fa4-bdc4-df7028759faf"}
```

**Round 5** `kind: verification`

- reviewer_run:  6e6d11bd-e793-46e1-9712-c1b13632ae56
- reviewer_session:  009f24f3-0522-4e57-9d5f-cc7fc14c68cb
- verdict: needs_review
- findings:
- F60-remote-owner-probe-contract / blocking / owner probe HTTP contract unspecified
- F61-migration-destination-graph / blocking / cross-class destination collisions untested
- F62-static-validation-blast-radius / blocking / V2 ruff/mypy omit most touched modules
- F63-deferred-obligation-task-contract / blocking / D1-D3 destination tasks lack deferred-from labels
- F64-remote-wiki-override-bypass / blocking / GOBBY_WIKI_HUB can open a remote local tree
- F65-daemon-singleton-maintenance-fence / blocking / stopped-daemon probe is check-then-act
- F66-upload-cancellation-owner-convergence / blocking / CancelledError leaves orphan bytes or rows
- F67-exdev-both-present-resume / blocking / equal both-present EXDEV pair has no rerun rule
- F68-force-collision-outcome / blocking / 6.2.10 contradicts --force overwrite
- F69-files-tree-special-entry-policy / blocking / symlink sources can pack then fail restore
- resolution_notes: Accepted F60-F64 and F66-F69. Accepted F65's start-after-probe race but declined a new files-home RW lock and declined requiring every gwiki CLI write to go through the daemon. Migrate/unpack/restore hold existing claim_pid_file. F63: added deferred-from:hub-owned-files-home:D1/D2/D3 and copied D1.1/D2.1/D3.1 onto #18779, #19664, and #17488. F62: V2 ruff/mypy now cover src/gobby/. Companion ledger must be re-pinned to the repaired plan hash before expansion.

```json plan-review-round
{"evidence_id":"92065682-0a15-43fb-93b6-4338318e0017","plan_hash":"1a66f824f26ff5b874635065084fd20c717ee63c2c6c2ef448449d029bf91363","round_number":5,"round_result":{"coverage_attestation":{"adjacent_variant_complete":true,"attestation_digest":"6799f1ccf35cdc0d0b246394e4aace77845bdc110bc4c0014fa74531399dab3d","cross_lane_interaction_complete":true,"disposition_counts":{"dismissed":2,"emitted_findings":9,"total":11},"evidence_id":"92065682-0a15-43fb-93b6-4338318e0017","lanes":[{"candidate_count":3,"lane_id":"requirements_traceability","status":"completed"},{"candidate_count":1,"lane_id":"repository_blast_radius","status":"completed"},{"candidate_count":7,"lane_id":"runtime_invariants","status":"completed"}],"shadow_manifest_status":{"entry_count":8,"manifest_digest":"c4b8e1898809e52f39999f5eede24e5e0e3f685454067313f25ca25b5933f688","status":"valid"},"source_digest":"b6033ca9912c37bb0d3f91dff0986604964b4b51acf7a9cca85a1a715e3c99e4","version":1},"findings":[{"category":"missing-requirement","check_key":"remote-owner-probe-contract","description":"Remote install cannot deterministically prove that hub_daemon_url terminates at a local files owner rather than another remote daemon. The plan names only a cheap probe and a remote-target error; it does not name the route, response schema, local-owner predicate, timeout, or a successful probe acceptance case.","finding_id":"F60","fix":"Use the existing authenticated GET /api/files/user-md surface with X-Gobby-Files-Proxy-Hop: 1 as the probe, require a 200 JSON response from a local owner within the stated timeout, and map auth, network, missing-root, and remote-target outcomes to typed install failures. Add positive success/startup and remote-target rejection/no-start acceptance cases.","location":"P2 / § 2.1 and P5 / § 5.1","prevention":"For every installation preflight, enumerate request, authentication, timeout, success response, negative responses, and the startup gate.","principle":"A pre-service ownership probe must define an exact authenticated request, response, success predicate, and failure mapping.","root_cause":"Sections 2.1 and 5.1 require an authenticated owner probe but leave its HTTP contract and positive success case unspecified.","section_id":"2.1","severity":"blocking"},{"category":"unhandled-edge","check_key":"migration-destination-graph","description":"Legacy sources can map onto the same destination tree: ~/wiki/topics/personal collides with the personal vault, and leftover personal/attachments can overlap project attachment publication. The current pairwise preflight does not define either collision or apply the shared reserved-topic guard to migrated topic names.","finding_id":"F61","fix":"Extend preflight to build the complete normalized source-to-destination graph, reject duplicate destinations and file/directory prefix conflicts, and validate legacy topic names through validate_topic_name before mutation. Add zero-mutation tests for a personal topic and overlapping personal/project attachment sources.","location":"P6 / § 6.1","prevention":"Before publication, normalize every planned destination and reject duplicates, ancestor/descendant conflicts, and reserved names across the whole migration graph.","principle":"A destructive migration must validate the complete normalized destination graph before its first mutation.","root_cause":"Section 6.1 preflights individual source/destination pairs without checking collisions and prefix overlaps across all seven source classes.","section_id":"6.1","severity":"blocking"},{"category":"weak-testability","check_key":"static-validation-blast-radius","description":"Most new and modified Python modules can ship without either lint or type checking, including bootstrap I/O, install preflight, owner dispatch, hub proxy routes, daemon transport, migration, backup files-home handling, attachment storage, wiki lifecycle, and compose projection.","finding_id":"F62","fix":"Replace the selective commands with uv run ruff check src/gobby/ and uv run mypy src/gobby/, or explicitly enumerate every touched production Python path in both commands.","location":"V2 End-to-end verification","prevention":"Diff the production target inventory against every static-check command before declaring verification complete.","principle":"The clean static-validation run must cover every touched hand-maintained production source.","root_cause":"V2 runs Ruff on eight Python files and Mypy on three while the deliverables change dozens of additional production modules.","section_id":"V2","severity":"blocking"},{"category":"gobby-format","check_key":"deferred-obligation-task-contract","description":"Plan-Coverage Contract rejection: malformed deferral. Tasks #18779, #19664, and #17488 lack deferred-from:hub-owned-files-home:D1, :D2, and :D3 respectively; their validation contracts also omit the transferred acceptance text, with #17488 having no validation criteria.","finding_id":"F63","fix":"Before resubmission, add each required deferred-from label and copy D1.1, D2.1, and D3.1 into the corresponding task validation criteria. Keep the plan deferrals untransferred until those task contracts are durable.","location":"D1-D3 deferrals","prevention":"For every deferral, read the destination task and verify its open status, deferred-from:<plan-id>:<section-id> label, and copied acceptance criterion before resubmission.","principle":"A deferred acceptance item is transferred only when the destination task durably carries its source label and validation criterion.","root_cause":"D1-D3 declare inherited obligations whose referenced tasks do not carry the required deferred-from labels or exact validation criteria.","section_id":"D1","severity":"blocking"},{"category":"unhandled-edge","check_key":"remote-wiki-override-bypass","description":"A remote daemon with either override set can resolve and mutate a node-local wiki even though the governing contract forbids every remote canonical copy. Acceptance 4.1.8 covers only remote mode without GOBBY_WIKI_HUB.","finding_id":"F64","fix":"When a present bootstrap says remote, refuse topic/personal filesystem resolution before evaluating environment or config overrides. Limit those overrides to local-owner or explicit missing-bootstrap fixture contexts, and add remote-with-environment and remote-with-config refusal tests plus an owner_dispatch-before-local-resolution test.","location":"P4 / § 4.1","prevention":"Test every filesystem override under both local and remote bootstrap modes, including the override-present remote branch.","principle":"Remote ownership mode must be decided before any override capable of opening a canonical local tree.","root_cause":"resolve_hub_path checks GOBBY_WIKI_HUB and wiki.hub_path/gwiki.hub_path before consulting FilesHomeView.","section_id":"4.1","severity":"blocking"},{"category":"unhandled-edge","check_key":"daemon-singleton-maintenance-fence","description":"Migration, unpack, restore, and backup snapshot windows have a check-then-act race with daemon startup. The architecture also says the hub daemon is the sole POSIX owner while §4.1 retains direct Rust mutations, so daemon-stopped state does not exclude all writers.","finding_id":"F65","fix":"Add the daemon singleton-lock helper to Targets and define a reusable maintenance claim held across discovery, preflight, publication, and restore/snapshot completion. Make direct gwiki mutations route through the owner daemon or honor the same claim. Add barriers proving concurrent gobby start and gwiki init cannot enter during migrate, unpack, backup, or restore.","location":"P4 / § 4.1 and P6 / §§ 6.1-6.2","prevention":"For stopped-daemon maintenance, hold the singleton claim from preflight through publication and inventory every alternate writer against the same claim.","principle":"A maintenance fence must be held continuously and observed by every writer it claims to exclude.","root_cause":"The plan treats a stopped-daemon probe as an exclusive fence, but another daemon can start after the probe and direct gwiki init/register_scope remains a POSIX writer outside that fence.","section_id":"6.1","severity":"blocking"},{"category":"unhandled-edge","check_key":"upload-cancellation-owner-convergence","description":"asyncio.CancelledError bypasses the current except Exception cleanup. Cancellation after target replace and before create_attachment can leave untracked bytes; cancellation while the database worker continues can instead leave a row after cleanup removes the file. Row-driven stale cleanup cannot reliably repair both states.","finding_id":"F66","fix":"Specify owner-side cancellation cleanup and a shielded publication/row-commit boundary: pre-commit cancellation removes temp and unpublished target bytes, while post-commit cancellation leaves a valid row/file state that normal cleanup can finish. Add barrier tests during streaming and after replace before row creation, asserting eventual row/byte convergence.","location":"P3 / § 3.1 and P5 / § 5.1","prevention":"Inject cancellation at body read, temp validation, target replace, and metadata commit; assert no orphan bytes, dangling row, or leaked upstream request.","principle":"Cancellation across a streamed upload must leave filesystem publication and metadata in one recoverable state.","root_cause":"The plan tests proxy-client cleanup while the owner upload's temp/published-file cleanup does not define CancelledError behavior or the replace-to-row-commit boundary.","section_id":"5.1","severity":"blocking"},{"category":"unhandled-edge","check_key":"exdev-both-present-resume","description":"On rerun, the plan says remaining sources publish only into absent destinations and that recognized partial destinations resume. It never says whether an equal both-present pair completes by deleting the source or whether a divergent pair refuses, so the promised resumability is incomplete.","finding_id":"F67","fix":"Add a preflight branch for both-present pairs: verify complete bytes and required metadata before mutation; treat equal pairs as published-but-not-retired and unlink the source only after full preflight, while differing pairs refuse with zero mutations. Add an injected crash immediately after destination publication and before source unlink.","location":"P6 / § 6.1","prevention":"Enumerate rerun states for source absent/present crossed with destination absent/equal/different before accepting resumability.","principle":"Every crash point in a copy-verify-delete migration needs a deterministic rerun state.","root_cause":"A crash after EXDEV destination publication and before source unlink leaves both paths present, but recognized-partial handling defines neither equality verification nor source retirement for that state.","section_id":"6.1","severity":"blocking"},{"category":"traceability","check_key":"force-collision-outcome","description":"A valid files_home collision cannot both proceed under --force and refuse under --force. The contradiction leaves unpack and hub-backup restore implementations and tests without a single required behavior.","finding_id":"F68","fix":"Split 6.2.10 into distinct cases: any invalid or late-invalid member always refuses before mutation in every mode; a valid destination collision aborts when confirmation is declined and proceeds only after complete preflight when confirmed or --force is supplied. Test all branches for unpack and hub-backup restore.","location":"P6 / § 6.2","prevention":"Cross-check each acceptance outcome against the behavioral contract for every named flag and confirmation branch.","principle":"Acceptance criteria for one input and mode must state one consistent outcome.","root_cause":"Section 6.2 preserves existing confirmation/--force overwrite behavior while acceptance 6.2.10 says a destination collision refuses in both confirm and --force modes.","section_id":"6.2","severity":"blocking"},{"category":"unhandled-edge","check_key":"files-tree-special-entry-policy","description":"A files_home containing a symlink can produce a successful pack or hub backup whose own restore fails as unsafe. The plan's round-trip and archive preflight criteria cover names, sizes, and collisions but define no source-entry type policy.","finding_id":"F69","fix":"Add _write_restricted_archive and the files-home backup helper to Targets, preflight files_home with lstat, and refuse symlinks, FIFOs, sockets, and device nodes before emitting an archive. Add no-output refusal and regular-file/directory round-trip tests for pack and hub-backup.","location":"P6 / § 6.2","prevention":"Preflight producer input types with lstat and round-trip each allowed filesystem entry class through every restore path.","principle":"A backup producer must not emit an archive that its paired restore path rejects.","root_cause":"Recursive tar creation preserves symlink and other special entry metadata while _safe_archive_target accepts only regular files and directories.","section_id":"6.2","severity":"blocking"}],"reviewer_session":"009f24f3-0522-4e57-9d5f-cc7fc14c68cb","round":5,"verdict":"needs_review"},"session_id":"e18533a2-3d7e-4fa4-bdc4-df7028759faf"}
```

**Round 6** `kind: verification`

- reviewer_run:  77cd4ff6-ec77-4e52-a11d-bb41bcd0766f
- reviewer_session:  cfc3a8e9-b72f-477a-83fc-db53ab9fa741
- verdict: needs_review
- findings:
- F70-acceptance-forward-dependency / blocking / 2.1/4.1 accepted 5.1 and 6.1 outputs
- F71-legacy-bootstrap-upgrade-entry / blocking / present legacy bootstrap cannot be loaded to upgrade
- F72-governing-contract-reconciliation / blocking / architecture still names daemon_url and root create
- F73-present-bootstrap-fixture-inventory / blocking / install/runtime/daemon/e2e fixtures omitted
- F74-pack-singleton-maintenance-fence / blocking / pack omitted from claim_pid_file campaigns
- F75-archive-hardlink-producer-consumer-parity / blocking / st_nlink>1 can emit unrestorable members
- F76-remote-profile-404-contract / blocking / 404 mapped to empty profile
- F77-portable-files-home-bootstrap-merge / blocking / unpack can import source-host files_home
- F78-multipart-proxy-backpressure-boundary / blocking / UploadFile may spool before proxy stream
- F79-migration-vault-metadata-resume / blocking / scope metadata rewrite is not independently resumable
- resolution_notes: Accepted F70-F79. F70 moved probe/dispatch/migrate-vault acceptance onto producing leaves. F71 is install --files-home pre-load upgrade of a legacy present bootstrap. F73 added CLI install/runtime/daemon-coverage fixtures to Targets and V2 without running e2e. F74 pack holds claim_pid_file. F75 refuses st_nlink>1. F76 treats only 200 JSON content as a successful profile read. F77 keeps destination files_home across bootstrap merge. F78 forwards Request.stream before multipart binding. F79 makes vault metadata rewrite an independent post-preflight step. Companion ledger must be re-pinned to the repaired plan hash before expansion.

```json plan-review-round
{"evidence_id":"0ca3390f-d210-4082-9b38-dd9c62aacc03","plan_hash":"9151f66d39d99970ddc6043c866fa148598f6f7e4a57dceb3ca7b949d601d04f","round_number":6,"round_result":{"coverage_attestation":{"adjacent_variant_complete":true,"attestation_digest":"4a1e2ea8fa5e4b97301c1e4c469f13c1764c10fa6cda709da2585759b5e87d78","cross_lane_interaction_complete":true,"disposition_counts":{"dismissed":3,"emitted_findings":10,"total":13},"evidence_id":"0ca3390f-d210-4082-9b38-dd9c62aacc03","lanes":[{"candidate_count":3,"lane_id":"requirements_traceability","status":"completed"},{"candidate_count":3,"lane_id":"repository_blast_radius","status":"completed"},{"candidate_count":7,"lane_id":"runtime_invariants","status":"completed"}],"shadow_manifest_status":{"entry_count":8,"manifest_digest":"d6de2e39d17b53310b00daae596b65a89ce3bbe1273c1085c4d487967d70666d","status":"valid"},"source_digest":"c2cd0e201a1b32ec3772c90b46867174f342032945dcd63011eb50595f971a67","version":1},"findings":[{"category":"bad-sequencing","check_key":"acceptance-forward-dependency","description":"§2.1 accepts the local-owner GET route introduced by §5.1, while §4.1 accepts owner_dispatch from §5.1 and migrate/rerun behavior from §6.1. Those leaves cannot pass their own criteria when executed in declared order.","finding_id":"F70","fix":"Move 2.1.9-2.1.10 to §5.1 or introduce the minimal owner route before §2.1; move 4.1.13 to §5.1 and the migrate portion of 4.1.12 to §6.1, leaving only producer-local assertions in §4.1. Update the coverage ledger mappings.","location":"P2/P4/P5/P6 / §§ 2.1, 4.1, 5.1, 6.1","prevention":"For every acceptance item, locate its producer and verify that producer is the same section or a resolved predecessor.","principle":"A deliverable's acceptance may rely only on behavior it produces or on declared predecessor outputs.","root_cause":"Acceptance items were attached to consumers that precede their producing sections in the manifest DAG.","section_id":"2.1","severity":"blocking"},{"category":"missing-requirement","check_key":"legacy-bootstrap-upgrade-entry","description":"Existing installs have a present local bootstrap without files_home. §1.1 makes that a hard load error, current install loads bootstrap before ensure_daemon_config, and the existing-file branch returns without rewriting it; migrate then cannot reach require_files_home successfully.","finding_id":"F71","fix":"Define install --files-home as a pre-load upgrade path: lock and raw-read the existing local mapping, preserve other keys, inject the validated existing absolute root, validate the completed mapping, and durably replace it before identity, database, service, or migrate work. Add a legacy-bootstrap upgrade test that then enters gobby files migrate.","location":"P1/P2/P6 / §§ 1.1, 2.1, 6.1","prevention":"Start upgrade tests from the prior released configuration and prove the migration entry command can cross every newly strict validation gate.","principle":"A strict configuration gate must leave an executable transition from every legacy state the migration claims to support.","root_cause":"The plan makes a legacy present local bootstrap invalid before defining how install can upgrade that file without first loading it.","section_id":"1.1","severity":"blocking"},{"category":"traceability","check_key":"governing-contract-reconciliation","description":"docs/architecture/hub-owned-files-home.md still names daemon_url as the remote owner URL and describes migration as creating the hub-files tree, while this plan requires hub_daemon_url and an operator-provisioned root that writers never create.","finding_id":"F72","fix":"Expand §6.3 and its acceptance to reconcile the architecture document with hub_daemon_url, existing-root/no-root-create installation and migration, and the finalized maintenance/migration contract.","location":"P6 / § 6.3","prevention":"Diff every governing contract statement against final plan decisions and add acceptance for each reconciled conflict.","principle":"A document named as the governing contract must agree with the implementation contract it governs.","root_cause":"§6.3 limits architecture edits to path examples while settled plan decisions changed bootstrap ownership and root-creation semantics.","section_id":"6.3","severity":"blocking"},{"category":"weak-testability","check_key":"present-bootstrap-fixture-inventory","description":"tests/cli/test_cli_install.py, tests/cli/test_runtime.py, tests/cli/test_daemon_coverage.py, tests/e2e/conftest.py, and tests/e2e/test_single_active_daemon.py construct or consume present local bootstraps without files_home and are absent from Targets and V2.","finding_id":"F73","fix":"Add those paths to §1.1/§2.1 Targets, update their present local fixtures with an existing absolute files_home, and include focused validation for the direct ensure_daemon_config/runtime consumers without running the full suite.","location":"P1/P2 / §§ 1.1-2.1","prevention":"Use caller and fixture inventory to enumerate every present bootstrap constructor, then include each affected path in Targets and focused validation.","principle":"A new required configuration field must cover every present-file fixture and direct consumer in focused validation.","root_cause":"The target inventory sampled bootstrap suites but omitted verified install, runtime, daemon-status, and e2e fixtures that construct legacy local mappings.","section_id":"1.1","severity":"blocking"},{"category":"unhandled-edge","check_key":"pack-singleton-maintenance-fence","description":"gobby pack snapshots files_home but is not explicitly included among claim_pid_file campaigns. Another daemon can start after pack stops/probes and mutate files during archive emission.","finding_id":"F74","fix":"Require pack to acquire claim_pid_file after any stop, hold it through files_home discovery and final archive publication, and release it before intentional restart. Add a tests/cli/test_pack.py barrier proving concurrent gobby start cannot enter.","location":"P6 / § 6.2","prevention":"Inventory every files_home reader or writer used for maintenance and barrier-test concurrent daemon start across its whole campaign.","principle":"A stopped-daemon snapshot must hold the daemon singleton claim continuously through final publication.","root_cause":"The accepted maintenance fence was applied to migrate, unpack, hub-backup snapshot, and restore while gobby pack retained a stop/probe followed by an unclaimed archive window.","section_id":"6.2","severity":"blocking"},{"category":"unhandled-edge","check_key":"archive-hardlink-producer-consumer-parity","description":"Two hardlinked regular files can pass producer preflight and yield a hardlink TarInfo that unpack/restore refuses, so a valid source can produce an unusable archive.","finding_id":"F75","fix":"Add st_nlink > 1 to the pre-archive refusal policy for both pack and hub-backup before any output is published. Add two-hardlink producer tests and assert no archive is emitted.","location":"P6 / § 6.2","prevention":"Cross every accepted source filesystem type with emitted TarInfo type and restore acceptance.","principle":"An archive producer must never emit a member type its paired restore rejects.","root_cause":"Source lstat treats multiply linked files as regular, while tar creation can encode later inode references as hardlink members and restore accepts only regular-file and directory members.","section_id":"6.2","severity":"blocking"},{"category":"unhandled-edge","check_key":"remote-profile-404-contract","description":"Treating 404 as an empty profile silently hides a wrong hub URL, an old hub without the route, or a non-owner target and violates the typed remote-owner failure boundary.","finding_id":"F76","fix":"Make only a validated 200 JSON body with string content, including content=\"\", a successful profile read. Make 404, malformed JSON/schema, remote-target responses, and all other non-2xx statuses typed failures with no local fallback; add regressions.","location":"P5 / § 5.1","prevention":"Enumerate success-empty, route-missing, malformed-body, auth, timeout, and remote-target responses separately for every proxy endpoint.","principle":"A missing data item and a missing protocol route require distinct outcomes.","root_cause":"The plan maps HTTP 404 to empty profile even though the owner endpoint represents absent USER.md as HTTP 200 with an empty content value.","section_id":"5.1","severity":"blocking"},{"category":"unhandled-edge","check_key":"portable-files-home-bootstrap-merge","description":"Unpack can publish files under the destination files_home and then overwrite bootstrap with the archived source path, leaving the daemon pointed at a missing or wrong tree. Without a destination bootstrap, unpack has no destination-owned root.","finding_id":"F77","fix":"Require files-member unpack to begin with a present destination-local bootstrap naming an existing absolute files_home. Capture it before mutation, restore files there, merge archived bootstrap data while preserving that destination files_home, and test different source and destination roots.","location":"P1/P6 / §§ 1.1 and 6.2","prevention":"For every archived absolute locator, test restore between hosts with different paths and verify the final configuration points at the published destination.","principle":"Portable restore must preserve destination-owned absolute paths rather than importing source-host locators.","root_cause":"Pack archives bootstrap.yaml with the source machine's absolute files_home while unpack restores files through the destination's configured root.","section_id":"6.2","severity":"blocking"},{"category":"unhandled-edge","check_key":"multipart-proxy-backpressure-boundary","description":"Streaming from UploadFile to DaemonClient does not meet the plan's end-to-end upload-backpressure requirement because the remote node may already have consumed and spooled the full multipart body.","finding_id":"F78","fix":"Specify raw Request.stream forwarding on remote attachment and wiki file-bearing branches before multipart binding, preserving the original multipart Content-Type; only the owner parses UploadFile data. Add incremental-body, node-temp-spool, maximum-size, cancellation, and slow-owner tests.","location":"P5 / § 5.1","prevention":"Barrier-test request-body consumption at the framework ingress, proxy client, and owner parser rather than only testing downstream chunk reads.","principle":"End-to-end backpressure must begin before the first buffering or spooling layer.","root_cause":"Existing FastAPI UploadFile binding parses and may spool multipart data before the remote proxy handler can stream it to the owner.","section_id":"5.1","severity":"blocking"},{"category":"unhandled-edge","check_key":"migration-vault-metadata-resume","description":"A crash after a personal or topic vault is published and its source retired but before scope metadata rewrite can leave a stale root; the rerun may see only a complete destination and skip normalization.","finding_id":"F79","fix":"Make each personal/topic scope-metadata rewrite an independent post-preflight normalization whenever the destination exists, including source-absent and equal-both-present states. Add crash injection after source retirement and prove rerun repairs the root.","location":"P6 / § 6.1","prevention":"Inject a crash between every source retirement and follow-on metadata rewrite, then require a source-absent rerun to converge.","principle":"Every crash point in a publish-then-normalize migration needs a deterministic rerun state.","root_cause":"Attachment locator rewrite is an explicit resumable step, while personal/topic scope metadata rewrite is described only immediately after publication.","section_id":"6.1","severity":"blocking"}],"reviewer_session":"cfc3a8e9-b72f-477a-83fc-db53ab9fa741","round":6,"verdict":"needs_review"},"session_id":"e18533a2-3d7e-4fa4-bdc4-df7028759faf"}
```

**Round 7** `kind: verification`

- reviewer_run: d7130f61-523d-44ed-92ec-238e6a64d674
- reviewer_session: 30428465-7799-4727-9dda-30ba15eed37e
- verdict: needs_review
- findings:
- F80-legacy-upgrade-leaf-ownership / blocking / 1.1.16 was install front-door work owned by 2.1
- F81-maintenance-fence-coverage-matrix / blocking / unpack/snapshot/restore lacked pid-lock acceptance
- F82-attachment-locator-representation / blocking / 3.1 left local_path either/or
- F83-complete-writer-exclusion-model / blocking / gwiki can race maintenance campaigns
- F84-postgres-installer-bootstrap-preflight / blocking / compose work can run before files_home validation
- F85-remote-sync-sessions-source-transfer / blocking / remote sync-sessions forwards a node path
- F86-start-probe-before-side-effects / blocking / start mutates services before the lock probe
- F87-maintenance-claim-owner-role / blocking / stop can SIGTERM a maintenance PID
- F88-verified-root-rust-mutation-stack / blocking / vault/obsidian create_dir_all can recreate the root
- F89-producer-archive-bounds-parity / blocking / pack can emit archives restore will refuse
- F90-restore-streaming-memory-bound / blocking / non-files members still read() whole payloads
- F91-user-md-bounded-ingress / blocking / USER.md PUT has no byte ceiling
- F92-present-bootstrap-fixture-inventory / blocking / three more present-file fixtures omit files_home
- F93-personal-scope-resolution-target / blocking / resolve_project_from_root always checkout-adjacent
- F94-registry-fixture-target-parity / blocking / overwrite fixture still asserts topics/ paths
- resolution_notes: Accepted F80-F82, F84-F89, F91-F94. Accepted F83 slim: gwiki init/register_scope refuse while claim role is maintenance; declined daemon-routed gwiki writes and a new RW lock. Accepted F90 slim: files_home declared-versus-actual streaming; declined rewriting historical project-gobby/$GOBBY_HOME/docker/postgres extract branches. 1.1.16 is the helper; 2.1.10 stays the install front door. 3.1 persists `_personal/attachments/<project>/<id[:2]>/<id>/<filename>`. 5.1 streams sync-sessions archives and caps USER.md at 1,048,576 bytes. 6.2 adds unpack/snapshot/restore fence items, start-before-side-effects, claimant role, producer 100k/100GiB bounds, and declared-versus-actual checks.

```json plan-review-round
{"evidence_id":"026138f7-7a01-4a68-bc1e-54b883c82b66","plan_hash":"4c696bee3123caa8489df215e32a2d4ffa5c8e4982e1561407646759a599eb7a","round_number":7,"round_result":{"coverage_attestation":{"adjacent_variant_complete":true,"attestation_digest":"4c0682fe56428863b0132b98030d6556f4d877b8a013a5f701e15306fb798b84","cross_lane_interaction_complete":true,"disposition_counts":{"dismissed":3,"emitted_findings":15,"total":18},"evidence_id":"026138f7-7a01-4a68-bc1e-54b883c82b66","lanes":[{"candidate_count":4,"lane_id":"requirements_traceability","status":"completed"},{"candidate_count":7,"lane_id":"repository_blast_radius","status":"completed"},{"candidate_count":7,"lane_id":"runtime_invariants","status":"completed"}],"shadow_manifest_status":{"entry_count":8,"manifest_digest":"84a08a38b49fc887bf31e6d8f94bee8f8b385ff674a483fe2d8e59281395aade","status":"valid"},"source_digest":"d497b66ec8b1c85a09d377cb1d7bbd6773402fd5081ca650f2e8f319d09d9061","version":1},"findings":[{"category":"bad-sequencing","causal_finding_id":"F71","causal_section_ids":["1.1","2.1"],"check_key":"legacy-upgrade-leaf-ownership","description":"Acceptance 1.1.16 requires front-door install ordering that 1.1 cannot implement from its targets, duplicates 2.1.10, and leaves direct helper/callback tests outside the owning target set.","finding_id":"F80-legacy-upgrade-leaf-ownership","fix":"Keep raw read/inject/validate/durable replacement helper behavior in 1.1. Move the sole end-to-end `install --files-home` ordering criterion to 2.1, and add the direct `ensure_daemon_config`, Click callback, prompt, and public-install test targets there. Update the coverage ledger.","introduced_in_round":6,"location":"P1/P2 / §§ 1.1 and 2.1","prevention":"For every new acceptance item, resolve each producing symbol and test target, then verify they are owned by that leaf or a predecessor.","principle":"A deliverable may accept only behavior produced by its own targets or declared predecessor outputs.","root_cause":"Round 6 added the end-to-end `install --files-home` legacy upgrade to predecessor 1.1 even though the install front door and orchestration tests remain owned by successor 2.1.","section_id":"1.1","severity":"blocking"},{"category":"weak-testability","check_key":"maintenance-fence-coverage-matrix","description":"Unpack, hub-backup snapshot, and hub-backup restore may omit `claim_pid_file` while satisfying every listed acceptance item.","finding_id":"F81-maintenance-fence-coverage-matrix","fix":"Add separate 6.2 acceptance items and V2/ledger entries proving a concurrent `gobby start` cannot enter during unpack, hub-backup snapshot, or hub-backup restore, from preflight through final publication.","location":"P6 / § 6.2","prevention":"Build a command-by-command fence matrix and require a barrier test for every reader or writer campaign.","principle":"Every maintenance campaign that claims an exclusive fence needs its own observable concurrency acceptance.","root_cause":"The body assigns the singleton claim to four section-6.2 campaigns, while acceptance and V2 exercise only pack.","section_id":"6.2","severity":"blocking"},{"category":"missing-requirement","check_key":"attachment-locator-representation","description":"The plan does not choose whether new attachment rows store a files_home-relative `local_path` or remove/reconstruct that field, so new and migrated rows can use incompatible identities and existing deletion tests can retain the retired absolute-path contract.","finding_id":"F82-attachment-locator-representation","fix":"Specify the exact files_home-relative locator stored by both new uploads and migration, or explicitly target schema/caller changes that remove or nullable-mark `local_path`. Add 3.1 acceptance proving no new absolute path is persisted and retarget `tests/servers/routes/test_chat_routes.py` to deterministic resolver paths and unlink-before-row-delete behavior.","location":"P3/P5/P6 / §§ 3.1, 5.1, and 6.1","prevention":"Trace every persisted locator through create, read, delete, migration, and existing test seams before accepting a storage-path redesign.","principle":"One canonical durable locator representation must govern new writes, migrated rows, resolvers, and deletion tests.","root_cause":"Section 3.1 leaves persistence as an either/or choice even though the existing schema requires `local_path`, section 6.1 mandates relative migrated locators, and message-delete tests still encode arbitrary absolute paths.","section_id":"3.1","severity":"blocking"},{"category":"unhandled-edge","check_key":"complete-writer-exclusion-model","description":"Standalone `gwiki init` and registry/vault mutations can race migrate, pack, unpack, snapshot, or restore, invalidating preflight or producing an inconsistent archive despite the stopped-daemon claim.","finding_id":"F83-complete-writer-exclusion-model","fix":"Define one verifiable writer model in Constraints and 6.3. Route hub-mode gwiki mutations through the owner daemon or expose the maintenance claim to Rust and make every direct mutator refuse while it is held. Add init and registry-mutation barriers for all maintenance campaigns.","location":"P4/P6 / §§ 4.1, 6.1, 6.2, and 6.3","prevention":"Inventory all cross-language tree readers and writers and prove each shares the same owner boundary or lock protocol.","principle":"An exclusive maintenance claim must exclude every process that can read or mutate the protected tree.","root_cause":"The plan calls the daemon the sole POSIX owner while retaining direct Rust gwiki writers and separate maintenance CLIs; those Rust commands neither route through the daemon nor observe the Python singleton claim.","section_id":"6.2","severity":"blocking"},{"category":"bad-sequencing","check_key":"postgres-installer-bootstrap-preflight","description":"The plan targets the PostgreSQL bootstrap helper and one fixture while omitting its production installer caller, so standalone Docker install can mutate assets or services before strict files_home validation fails.","finding_id":"F84-postgres-installer-bootstrap-preflight","fix":"Target `src/gobby/cli/installers/postgres.py` and its installer test. Require a present valid local bootstrap and refuse before compose/assets/datastore work with a typed `gobby install --files-home` instruction, or supply and persist an explicit existing files_home before those effects.","location":"P1/P6 / §§ 1.1 and 6.2","prevention":"Enumerate every direct caller of a tightened writer and place a failure-before-side-effects acceptance at each entry point.","principle":"A writer subject to a new strict configuration invariant must validate its required inputs before irreversible or externally visible side effects.","root_cause":"The standalone PostgreSQL installer can start compose/setup work from a missing bootstrap and only later call `write_postgres_defaults`, which cannot create a valid local mapping without files_home.","section_id":"1.1","severity":"blocking"},{"category":"unhandled-edge","check_key":"remote-sync-sessions-source-transfer","description":"Remote `sync-sessions` can send a node-local `archive_dir` that the hub cannot interpret, or omit it and silently sync the hub's own archives instead of the node's.","finding_id":"F85-remote-sync-sessions-source-transfer","fix":"Define a bounded streamed archive-upload contract for `sync-sessions`, covering explicit `archive_dir` and the scheduled default. Target `wiki_sync_sessions` and add remote tests proving hub ingestion uses transferred node bytes and never interprets a node path.","location":"P5 / § 5.1","prevention":"For every remote command argument, classify scalar versus file-bearing inputs and test explicit and default source variants across machines.","principle":"Every file-bearing remote operation must serialize source bytes across the owner boundary instead of forwarding a machine-local path.","root_cause":"The generic command inventory routes `sync-sessions` to the hub, while the only defined file transport covers attach/ingest and scheduled sync defaults to the node archive tree.","section_id":"5.1","severity":"blocking"},{"category":"bad-sequencing","check_key":"start-probe-before-side-effects","description":"A concurrent start can revive or mutate datastores during migrate, pack, unpack, backup, or restore even though the eventual daemon launch refuses.","finding_id":"F86-start-probe-before-side-effects","fix":"Target the start front door and move the singleton probe ahead of managed-service startup, runtime/DB access, and `service_start`. Add barriers for every maintenance campaign asserting those operations never execute while the claim is held.","location":"P6 / §§ 6.1, 6.2, and 6.3","prevention":"For every exclusive-start gate, enumerate all calls before acquisition/probe and barrier-test that none run during maintenance.","principle":"A rejected singleton entrant must perform zero protected-service or datastore work before observing the exclusive claim.","root_cause":"Current `gobby start` starts managed services, loads DB-backed runtime configuration, and may invoke the OS service manager before probing the PID lock.","section_id":"6.2","severity":"blocking"},{"category":"unhandled-edge","check_key":"maintenance-claim-owner-role","description":"A concurrent `gobby stop` or restart can kill an in-progress migrate, pack, unpack, backup, or restore campaign.","finding_id":"F87-maintenance-claim-owner-role","fix":"Add an explicit claimant role to the singleton record. Maintenance must hold exclusion without masquerading as the daemon; stop/restart must detect the role and refuse. Add stop-during-campaign barriers for all maintenance commands.","location":"P6 / §§ 6.1, 6.2, and 6.3","prevention":"Cross every lock claimant role with start, stop, restart, stale-lock, and release behavior.","principle":"A shared lock file must distinguish daemon ownership from maintenance ownership before control commands act on its PID.","root_cause":"`claim_pid_file` publishes the maintenance process PID as `gobby.pid`, while `stop_daemon` treats a matching Gobby command line as the daemon and sends termination signals.","section_id":"6.2","severity":"blocking"},{"category":"unhandled-edge","check_key":"verified-root-rust-mutation-stack","description":"The Rust init path can recreate missing files_home ancestry or write through a replaced root between scope resolution and vault/Obsidian writes.","finding_id":"F88-verified-root-rust-mutation-stack","fix":"Add `crates/gwiki/src/vault.rs` and `crates/gwiki/src/obsidian.rs` targets. Pass a verified owner-root handle or identity through all mutations, create descendants relative to it without recursive root creation, reject symlink/non-directory ancestors, and barrier-test removal/replacement at each write.","location":"P4 / § 4.1","prevention":"Trace root identity through the complete write stack and race removal/replacement before every descendant creation and publication.","principle":"A no-root-create guarantee must remain true through every descendant writer after root identity is verified.","root_cause":"`gwiki init` delegates to untargeted vault and Obsidian helpers that recursively create directories, so a caller-level stat cannot prevent root recreation after removal or replacement.","section_id":"4.1","severity":"blocking"},{"category":"unhandled-edge","check_key":"producer-archive-bounds-parity","description":"Pack or backup can successfully publish an archive that unpack or restore deterministically rejects.","finding_id":"F89-producer-archive-bounds-parity","fix":"Prewalk producer inputs into the normalized member graph, checked-sum regular-file sizes, and enforce the same 100,000-member and 100-GiB limits before output creation in both pack and files-home hub-backup. Add exact-boundary and no-output tests.","location":"P6 / § 6.2","prevention":"Compare producer preflight and restore acceptance field-by-field, including types, paths, link counts, member counts, and sizes.","principle":"An archive producer must enforce every structural bound its paired restore requires.","root_cause":"Restore preflight enforces 100,000 members and 100 GiB, while pack and files-home backup producers check entry types without applying the same count and aggregate-size bounds.","section_id":"6.2","severity":"blocking"},{"category":"unhandled-edge","check_key":"restore-streaming-memory-bound","description":"A valid archive can contain a near-100-GiB non-files member that passes aggregate preflight and exhausts process memory during restore.","finding_id":"F90-restore-streaming-memory-bound","fix":"Require fixed-size streaming copies for every regular and nested-archive member, add per-artifact member ceilings, verify actual bytes equal declared TarInfo sizes, and use temporary-file publication where authoritative. Add large-member tests without allocating full payloads.","location":"P6 / § 6.2","prevention":"Inventory every extraction branch and prove fixed-size transfer, declared-versus-actual byte validation, and publication behavior at its maximum accepted size.","principle":"Every accepted archive member must be processed within an explicit memory bound independent of aggregate archive limits.","root_cause":"The plan streams files_home members while existing project-gobby, ordinary gobby, nested Docker-volume, and PostgreSQL branches still call `read()` on complete members.","section_id":"6.2","severity":"blocking"},{"category":"unhandled-edge","check_key":"user-md-bounded-ingress","description":"An authenticated oversized USER.md request can consume unbounded memory and disk before publication.","finding_id":"F91-user-md-bounded-ingress","fix":"Define a USER.md byte ceiling, reject oversized Content-Length before body parsing, enforce decoded UTF-8 bytes while reading, and publish only after limit and schema checks. Add direct-owner and proxied over-limit tests proving no temp or destination change.","location":"P5 / § 5.1","prevention":"For every new request body, specify encoded and decoded size limits and test over-limit behavior before parsing, buffering, and publication.","principle":"Every remote file-write endpoint needs a byte ceiling enforced before complete buffering or destination mutation.","root_cause":"PUT `/api/files/user-md` accepts an unrestricted JSON string and feeds a bytes-based durable replacement, unlike the bounded streaming attachment path.","section_id":"5.1","severity":"blocking"},{"category":"traceability","check_key":"present-bootstrap-fixture-inventory-round7","description":"Unlisted tests pass invalid present local bootstraps through `load_bootstrap`, `resolve_compose_runtime`, root CLI callbacks, or `run_gobby`, so implementation will fail outside the named validation inventory.","finding_id":"F92-present-bootstrap-fixture-inventory","fix":"Add `tests/cli/installers/test_postgres_installer.py`, `tests/storage/hub/test_postgres_pool_atexit.py`, and `tests/e2e/test_runtime_boundary.py` to the owning Targets and ledger/V2 inventory. Provision an existing temporary files_home in each true local present-file fixture while preserving missing-file default cases.","location":"P1/P6 / §§ 1.1 and 6.2","prevention":"Search every bootstrap mapping writer and classify present-file versus missing-file defaults before finalizing strict-field targets.","principle":"A new hard configuration requirement must cover every reachable fixture that writes and loads a present mapping.","root_cause":"Round 6 expanded the fixture inventory selectively; additional PostgreSQL installer, pool-atexit, and runtime-boundary fixtures still create local mappings without files_home.","section_id":"1.1","severity":"blocking"},{"category":"traceability","check_key":"personal-scope-resolution-target","description":"The exact target set cannot implement explicit personal scope as `<files_home>/wiki/personal`: `--topic personal` is reserved and the project path reaches an untargeted resolver with ordinary checkout semantics.","finding_id":"F93-personal-scope-resolution-target","fix":"Add `crates/gwiki/src/scope.rs::resolve_project_from_root` and a personal-scope test target. Special-case `PERSONAL_PROJECT_ID` only after verifying the project root is the configured `<files_home>/_personal`, return `<files_home>/wiki/personal` with the wiki-home registry, and retain ordinary project resolution.","location":"P4 / § 4.1","prevention":"Trace each scope kind from Python arguments through Rust selection and resolution, then target the first symbol that must branch.","principle":"Every required scope-selection branch must have an exact implementation target and a positive control.","root_cause":"The planned gateway exposes personal selection through project identity, but targeted Rust symbols exclude `resolve_project_from_root`, which currently always chooses a checkout-adjacent vault.","section_id":"4.1","severity":"blocking"},{"category":"traceability","check_key":"registry-fixture-target-parity","description":"The plan requires relative direct-child registry fixtures but the exact target set excludes the existing test that must change.","finding_id":"F94-registry-fixture-target-parity","fix":"Add `crates/gwiki/src/registry.rs::register_overwrites_existing_entries` or broaden `registry.rs` to `::*` with a scope reason. Assert direct-child relative paths and escape rejection.","location":"P4 / § 4.1","prevention":"Resolve in-file tests and fixtures as changed symbols during target coverage, even when production code shares the same source file.","principle":"When a deliverable explicitly requires changing an in-file fixture, its exact Targets must include that fixture symbol or justified file scope.","root_cause":"Section 4.1 targets only `register_scope`, while `register_overwrites_existing_entries` still constructs and asserts the retired absolute nested path.","section_id":"4.1","severity":"blocking"}],"reviewer_session":"30428465-7799-4727-9dda-30ba15eed37e","round":7,"verdict":"needs_review"},"session_id":"e18533a2-3d7e-4fa4-bdc4-df7028759faf"}
```

**Round 8** `kind: verification`

- reviewer_run: 36dda21b-8b68-44d1-81f8-21ff497b85cc
- reviewer_session: c4efdd02-5f28-4dbe-95b7-7bcb3ee615d5
- verdict: needs_review
- findings:
- F95-archive-exact-boundary-contract / blocking / exact 100k/100GiB also left no archive
- F96-operator-files-home-provisioning-docs / blocking / 6.3 omitted --files-home provisioning
- F97-config-open-coverage-seam / blocking / test_utils_coverage.py always called ensure_personal_project
- F98-runner-probe-role-consumers / blocking / runner.py treated every holder as a daemon PID
- F99-attachment-project-identity-propagation / blocking / GET/DELETE carried attachment_id only
- F100-bootstrap-writer-lock-domain / blocking / write_bootstrap_yaml published unlocked
- F101-verified-root-python-capability / blocking / re-stat then pathname write is TOCTOU
- F102-user-md-wire-content-bounds / blocking / Content-Length used the decoded ceiling
- F103-files-home-legacy-source-overlap / blocking / files_home could equal personal or contain sources
- F104-attachment-delete-claim-recovery / blocking / deleting claim had no expiry or reclaim
- F105-pack-output-source-overlap / blocking / pack output could sit inside files_home
- resolution_notes: Accepted F95-F98, F100, F102-F105. Slimmed F99: HTTP GET/DELETE stay attachment_id-only and load project_id from the row; declined new route parameters and nonexistent preparation/observe modules. Slimmed F101: extend require_files_home to hold a directory fd for no-follow descendant ops; declined a separately named capability type. Four adversary-dismissed candidates were not named in the typed result payload. Companion ledger must be re-pinned to the repaired plan hash before expansion.

```json plan-review-round
{"evidence_id":"72b14efb-08c6-42f5-ada0-650cb4df6e95","plan_hash":"a7824baf25fbfd8888e617d79df20812bdb9e8e2db5d335c256a488612332418","round_number":8,"round_result":{"coverage_attestation":{"adjacent_variant_complete":true,"attestation_digest":"2a9d93624d5e5439e82ba6a2d7b21da9d50be9b0cd65e164f4d80d793af7c29e","cross_lane_interaction_complete":true,"disposition_counts":{"dismissed":4,"emitted_findings":11,"total":15},"evidence_id":"72b14efb-08c6-42f5-ada0-650cb4df6e95","lanes":[{"candidate_count":3,"lane_id":"requirements_traceability","status":"completed"},{"candidate_count":3,"lane_id":"repository_blast_radius","status":"completed"},{"candidate_count":9,"lane_id":"runtime_invariants","status":"completed"}],"shadow_manifest_status":{"entry_count":8,"manifest_digest":"c2c96aaf54e5da52ee268f82b8824f2532321efc2837d7e54a7a0800036dbd59","status":"valid"},"source_digest":"8eef8bacbbac7ddf42ba204a4b0d68621ec984737122b46a026b4baea0e45652","version":1},"findings":[{"category":"weak-testability","causal_finding_id":"F89-producer-archive-bounds-parity","causal_section_ids":["6.2"],"check_key":"archive-exact-boundary-contract","description":"Section 6.2 allows at most 100,000 members and 100 GiB and refuses only larger inputs, yet its producer prose and acceptance 6.2.21 say exact-boundary cases leave no archive. Implementers cannot satisfy both contracts, and producer/restore parity can diverge at the limit.","finding_id":"F95-archive-exact-boundary-contract","fix":"State that exactly 100,000 members and exactly 100 GiB are accepted and round-trip. State that 100,001 members and 100 GiB plus one byte refuse before output creation and leave no archive. Split producer and restore assertions accordingly.","introduced_in_round":7,"location":"P6 / § 6.2","prevention":"For every resource ceiling, list max-minus-one, exact-max, and max-plus-one outcomes for both producer and consumer.","principle":"A numeric maximum needs one consistent inclusive or exclusive boundary across producer, consumer, and acceptance tests.","root_cause":"The Round 7 F89 repair collapsed separate exact-boundary success and over-limit no-output tests into wording that makes exact-boundary producers emit no archive.","section_id":"6.2","severity":"blocking"},{"category":"missing-requirement","check_key":"operator-files-home-provisioning-docs","description":"Local install now requires an existing absolute files_home, yet §6.3 does not require the operator guide to explain root provisioning or the install --files-home invocation. The current guide's bare install instruction becomes incomplete.","finding_id":"F96-operator-files-home-provisioning-docs","fix":"Expand §6.3 and its acceptance to document provisioning the hub bind directory, local install --files-home, the remote hub_daemon_url and existing-token prerequisite, and migrate-before-start sequencing. Add those instructions to V2 verification.","location":"P1/P2/P6 / §§ 1.1, 2.1, and 6.3","prevention":"Trace every new required CLI option into help, install guide, migration order, and failure-recovery documentation.","principle":"A mandatory install input needs an operator-facing provisioning and sequencing contract.","root_cause":"The code leaves add required --files-home behavior, while the documentation leaf limits system-requirements acceptance to the resulting profile path.","section_id":"6.3","severity":"blocking"},{"category":"traceability","check_key":"config-open-coverage-seam","description":"tests/cli/test_utils_coverage.py constructs the config-open seam without explicit datastore_mode and asserts ensure_personal_project is always called. The planned local-only guard makes that existing assertion stale outside §2.1 Targets and focused validation.","finding_id":"F97-config-open-coverage-seam","fix":"Add tests/cli/test_utils_coverage.py::* to §2.1 Targets. Make its fixture explicitly local and add or preserve a remote case proving sentinel-row handling without filesystem identity creation.","location":"P2 / § 2.1","prevention":"After changing a mode guard, enumerate every direct unit and coverage test of the guarded caller and classify local and remote expectations.","principle":"Every existing test that asserts behavior at a changed branch must be owned by the leaf that changes that branch.","root_cause":"The target inventory includes tests/cli/test_cli_utils.py and the new remote suite while omitting tests/cli/test_utils_coverage.py's unconditional config-open personal-initialization assertion.","section_id":"2.1","severity":"blocking"},{"category":"traceability","check_key":"runner-probe-role-consumers","description":"src/gobby/runner.py has two contention paths that interpolate probe_daemon_lock as a daemon PID and describe every holder as a live daemon. A maintenance holder therefore reaches unplanned production consumers with the wrong contract.","finding_id":"F98-runner-probe-role-consumers","fix":"Add the two src/gobby/runner.py entry paths to §6.2 Targets. Require them to destructure daemon versus maintenance roles and exit before initialization for either held role, with both paths covered in tests/test_runner_pid_file.py.","location":"P6 / §§ 6.1 and 6.2","prevention":"For each changed return type, inventory all callers, formatting sites, branches, fakes, and tests before assigning Targets.","principle":"A return-shape change needs every destructuring and exhaustive consumer in the target and test inventory.","root_cause":"Section 6.2 changes probe_daemon_lock from an integer PID probe to a role-bearing result while targeting CLI start/stop paths and omitting two runner.py consumers.","section_id":"6.2","severity":"blocking"},{"category":"missing-requirement","check_key":"attachment-project-identity-propagation","description":"The plan removes machine_id filtering and requires project-plus-attachment visibility without defining how project_id reaches existing ID-only routes and WebSocket callers. The tuple contract is therefore unimplementable from the named seams.","finding_id":"F99-attachment-project-identity-propagation","fix":"Change hub attachment GET/DELETE and WebSocket preparation/bind inputs to carry project_id explicitly, add _attachment_preparation.py and session_observe_proxy.py caller targets, forward project_id through remote proxy requests, and require both columns in storage predicates and tests.","location":"P3/P5 / §§ 3.1 and 5.1","prevention":"Trace identity fields from route parameters and message context through every storage predicate and proxy request.","principle":"Every component of a declared storage identity must be available before each fetch, bind, claim, and delete.","root_cause":"The plan declares (project_id, attachment_id) identity, while current HTTP GET/DELETE and direct WebSocket preparation callers carry attachment_id alone.","section_id":"3.1","severity":"blocking"},{"category":"unhandled-edge","check_key":"bootstrap-writer-lock-domain","description":"A locked read-modify-write can race an unlocked full writer, lose the other operation's mode-specific owner field, or observe partial fresh-install publication. The two-update acceptance case does not cover these reachable writers.","finding_id":"F100-bootstrap-writer-lock-domain","fix":"Require every bootstrap publisher to use the existing exclusive_file_lock and one durable validated replace path. Add a private already-locked writer for update_bootstrap_yaml, then test update-versus-direct-create and update-versus-exposure-rollback races.","location":"P1/P2 / §§ 1.1 and 2.1","prevention":"Inventory every writer of a shared file and test each direct-writer-versus-RMW pairing under one lock protocol.","principle":"A serialization lock protects a publication domain only when every publisher of that file participates.","root_cause":"The plan locks update_bootstrap_yaml while direct write_bootstrap_yaml, fresh template/fallback publication, PostgreSQL defaults, and exposure staging or rollback can publish without the same sidecar.","section_id":"1.1","severity":"blocking"},{"category":"unhandled-edge","check_key":"verified-root-python-capability","description":"The plan's device/inode re-stat cannot prevent a TOCTOU swap that redirects profile, attachment, migration, unpack, or restore operations outside the verified bind. Rust receives an anchored verified-root write contract; the Python writers and removers do not.","finding_id":"F101-verified-root-python-capability","fix":"Define one verified-root capability that holds the files_home directory identity and performs no-follow descendant traversal and publication relative to it. Apply it to Python profile, identity, attachment, migration, unpack, and restore paths, with root- and ancestor-swap barrier tests.","location":"P1-P6 / §§ 1.1 through 6.2","prevention":"For every bind-root read, write, and delete, inject root and ancestor swaps after validation and before the final filesystem syscall.","principle":"A no-replaced-root guarantee needs descendant operations anchored to a held root identity through publication.","root_cause":"Python paths re-stat files_home and then perform later pathname-based mkdir, open, replace, resolve, or unlink operations; the checked root or an intermediate ancestor can change between those steps.","section_id":"3.1","severity":"blocking"},{"category":"unhandled-edge","causal_finding_id":"F91-user-md-bounded-ingress","causal_section_ids":["5.1"],"check_key":"user-md-wire-content-bounds","description":"A content string containing exactly 1,048,576 UTF-8 bytes always produces a larger JSON body, and escaping can expand the wire body further. Rejecting Content-Length above the content ceiling rejects valid boundary content; treating the ceiling as wire bytes contradicts the decoded-byte rule.","finding_id":"F102-user-md-wire-content-bounds","fix":"Specify a separate bounded JSON wire-body maximum large enough for the worst permitted escaping and fixed envelope overhead. Apply Content-Length only to that wire bound, incrementally enforce content.encode('utf-8') at 1,048,576 bytes, and test both exact boundaries.","introduced_in_round":7,"location":"P5 / § 5.1","prevention":"For every structured upload, test wire bytes and decoded payload bytes independently with ASCII, multibyte, escaped, exact-limit, and limit-plus-one cases.","principle":"Encoded transport size and decoded content size need separate limits and exact-boundary outcomes.","root_cause":"The Round 7 F91 repair applies the 1,048,576-byte decoded UTF-8 ceiling to Content-Length even though JSON envelope and escaping bytes are part of Content-Length.","section_id":"5.1","severity":"blocking"},{"category":"unhandled-edge","check_key":"files-home-legacy-source-overlap","description":"files_home can equal $GOBBY_HOME/personal despite Constraint 1, making the USER.md source and destination identical and allowing equal-both-present recovery to unlink canonical content. Containment with personal, topic, or attachment roots also creates recursive or overlapping moves.","finding_id":"F103-files-home-legacy-source-overlap","fix":"Reject filesystem-root paths and equality, ancestor, or descendant overlap between files_home and every resolved legacy personal, project-attachment, and wiki-topic source during install validation and migration preflight. Add zero-mutation table-driven overlap tests.","location":"P1/P2/P6 / §§ 1.1, 2.1, and 6.1","prevention":"Before migration, cross the destination root against every source root for equality and both containment directions.","principle":"A destructive migration requires source and destination roots to be disjoint before graph construction or publication.","root_cause":"Bootstrap and install validate only that files_home is existing and absolute; migration checks destination collisions without rejecting root equality or ancestor and descendant overlap with legacy sources.","section_id":"6.1","severity":"blocking"},{"category":"unhandled-edge","check_key":"attachment-delete-claim-recovery","description":"A worker can die after claiming or after unlinking, leaving a permanently claimed row or a missing-file row that bind waits on forever. Cancellation of a to_thread unlink also leaves the filesystem operation running after the coroutine exits.","finding_id":"F104-attachment-delete-claim-recovery","fix":"Specify a tokenized deletion lease with claimed_at and bounded expiry, CAS claim/reclaim/release/delete, and FileNotFound convergence. Add any required schema migration target and crash/cancellation tests at each claim-unlink-delete boundary.","location":"P3/P5 / §§ 3.1 and 5.1","prevention":"Inject failure after claim, after byte deletion, and before row delete; prove another worker can reclaim and converge each state.","principle":"A persisted destructive claim needs bounded ownership and a recovery transition after claimant loss.","root_cause":"The plan adds a deleting state and bind wait/reject behavior without expiry, claimant token, or reclaim semantics across process death and cancellation.","section_id":"3.1","severity":"blocking"},{"category":"unhandled-edge","check_key":"pack-output-source-overlap","description":"An output such as <files_home>/USER.md can overwrite canonical content, while any absent descendant mutates the source tree after prewalk and can make archive construction self-referential or invalidate the normalized graph.","finding_id":"F105-pack-output-source-overlap","fix":"Reject pack and files-home backup output equality or containment within files_home and every other archived root before stopping services or creating output. Create temporary archives outside all source roots, then fsync and atomically publish to the validated destination.","location":"P6 / § 6.2","prevention":"For every archive producer, compare final and temporary output paths against equality and containment for each source root before source discovery.","principle":"An archive output and its temporary publication path must remain outside every archived source root.","root_cause":"Pack accepts an arbitrary resolved output while the producer prewalk treats files_home as an input tree and never cross-checks output containment.","section_id":"6.2","severity":"blocking"}],"reviewer_session":"c4efdd02-5f28-4dbe-95b7-7bcb3ee615d5","round":8,"verdict":"needs_review"},"session_id":"e18533a2-3d7e-4fa4-bdc4-df7028759faf"}
```

**Round 9** `kind: verification`

- reviewer_run: 712c1f12-97be-4c5d-b278-6048b45891e3
- reviewer_session: 9ef578b6-91f9-46c3-938e-908411948187
- verdict: needs_review
- findings:
- F106-remote-probe-negative-acceptance / blocking / auth, network, timeout, missing-root probe failures lack zero-effect acceptance
- F107-archive-free-space-acceptance / blocking / insufficient-space preflight has no acceptance
- F108-archive-publication-recovery-acceptance / blocking / temp/fsync/replace/interrupt have no failure-injection acceptance
- F109-stop-maintenance-precheck / blocking / _do_stop omitted; refuse can run after runtime/service effects
- F110-archive-source-swap / blocking / producers re-resolve after prewalk
- F111-wire-raw-byte-bound / blocking / wire ceiling is Content-Length only
- F112-unlink-cancellation-lease / blocking / cancel/expiry can release a live unlink
- F113-expired-lease-bind / blocking / expiry after unlink can bind missing bytes
- F114-start-probe-claim-gap / blocking / start probe releases before runner claim
- F115-gwiki-probe-claim-gap / blocking / gwiki probe then mutate after releasing lock
- resolution_notes: Accepted F106-F115. F106 expands 5.1.22 and adds 2.1.13. F107/F108 add unpack/restore space and publication-failure acceptance. F109 adds _do_stop and requires the role check first. F110 emits through the held files_home fd. F111 counts Request.stream bytes. F112/F113 shield unlink and restrict bind to claim_token IS NULL. F114/F115 keep claim_pid_file: start claims and hands off; gwiki holds through mutation; no new lock, no third role, no daemon-routed gwiki.

```json plan-review-round
{"evidence_id":"52acc782-383e-4dbc-a3c1-935829875b6e","plan_hash":"99d3b3b16cd3f5a5ae36d56d2b42e8aeb74bfe9862fbac79d5aeca2f178142d4","round_number":9,"round_result":{"coverage_attestation":{"adjacent_variant_complete":true,"attestation_digest":"de9807639ceafd6682520e26407d7ee0ae1a851e85b095389135185a9d232c81","cross_lane_interaction_complete":true,"disposition_counts":{"dismissed":0,"emitted_findings":10,"total":10},"evidence_id":"52acc782-383e-4dbc-a3c1-935829875b6e","lanes":[{"candidate_count":3,"lane_id":"requirements_traceability","status":"completed"},{"candidate_count":2,"lane_id":"repository_blast_radius","status":"completed"},{"candidate_count":5,"lane_id":"runtime_invariants","status":"completed"}],"shadow_manifest_status":{"entry_count":8,"manifest_digest":"a4aa08f07ed830227c4294b633eb26990e52c2545fd1e94bda386504743741dd","status":"valid"},"source_digest":"7495cddb87ce0a3ab29050a793b04d1b157a53f3f1d831bf3a2a3c0bca0cd214","version":1},"findings":[{"category":"weak-testability","check_key":"remote-probe-negative-acceptance","description":"Remote install can fail for invalid credentials, network or timeout errors, and a missing owner root, yet no acceptance item proves those branches start no managed services.","finding_id":"F106-remote-probe-negative-acceptance","fix":"Expand 5.1.22 or add an acceptance item covering invalid-token/auth rejection, network or timeout failure, and missing-owner-root failure as typed install errors with zero managed-service startup; exercise them in tests/cli/test_install_coverage.py.","location":"P2/P5 / §§ 2.1 and 5.1","prevention":"Enumerate every typed preflight failure and pair it with an assertion over services, bootstrap state, and filesystem effects.","principle":"Every install-preflight failure that gates managed-service startup needs an observable zero-side-effect acceptance case.","root_cause":"Auth, network, timeout, and missing-owner-root probe failures are specified in prose, while acceptance covers only a missing token, hop refusal, and remote-target refusal.","section_id":"2.1","severity":"blocking"},{"category":"weak-testability","check_key":"archive-free-space-acceptance","description":"Insufficient destination space is a required archive-wide preflight branch with no acceptance proof that files_home and datastores remain unchanged.","finding_id":"F107-archive-free-space-acceptance","fix":"Add insufficient-space cases for files-home unpack and hub-backup restore that refuse before either domain mutates, including confirmation and --force paths where applicable.","location":"P6 / § 6.2","prevention":"List each restore preflight predicate and assert its failure under ordinary, confirmed, and force paths before either storage domain changes.","principle":"A destructive restore preflight requirement needs a failure test proving refusal precedes every mutation.","root_cause":"Section 6.2 requires a free-space check, while its acceptance matrix covers bounds, graph conflicts, collisions, late invalid members, and copied-size mismatch without insufficient space.","section_id":"6.2","severity":"blocking"},{"category":"weak-testability","check_key":"archive-publication-recovery-acceptance","description":"Producer and consumer acceptance never exercises temp-write, fsync, replace, or mid-member interruption, so partial-output and prior-destination preservation remain unproved.","finding_id":"F108-archive-publication-recovery-acceptance","fix":"Add producer and consumer failure-injection acceptance covering temporary archive write, fsync, atomic replace, and files-member publication; require no partial authoritative output, preservation of any prior destination, defined temp handling, and successful rerun.","location":"P6 / § 6.2","prevention":"Inject failure during temp write, file and directory fsync, replace, and member publication; verify prior authoritative state and rerun convergence.","principle":"Durable publication and operator-rerun claims require failure injection at each durability boundary.","root_cause":"The plan specifies temporary output, fsync, atomic replace, non-authoritative interrupted members, and rerun recovery without acceptance cases for those transitions.","section_id":"6.2","severity":"blocking"},{"category":"traceability","check_key":"stop-maintenance-precheck","description":"The Targets omit src/gobby/cli/daemon.py::_do_stop, so the planned maintenance refusal can occur only after runtime or service-manager effects have already started.","finding_id":"F109-stop-maintenance-precheck","fix":"Add src/gobby/cli/daemon.py::_do_stop to § 6.2 Targets and require its first operation to inspect the role-bearing singleton and refuse maintenance before get_cli_runtime, service status/stop, Docker handling, or stop_daemon_util; test direct stop and restart zero-effect paths.","location":"P6 / § 6.2","prevention":"Trace each public command through shared helpers and target the earliest consumer that can perform configuration, service, Docker, or signaling effects.","principle":"Every entry point that consumes a changed claimant-role contract must be targeted before any protected side effect.","root_cause":"Both stop and restart enter daemon._do_stop, which loads DB-backed runtime and may invoke service-manager work before the targeted utils_shutdown.stop_daemon check.","section_id":"6.2","severity":"blocking"},{"category":"unhandled-edge","check_key":"archive-source-swap","description":"Both files-home producers can archive a symlink, hardlink, special node, or different file swapped in after the normalized graph passed preflight.","finding_id":"F110-archive-source-swap","fix":"Require both producers to emit the prewalk graph relative to the held files_home directory fd, open descendants no-follow, fstat and compare identity/type/link-count/size at emission, feed addfile from the already-open stream, and remove unpublished temp output on mismatch. Add leaf- and ancestor-swap barrier tests.","location":"P6 / § 6.2","prevention":"Place barriers after prewalk and swap leaves and ancestors to every forbidden type and to a different regular-file identity before emission.","principle":"Archive emission must consume the same file identities and types that passed preflight.","root_cause":"A prewalk lstat is followed by path-based TarFile.add-style emission that re-resolves descendants and ancestors after validation.","section_id":"6.2","severity":"blocking"},{"category":"unhandled-edge","causal_finding_id":"F102-user-md-wire-content-bounds","causal_section_ids":["5.1"],"check_key":"wire-raw-byte-bound","description":"A missing-length, chunked, or under-reported USER.md request can exceed 6,291,470 wire bytes while decoding to content within 1,048,576 bytes.","finding_id":"F111-wire-raw-byte-bound","fix":"Count Request.stream() bytes and reject the first byte above 6,291,470 regardless of HTTP framing; retain the header as an early check, parse only the bounded body, then enforce the decoded ceiling. Add no-publication boundary tests for each framing variant.","introduced_in_round":8,"location":"P5 / § 5.1","prevention":"Test exact-limit and plus-one raw bodies with Content-Length present, absent, chunked, and under-reported before JSON parsing or publication.","principle":"A wire-body ceiling must constrain bytes actually received independently of declared Content-Length and decoded content size.","root_cause":"The Round 8 wire-limit repair rejects oversized Content-Length and counts decoded content bytes without requiring an incremental raw-wire byte count.","section_id":"5.1","severity":"blocking"},{"category":"unhandled-edge","causal_finding_id":"F104-attachment-delete-claim-recovery","causal_section_ids":["3.1"],"check_key":"unlink-cancellation-lease","description":"Cancellation or a slow unlink can free or expire the claim while the original worker still has authority to delete bytes, allowing bind or another cleaner to race that deletion.","finding_id":"F112-unlink-cancellation-lease","fix":"Track and shield the unlink task, keep or renew the token while it runs, and on cancellation await its terminal result before CAS delete/release and re-raising. Add cancellation and longer-than-60-second barrier tests.","introduced_in_round":8,"location":"P3 / § 3.1","prevention":"Cancel after the unlink worker starts and hold it past lease expiry; prove bind and reclaim remain excluded until the worker terminates.","principle":"A deletion lease must remain exclusive until its destructive filesystem operation reaches a known terminal state.","root_cause":"The Round 8 repair releases a lease when cancellation interrupts asyncio.to_thread even though cancellation does not stop the unlinking thread; a fixed expiry can likewise elapse during a live worker.","section_id":"3.1","severity":"blocking"},{"category":"unhandled-edge","causal_finding_id":"F104-attachment-delete-claim-recovery","causal_section_ids":["3.1"],"check_key":"expired-lease-bind","description":"After unlink succeeds and the worker crashes before row deletion, lease expiry can expose a surviving row to bind while its file is absent.","finding_id":"F113-expired-lease-bind","fix":"Require bind CAS to accept only claim_token IS NULL; reserve expired-token reclaim for cleanup, treat FileNotFound as completed deletion, and let bind proceed only after a live-file unclaimed state or row deletion. Add the expiry barrier test.","introduced_in_round":8,"location":"P3 / § 3.1","prevention":"Crash after unlink, wait through expiry, then race bind against cleanup reclaim and assert that no bound row without bytes can result.","principle":"Crash recovery must never make a metadata row bindable after its canonical bytes have already been removed.","root_cause":"The Round 8 lease wording governs bind only for non-expired claims and leaves expired-claim ownership between bind and cleanup unspecified.","section_id":"3.1","severity":"blocking"},{"category":"unhandled-edge","check_key":"start-probe-claim-gap","description":"Maintenance can claim after gobby start's early probe and before the runner claim, while start continues the side effects the maintenance fence is meant to exclude.","finding_id":"F114-start-probe-claim-gap","fix":"Require the start front door to acquire a daemon/startup reservation before its first protected effect and retain or atomically hand it to the direct or service-managed runner without an unlocked interval. Add reciprocal barriers for migrate, pack, unpack, snapshot, and restore.","location":"P6 / § 6.2","prevention":"Pause after startup reservation and race every maintenance command in both winner orders; assert exactly one entrant and zero startup effects when maintenance wins.","principle":"An exclusivity check must reserve ownership continuously from the check through every protected startup effect.","root_cause":"probe_daemon_lock releases a free lock immediately, while the runner claims daemon ownership only after services, runtime loading, service-manager dispatch, or subprocess launch can begin.","section_id":"6.2","severity":"blocking"},{"category":"unhandled-edge","check_key":"gwiki-probe-claim-gap","description":"Maintenance can claim after gwiki's probe and before descendant or wikis.json mutation, invalidating migration or archive preflight during the protected campaign.","finding_id":"F115-gwiki-probe-claim-gap","fix":"Hold exclusion through the complete gwiki mutation. One compatible protocol is: refuse a maintenance holder; proceed under a live daemon holder because it excludes maintenance; when free, atomically claim the singleton for the direct mutation and release after publication. Add barriers against migrate, pack, unpack, snapshot, and restore.","location":"P4/P6 / §§ 4.1 and 6.2","prevention":"Pause after eligibility checking and before each Rust publication, race every maintenance campaign, and prove no mutation occurs while maintenance owns the singleton.","principle":"A maintenance refusal check must fence the entire direct mutation, including every vault and registry publication.","root_cause":"gwiki init and register_scope perform a one-time singleton probe, then mutate after releasing the probed lock.","section_id":"4.1","severity":"blocking"}],"reviewer_session":"9ef578b6-91f9-46c3-938e-908411948187","round":9,"verdict":"needs_review"},"session_id":"e18533a2-3d7e-4fa4-bdc4-df7028759faf"}
```

## T1 Task Mapping
`kind: framing`

| Plan Item | Task Ref | Status |
|-----------|----------|--------|
| Epic | #20330 | open |
