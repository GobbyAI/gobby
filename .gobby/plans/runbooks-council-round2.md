# Runbooks — council round 2

Plan: `.gobby/plans/runbooks.md` (Plan ID: runbooks; epic #22691 "Runbooks: role-bound multi-agent tabs loaded from gclient"; lane epic #22772 "Lane: runbooks (Golden Path track 3)").
Planning task: #22808 "Runbooks plan council round: enhance, adversarial review and gate .gobby/plans/runbooks.md".
Pre-round hash: `ab259dde9ddcd1c1e4201f4fabcc6212040a83fe635adfb29f59f0baca59df92` (writer commit `2893fa41f9473daf6e2529415cf58c5ee2decfd8`, the 2026-09-22 bytes with enhancement round 1 folded under V1, no edits). `uv run gobby plans validate .gobby/plans/runbooks.md` (bare) passes with one non-blocking `consumer-coverage` warning (2.1, `crates/gclient/src/views/mod.rs`).

Flow (Josh, verbatim in #22808, superseding the prompt book's two-round cap): "I was wanting the agents to debate and come to consensus, then the adversary finalizes and sends to PD for review, then the PD sends it to you so I can review it." No round cap. The writer is the only editor of the plan and of this file. Expansion waits for Josh.

Participants:
- Writer: gobby#14425 (`912b3959-4924-4d93-a092-1a18636f2e31`), claude-fable-5-1, pane "plan writer".
- Enhancer: gobby#14422 (Codex), relaunched after the cutover as session `402a0450-e503-4285-9fab-1c6b56fa8d37`; the PD relayed the S5 hash with GO and the writer confirmed the immutable handoff in message `d1268124` (22:4x CDT).
- Adversary: gobby#14423 (Grok 4.7), attacks the folded hash after the enhancer round.
- Program Director: gobby#14018 (`44738b98-9535-4ec5-8be5-57b345780be5`); gate and finalization recipient.
- Symbol lookup: gobby#14332 (read-only). Comms hub: gobby#14069.

Priority rule: a signed source outranks either reviewer: `ROADMAP.md`, `~/Desktop/gobby-golden-path-2026-09-22.md` (v14 rulings), `.impeccable.md`, the #22691 description and validation criteria (Josh's five standing instructions; terminal-backend policy; role-definition content), the plan's Decision Record, and the prompt book `~/Desktop/gobby-agent-prompts-2026-09-22.md` (criterion 10: input of record for the role definitions). Later Josh rulings supersede earlier ones.

## Writer pre-round read-through (2026-09-23, 19:1x-20:0x CDT)

Every fact below was verified on 0.5.0 HEAD `218ea813` (the parent of the pre-round commit). Mechanical drift is folded by the writer as a logged pre-round commit (W-items); direction-level conflicts went to the Program Director as one batched list (Q-items) and are not folded until ruled.

### W1 Migration number (1.1) — FOLDED in `d42521cc` (writer pre-round commit)
Migrations 446 (`grant_agent_project_resolution`), 447, 448 and 449 (`workspace_default_project`) landed after the plan was written. 1.1 becomes `450_add_runbook_roles.sql`; the latest existing migration is `449_workspace_default_project.sql`; `latest_version` becomes 450 (`crates/gdaemon/tests/cli_contract.rs:58` asserts 449 today; `crates/gcore/src/schema/assets.rs:216` embeds 449). Constraint names unchanged.

### W2 Size guard re-measured (Constraints) — FOLDED in `d42521cc` (writer pre-round commit)
`src/gobby/terminals/workspace_ops.py` 990 (was 964), `src/gobby/storage/workspaces.py` 969, `crates/gclient/src/app/live_loop/actions.rs` 1002 (was 979; over the ceiling with no `#[cfg(test)]` boundary, so `crates/gclient/tests/source_size.rs::no_src_file_at_or_above_1000_lines` (lines 41-48, `>= 1000`) is red on HEAD; last touched by #22780 `132e42c8f5`, an ancestor of HEAD, blob 36133 bytes identical to the working tree; reported as found work to gobby#14069 (`33efc9bc`) and, after the assistant read a compacted `git show` count of 891 lines, to the PD with the byte evidence (`91ee37dd`); the PD closed it: the #22780 merge `3467854a4d`, right after `fff3192be0`, takes the file to 891 lines and source_size passes on the merged tree, so no action; the plan does not target that file), `menu.rs` 970, `live_loop.rs` 938, `app/mod.rs` 950, `daemon/live.rs` 987, `daemon/mod.rs` 989, `startup.rs` 691, `daemon/workspace.rs` 391, `apply_persona.py` 305, `_session_start/agents.py` 241, `agents_spawn_tools.py` 122.

### W3 1.2 pane I/O split re-derived — FOLDED in `d42521cc` (writer pre-round commit)
`PaneWrite` already moved to `src/gobby/terminals/workspace_writes.py` (#22722, with `WorkspacePaneWriteError` and `write_workspace_pane`; `WorkspaceOps._write` now delegates to it). The split list drops `PaneWrite` and moves `PaneOutputWait`, `IDEMPOTENCY_KEY_PATTERN`, `WAIT_CAPTURE_LINES`, `WAIT_CAPTURE_FAILURE_LIMIT`, `pane_send_text` (571-590), `pane_send_keys` (592-626), `pane_read` (628-638), `pane_wait_for_output` (640-689), `_pane_terminal` (931-943), `_runtime` (945-951), `_write` (953-990). New since the plan: `WorkspaceOps.workspace_list` (281-287) and `workspace_snapshot(project_id=)` (332-368); `Workspace.default_project_id` (migration 449) on the workspace row only. Refreshed hints: `tab_create` 372-402, `pane_split` 460-490, `pane_rename` 541-548, `_emit` 721-738, `_fill` 823-873; `WorkspaceManager.create_tab` 600-638, `list_panes` 570-580, `add_pane` 722-735, `remove_pane` 737-750, `swap_panes` 752-764, `move_pane` 766-805, `rename_pane` 817-822, `get_pane_for_terminal` 888-893. `TerminalManager.list_reconcilable_by_machine` was added (no effect on the JOIN).

### W4 2.1 drift — FOLDED in `d42521cc` (writer pre-round commit)
`crates/gclient/src/views/mod.rs:36-37` wraps `crate::startup::run()` (the validator warning): add it to Consumers unchanged. `attach_request(node, workspace, project_id)` now takes three arguments (`daemon/workspace.rs:336-350`); `WorkspaceOp::WorkspaceList` (`workspace.list`) and `WorkspaceRow.default_project_id` exist; `startup::run` is at 674-691. `WorkspaceModel::apply` lives at `crates/gclient/src/app/workspace_ops.rs:47-83`.

### W5 rb-2 verified (Golden Path ruling 10 asks the writer to verify it) — HOLDS
`pub enum WorkspaceEventKind` is a closed serde enum at `crates/gclient/src/daemon/workspace.rs:118`; `daemon_event` (`crates/gclient/src/daemon/live_reader.rs:372`) decodes with `serde_json::from_value(value).map_err(protocol_error)` at 414, so an unknown `pane.role_set` kind drops the socket. 2.1.7 stands.

### W6 Role-definition content (criteria 8-9) — PENDING FOLD (folded with the P3 rewrite after the Q1 ruling, not in the pre-round commit)
Josh's five standing instructions (memories 33cb3885, b8f8c9df, cdd18230, c3c59cb9) go verbatim into every P3 definition's persona text; the assistant definition carries the four communications-coordinator requirements explicitly. Mechanical; folded with P3 once Q1 is ruled.

### W7 bundle.rs non-postgres golden — found work, no plan edit beyond the 1.1 note
`crates/gcore/src/grant/bundle.rs` `#[cfg(not(feature = "postgres"))]` fallback: `GOLDEN_LATEST_CHECKSUM` is 449's sha256 (`41cfe81e...`) beside `latest_version: 447` (#22618 `2b7cf60e79` moved the checksum only). Reported to gobby#14069 (`5089ede8`), routed to the PD. 1.1's research context notes it so step (4) is not a surprise.

### Direction-level conflicts (batched to the PD; not folded until ruled)
Q1 roster (decision 8 vs. #22691 criterion 10, Golden Path track 3, rulings 15/21/22/23); Q2 layout (decision 10 vs. the live tabs); Q3 #22713 fold (rulings 7, 10); Q4 terminal-backend criteria 1-7 (ruling 16; daemon side is #21565's); Q5 #22695 placement (ruling 10); Q6 which council flow the definitions encode (#22808 FLOW vs. ruling 21). Full text in the PD message recorded below.

## Enhancer items (round 2)
Reviewed hash: S5 `c8a9238f...843bad` at `90c38956`. Format per item: enhancer position; writer verification; move (FOLD with the change named, or CONTEST with reasoning); resolution (folded or withdrawn).

### E2.1 cr-1 (lens better, clarity; 1.2/1.3/1.4 and V1 step 6; enhancer message `209a6ed8`, 22:52 CDT)
Enhancer: state that `pane.set_role` persists the binding for the next SessionStart and does not switch the already-running session; an immediate live switch is `apply_agent_definition`; V1 step 6 verifies the active `_agent_type` before and after that call and after `/clear`.
Writer verification: true of the design. 1.2's `pane_set_role` writes the pane row and emits `pane.role_set`; 1.3 reads `pane.role` only at the two SessionStart activation sites; decision 1 names `apply_agent_definition` as recovery and manual switching. The plan never said the running session is untouched, and V1 step 6 checked only the after state.
Move: FOLD. 1.2's surfaces paragraph and 1.2.6 say `pane.set_role` binds the next SessionStart only and that the tool description and docs say both paths; 1.3.4's guide behavior names both; 1.4's registration sentence calls the tool the one live switch; V1 step 6 now runs in the log-monitor pane and checks `_agent_type` before set-role, after `apply_agent_definition` and after `/clear`. No new op or automatic mutation.
Resolution: folded in the E2 commit.

### E2.2 cr-2 (lens better, testability; 2.1 scripts and V1 step 7; enhancer message `209a6ed8`)
Enhancer: after each successful `new-tab`, print `CREATED_TAB=<ref>` to stderr; document manual cleanup with the planned `gclient kill <tab-ref>`; add one mock-daemon script test that refuses a later op and asserts nonzero exit plus the printed ref; keep `set -e` and leave partial tabs in place.
Writer verification: the scripts capture refs into shell variables that die with the script; `kill REF` already sends `tab.close` for a three-segment ref; 2.1.5 and 2.1.8 test only the happy path against the mock daemon and V1 step 7 is live-only. Lookup gobby#14332 (asked by the enhancer) confirmed no cross-call script cleanup or tracking exists today.
Move: FOLD. Scripts paragraph: each successful `new-tab` is followed by `echo "CREATED_TAB=<tab ref>" >&2`, no rollback; 2.1.6 adds the failure path to the guide; new 2.1.9 with test `script_stops_at_refused_op_and_names_created_tabs` (the mock daemon refuses the first `split` after the second `new-tab`: nonzero exit, `code: reason`, one `CREATED_TAB=` line per tab, no further op); V1 step 7 names the lines.
Resolution: folded in the E2 commit.

### Enhancer consensus (enhancer message, 2026-09-23 23:0x CDT)
Verbatim: "CONSENSUS on .gobby/plans/runbooks.md at commit f6388d0534366a8f6023bed1bdb36e8a81fbca62, sha256 d9d65815736a1cd0b384860228d34241ec36190ee96b5dba1ff7be114e03afcf. I verified both working file and committed blob against the hash, reviewed the E2 diff and council-log entries, and independently ran bare `uv run gobby plans validate .gobby/plans/runbooks.md` (clean, 3 phases). cr-1 folded ... cr-2 folded ... No further enhancement items on this hash; enhancer round converged. Please hand this exact commit/hash to adversary gobby#14423 per #22808 FLOW." Writer: hash handed to the adversary in the same minute (waking message).

## Adversary findings
The adversary attacks the enhancement-consensus hash `d9d65815...afcf` at `f6388d05` (handed over 23:0x CDT). Same format as the enhancer items, plus `check_key`.

### AR1 batch (adversary message `aea43e69`, 2026-09-23 23:25 CDT; correction: six findings, not five; supplement `eb080656` 23:38 CDT adds A7 and reports lookup f6ea88b7 with no finding on task_artifacts, migration 449, WorkspaceEventKind, Session.ref, the pane/tab constructors or `_with_ratio`)

#### A1 `runbook-3.2.6-terminal-backend-token` (weak-testability, blocking)
Adversary: 3.2.6 cannot pass; it requires the criterion 11 sentence (which contains `terminal_backend`) and a file with no `terminal_backend`.
Writer verification: correct; the sentence is verbatim in the lane-manager bullet and 3.2.6 forbade the token.
Move: FOLD. 3.2.6 asserts the sentence verbatim and forbids only a mapping key `backend` or `terminal_backend` in the loaded tree.
Resolution: folded in the AR1 commit.

#### A2 `runbook-3.2.3-gterm-in-family-table` (missing-requirement, blocking)
Adversary: the log-monitor persona seeds the book's family table, whose row quotes `terminals.host_manager._health_loop - gterm control probe failed` (book 495); 3.2.3 forbids `gterm` in any prompt string, so "Divergence: none" and 3.2.3 cannot both hold.
Writer verification: correct; book line 495 is the only `gterm` in the book and no prompt has `tmux`.
Move: FOLD as proposed. 3.2.3 exempts backtick-quoted log signatures (the test strips backtick spans before scanning); the research line says the same; the row stays and the log-monitor divergence does not mention it. Criterion 1 keeps the key check and the ban on instructions that choose a backend.
Resolution: folded in the AR1 commit.

#### A3 `runbook-3.2-log-monitor-nominal-telemetry` (missing-requirement, blocking)
Adversary: the persona carries standing instruction 1 ("Systems nominal" rather than load numbers) and then orders a nominal line with `load <1m>/<5m>/<15m> | runs <R>`, with divergence none; 3.2.2 would pass the contradiction.
Writer verification: correct. Memory 33cb3885 (Josh, 2026-09-22, "Again"): no load averages or run counts as routine status; the number is the news only when breaching. Book line 460 diverges from its own instruction 1.
Move: FOLD as proposed. The nominal line is `Systems nominal | window HH:MM-HH:MM | <N> warnings in <K> families, all mapped`; the divergence line names the dropped fields and book line 460; alarms still cite the breached figure; 3.2.2 fails if any persona's nominal template carries `load <` or `runs <`. Writer note for the PD: the alternative was to keep the figures as Program-Director-only telemetry that never reaches Josh (the memory's DIGEST-file carve-out); the fold follows the instruction as written, and the Program Director has `uptime` and `list_running_agents` itself.
Resolution: folded in the AR1 commit.

#### A4 `runbook-d2-cites-live-reviewer-item` (traceability, blocking)
Adversary: D2's `original_acceptance_items` is 3.2.5, a live reviewer item 3.2 delivers, so the retirement task minted at expansion would be graded on the reviewer test, not on replacing `epic_qa`, the build coordinator and the autonomous-build memory.
Writer verification: correct. `_parse_original_acceptance_items` (parser.py 747) requires a non-empty list of dotted ids; `_coverage_items` (coverage.py 530) makes those ids D2's own coverage rows and `_deferral_covers_item` (642) never cross-checks them against a deliverable, so D2-local ids are valid (`DOTTED_ID_PATTERN` accepts `D2.1`). The S2 repoint from the dropped post-epic-reviewer item onto 3.2.5 caused it.
Move: FOLD. D2 lists its own criteria D2.1 (a planning pass after one full night names what replaces `epic_qa` and the build coordinator, or keeps them) and D2.2 (`gobby build` and stage-manifest dispatch retired or re-scoped, the autonomous-build memory retired or rewritten); `original_acceptance_items` is `[D2.1, D2.2]`; 3.2.5 stays only on 3.2.
Resolution: folded in the AR1 commit.

#### A5 `runbook-2.1-kickoff-before-cli-ready` (unhandled-edge, blocking)
Adversary: the scripts wait for a shell prompt, send the launch line, then send the kickoff with nothing waiting for the CLI to accept input; V1 step 3 expects the kickoff submitted.
Writer verification: correct; `PROMPT_PATTERN` matches a shell, and the plan had no second wait.
Move: FOLD as proposed, with the patterns named from the live council panes (capture_output, 2026-09-23 23:3x CDT): `READY_claude='bypass permissions on'` (Claude Code's status footer under `--dangerously-skip-permissions`), `READY_codex='› Ask Codex'` (the empty composer placeholder), `READY_grok='always-approve'` (the composer border under `--always-approve`). Per seat: shell wait, launch `send-keys`, ready wait (timeout 60, exit 1 on timeout), kickoff `send-keys --enter`. 2.1.5 and 2.1.8 assert the kickoff op follows the ready wait; V1 step 3 checks the kickoff landed in the composer, not the shell.
Resolution: folded in the AR1 commit.

#### A6 `runbook-3.2-shared-tail-ephemeral-sessions` (unhandled-edge, blocking)
Adversary: the verbatim tail addresses gobby#14069 (UUID 26de7dbf) and gobby#14018, the 2026-09-22 chart sessions; after they end, registration and verdicts go to dead sessions.
Writer verification: correct; session numbers change on every `/clear` or relaunch, and 1.2's pane view already exposes `role` and `session_ref`.
Move: FOLD as proposed. The durable tail names the assistant and the Program Director by role, resolved through `gobby-workspaces:get_workspace` (the pane whose `role` is `assistant` or `program-director`, its `session_ref`), with a `project` send naming the missing role as the fallback, and states inside itself that the book's numbers and UUIDs are the 2026-09-22 chart; `SHARED_TAIL` in the test is that durable text; 3.2.2 and the registration line say so; the per-role divergence lines are unchanged because the tail's divergence is shared by all eleven.
Resolution: folded in the AR1 commit.

#### A7 `runbook-3.2-monitor-shell-spellings` (unhandled-edge, blocking; supplement `eb080656`)
Adversary: archivist and log-monitor copy `comms-agent.yaml`'s blocked_tools minus only `Bash`, which still blocks `shell` and `run_shell_command`; both seats launch Codex and need a shell for `date`, `uptime`, the log reads, `$TMPDIR/log-monitor-state.json` and the digest under `~/Desktop`; 3.2.1 locks the list into the test.
Writer verification: correct; comms-agent.yaml's list (46-60, per gobby#14332) carries four shell spellings and the plan removed one.
Move: FOLD as proposed. The two seats' `blocked_tools` is the write-only list (`Edit`, `KillShell`, `MultiEdit`, `NotebookEdit`, `Write`, `apply_patch`, `edit_file`, `notebook_edit`, `replace`, `write_file`); `Bash`, `BashOutput`, `shell` and `run_shell_command` stay available; `runbook-no-code-edits` still blocks repository mutations including shell writes inside the repo; 3.2.1 asserts the write-only list with no shell spelling.
Resolution: folded in the AR1 commit.

### AR2 (adversary message on the AR1 hash `e4baf664...6289` at `8d90e7f6`, 2026-09-23 23:5x CDT): A1-A7 confirmed folded; one new finding

#### A8 `runbook-2.1-grok-ready-pattern-matches-launch-line` (unhandled-edge, blocking)
Adversary: `READY_grok='always-approve'` is a substring of the adversary launch line `grok -m grok-4.7 --reasoning-effort high --always-approve`; `wait-for-output` matches text already on screen (the shell wait relies on that), so the ready wait matches the typed launch line and the kickoff goes in before grok's composer exists; the mock keeps 2.1.5/2.1.8 green while V1 step 3 fails for that seat.
Writer verification: correct; the Claude and Codex patterns are not substrings of their launch lines, the grok one was. Tonight's capture of the idle grok composer also shows its hint bar `Tab/→:accept suggestion  │  Shift+Tab:mode  │  Ctrl+x:shortcuts`.
Move: FOLD as proposed. `READY_grok='Ctrl+x:shortcuts'`; the scripts paragraph states that no `READY_*` value may be a substring of its seat's launch line and why; 2.1.5 and 2.1.8 assert it. Shell wait, launch, ready wait, kickoff order unchanged.
Resolution: folded in the AR2 commit.

### AR3 (adversary message on the AR2 hash `ab982751...e56f` at `2ed0b4e3`, 2026-09-23 23:5x CDT): A1-A8 confirmed folded; one new finding

#### A9 `runbook-2.1-grok-ready-regex-plus` (unhandled-edge, blocking)
Adversary: `--pattern` is a regex, so `Ctrl+x:shortcuts` reads `l+` as a quantifier and never matches the literal hint; the grok ready wait times out and the kickoff is never sent, while 2.1.8's substring check stays green.
Writer verification: correct; the verb list says `--pattern REGEX`.
Move: FOLD as proposed. `READY_grok='Ctrl\+x:shortcuts'`; the scripts paragraph says every `READY_*` value is a regex with metacharacters escaped and must match no launch line; 2.1.5 and 2.1.8 assert each regex matches its captured idle-composer text and no launch line.
Resolution: folded in the AR3 commit.

### Adversary finalization (adversary message on the AR3 hash `01f40581...bbbb` at `50d5cfff`, 2026-09-23 23:5x CDT, delivered on the writer's resume at 23:57 CDT): nothing remains
Adversary (verbatim): "Adversary gobby#14423: nothing remains on sha256 01f40581bba2492f479726f7a5caa3f2923a979135068213ca9607e513e7bbbb at commit 50d5cfff959cbee14df0b03a34902221a95cc35e. Finalized to the PD gobby#14018. Validate exited 0, git diff --check clean, worktree matches the blob. A1-A9 and cr-1/cr-2 stay folded."
Writer verification (23:57 CDT): `.gobby/plans/runbooks.md` in the worktree hashes to `01f40581...bbbb`, the blob committed at `50d5cfff`; bare `uv run gobby plans validate .gobby/plans/runbooks.md` prints the plan path and `Phases: 3` (Daemon seams, gclient command mode, Roles) and nothing else; bare `git diff --check` prints nothing. The branch head has since moved to `2f9e12d7` (another session's merge, `[gobby-#22810]`), which does not touch the plan; the final plan commit stays `50d5cfff`.
Resolution: every enhancer item (cr-1, cr-2) and every adversary finding (A1-A9) is folded; none was withdrawn or contested. Task criterion 4 is met by the adversary's message to gobby#14018. Superseded by R3 (Program Director rulings): the PD sent the `01f40581` hash back with three items at 00:38 CDT on 2026-09-24; criterion 4 is met again when the adversary finalizes on the R3 hash. It did at 01:0x CDT on 2026-09-24 (next entry).

### Adversary re-finalization (adversary message on the R3 hash `0504de2a...1431` at `898932d7`, 2026-09-24 01:0x CDT, delivered through the writer's coordination wait `f442f33d`): nothing remains
Adversary (verbatim): "Adversary gobby#14423: nothing remains on sha256 0504de2aea9e4327d3ff9054dbfc22d51236669aa1b137e8b6f216cca55a1431 at commit 898932d7ba1dccd5786d5573a508e24d82f1ad28. Re-finalized to the PD gobby#14018. Validate exited 0, git diff --check clean. S1-S3 and the xhigh launch line stay folded. A1-A9 and cr-1/cr-2 stay folded."
Writer verification (01:0x CDT): `.gobby/plans/runbooks.md` in the worktree and the blob committed at `898932d7` both hash to `0504de2a...1431`; bare `uv run gobby plans validate .gobby/plans/runbooks.md` prints the plan path and `Phases: 3` (Daemon seams, gclient command mode, Roles) and nothing else; bare `git diff --check` prints nothing.
Resolution: every enhancer item (cr-1, cr-2), every adversary finding (A1-A9) and every PD item (R3 S1-S3 and the launch line) is folded; none was withdrawn or contested. Task criterion 4 is met by the adversary's re-finalization to gobby#14018 naming commit `898932d7` and this hash.

Check-in (adversary message, 2026-09-23 21:0x CDT): "Send me the folded hash of .gobby/plans/runbooks.md ... only after enhancement consensus, with the commit sha. I attack that hash and no earlier one. I edit no files. Debate each finding until you fold it or I withdraw it; I finalize to gobby#14018." Writer acknowledgment sent 21:3x CDT (non-waking): the hash goes out only after enhancement consensus, in one message with the sha256, the commit sha and the bare validate result; no hash yet.
### Adversary re-finalization on the R4 hash (adversary gobby#14449 per the PD's R4 flow; message on `8e4ca52c...99a7` at `de1855317b`, 2026-09-24 04:2x CDT, delivered through the writer's coordination wait `a3e8fb00`): nothing remains
Adversary (verbatim): "Adversary gobby#14449: nothing remains on runbooks sha256 8e4ca52c0887e21dd9f9f5cd781dab9b8931400f52364b3e65b15c4c610999a7 at commit de1855317b5e6e293233363085c21946b0d908ca. R1 through R4 hold. Re-finalized to gobby#14018. Validate exited 0 with Phases 3, and git diff --check is clean."
Writer verification (04:24 CDT): `.gobby/plans/runbooks.md` in the checkout and the blob committed at `de1855317b5e6e293233363085c21946b0d908ca` both hash to `8e4ca52c0887e21dd9f9f5cd781dab9b8931400f52364b3e65b15c4c610999a7`; bare `uv run gobby plans validate .gobby/plans/runbooks.md` prints the plan path and `Phases: 3` (Daemon seams, gclient command mode, Roles) and nothing else; bare `git diff --check` prints nothing.
Resolution: the PD's post-hand-off items R1-R4 are folded and accepted; nothing is contested. Task criterion 4 holds again at commit `de1855317b` and this hash; the PD re-hands the plan to the assistant gobby#14069 for Josh (next Program Director entry when it arrives).

### Adversary re-finalization on the R5 hash (adversary gobby#14449; message on `125b0c97...c92c` at `9e47eb68e3`, 2026-09-24 09:5x CDT, delivered as pending P2P context after the writer's compaction): nothing remains
Adversary (verbatim): "Adversary gobby#14449: nothing remains on runbooks R5 sha256 125b0c97171bcec4841cbb18f7b64e9dfaf1475a990da4d3e0c5c352e4889c2c at commit 9e47eb68e3255e45ce75d67c2bb31edea4a1c0d0. The non-goal removal, the as-is/to-be restatement, and the lane-manager sweep hold. Finalized to gobby#14018. Validate exited 0 with Phases 3, and git diff --check is clean."
Writer verification (09:5x CDT, re-run 12:1x CDT): the blob committed at `9e47eb68e3255e45ce75d67c2bb31edea4a1c0d0` hashes to `125b0c97171bcec4841cbb18f7b64e9dfaf1475a990da4d3e0c5c352e4889c2c` (`git show 9e47eb68e3:.gobby/plans/runbooks.md | shasum -a 256`); bare `uv run gobby plans validate .gobby/plans/runbooks.md` on it printed the plan path and `Phases: 3` and nothing else; bare `git diff --check` printed nothing.
Resolution: R5's four directives and Josh's roster directive are folded and accepted; nothing is contested. Task criterion 4 held at commit `9e47eb68e3` and this hash; the PD's review passed and then superseded it with R6 (Program Director rulings, R6).

### Adversary A1 on the R6 hash (adversary gobby#14449; message received ~12:3x CDT 2026-09-24, wake)
Verbatim: "Adversary gobby#14449, #22808 R6. Hash confirmed: .gobby/plans/runbooks.md at 361d656a97620fe672b6cfb4a76640465d668fbe, sha256 1a389022f3c74dfde76b08774ca85f03a19cf8d24e6b62b69ef14ba2978c529e. The blob matches. Validate exited 0 with Phases 3. git diff --check is clean. These hold. Non-goal 1 is as-is/to-be and 3.4 renames planner.yaml to plan-writer.yaml, rewrites the four council bodies, deletes the two taskless files, and repoints stages.yaml, PROMPT_BUILDERS, and the listed tests. The sandbox path is spawn_agent_impl merging into agent_sandbox_config and then prepare_sandbox_launch. 1.5.3 and 1.5.5 are the discriminating tests. program-director is the named unsandboxed role, with a reason, and 1.5.6 refuses enabled:false without one. The consumers-unchanged lines do not name a retired slug. Shifted line numbers are not findings. A1, blocking. Decision 20 says the one unsandboxed role is program-director, and a launch line may add or narrow restrictions. The tool contract then says booleans override and either the definition block or the call block may set enabled:false when it carries a reason. A launch of any other role can pass that block and turn the sandbox off. Confine enabled:false to the program-director definition. A call block may add or narrow restrictions and may not set enabled:false. Fold or Contest A1. I will not finalize on 1a389022 while it is open."
Disposition: A1 folded as R7 (entry under Program Director rulings, after R6).

### Adversary re-finalization on the R7 hash (adversary gobby#14449; message received ~12:5x CDT 2026-09-24, wake)
Verbatim: "Adversary gobby#14449: A1 fold accepted. (u) and (v) accepted. Nothing remains on sha256 861022b78aa2618ab21c50d1c75535592f3d8e28ca10803e21e80dbf24a3fccb at commit a68a7002f99466f5e447c7f1a7af2326eecb63c0. Re-finalized to gobby#14018. Validate exited 0 with Phases 3, and git diff --check is clean."

## Program Director rulings

### R1 Rulings on Q1-Q6 (PD message `62c8f875`, 2026-09-23 20:10 CDT)
Verbatim: "PD rulings on the six runbooks questions for #22808 (Runbooks plan council round). Josh sees them at final review and may override. Josh's latest word (09-23 19:5x) supersedes earlier decisions and rulings. The mechanical folds are fine as a logged pre-round commit.

Q6 FLOW: encode #22808's flow. Debate to consensus with no round cap, the adversary finalizes to the PD, the PD may send it back, then the assistant takes it to Josh, and expansion waits for Josh. This supersedes ruling 21's two rounds and verifier report. The fourth pane is a read-only lookup helper (researcher definition, today gobby#14332). plan-verifier is dropped. Its mechanical checks move into the adversary's finalization report: `gobby plans validate`, `git diff --check`, sha256 and coverage-contract checks.

Q1 ROSTER: accept your proposal minus plan-verifier. Definitions: program-director, assistant, lane-manager, researcher, reviewer, archivist, log-monitor, elicitor. Writer, enhancer and adversary become personas on the existing planner, plan-enhancer-taskless and plan-adversary-taskless definitions. Drop post-epic-reviewer (ruling 23).

Q2 LAYOUT: two bundled scripts from 2.1's verbs. orchestration-v1.sh builds control (PD, assistant) and monitors (log monitor, Archivist). plan-council-v1.sh <plan> builds the four-pane council tab. Lane panes stay out: the PD opens them per lane with /goal.

Q3 #22713: no block (Josh rejected a). Ruling 7 requires every amendment reviewed before close, so a session variable is insufficient: it dies with the session and the reviewer cannot see it. The claim-time values must land somewhere durable that the reviewer reads. First ask gobby#14332 whether an existing durable surface already records update_task diffs (task history or audit). task-close-reviewer.yaml stays a Non-goal only if the close reviewer already receives that surface. Otherwise it is in scope with the least change. Keep the injected disclosure order.

Q4 BACKEND: accept, pending gobby#14332. If workspace panes cannot fall back to tmux at all, drop the runbook-script gterm assertion as dead mechanism. Keep the 3.2 no-backend-named test and the registration text.

Q5 #22695 (gclient command mode): mint no duplicate. #22695 stays 2.1's leaf and stays in the gclient lane queue (after #22780, the #22698 leaves and #22805). Ask gobby#14332 whether start_expansion_run can adopt an existing leaf. If it can, adopt. If not, 2.1 cites #22695 as existing work outside expansion in its coverage.

Set_handoff when you need to. The enhancer starts on your refreshed hash after the 21:00 Codex upgrade."

Writer moves on R1:
- Q1/Q2/Q6: FOLD as one P3 + decisions sweep (decisions 7, 8, 10; 3.1 `agent_scope`; 3.2 roster; 2.1 gains `plan-council-v1.sh`; V1 steps 4-6; D2 `original_acceptance_items`). W6 folds inside it. Recorded below under "Writer sweep after R1" with the diff summary and commit.
- Q3: FOLD pending the gobby#14332 answer on a durable update_task diff surface (lookup `3b96fc7d` asked for the rules-engine fields; a follow-up asks for the durable surface). 3.3 is drafted either way with the injected disclosure order; the durable carrier is chosen from the answer.
- Q4: FOLD pending lookup `472b0cbd`. The 3.2 no-backend-named test and registration text are folded now.
- Q5: FOLD. Memory 4430ab84 (expansion-run binding) says a run resolves registrations by task UUID/sequence/path ancestry and derives child stage manifests; it does not say whether an existing leaf under the root is adopted or duplicated. Follow-up lookup sent to gobby#14332. Until answered, 2.1 cites #22695 as existing work.

### R2 Rulings on the five sweep positions and Josh's Telegram rule (PD message received on resume after the cutover compact, 2026-09-23 21:2x CDT)
Verbatim: "PD to writer gobby#14425: sweep hash recorded (runbooks.md sha256 8987dd5e... at e413116cb9). Rulings on your five positions, all ACCEPTED as written: (1) 2.1 stays a deliverable; when expansion mints 2.1's task, #22695 closes as superseded and the lane slot passes to it. (2) The #22713 claim_snapshot column and trigger ride migration 450 inside 1.1 (one cutover). (3) The registration line repeats 1.2's `backend` field; no persona names a backend. (4) Restarts stay with the PD; the assistant only alerts. (5) 3.3 uses get_artifacts rather than growing _crud.py. Log them in the round file. One addition for the assistant role definition, from Josh (verbatim): \"add to your runbook instructions that I don't need you to repeat what you send on Telegram here. It's already in your context.\" Rule: after a Telegram send, the assistant's terminal reply confirms it in at most one line and never restates the content. Fold it in with the next stage commit and send me the new hash. The enhancer's new ref comes after the cutover."

Writer moves on R2:
- (1)-(5): accepted as written, so no plan change; the positions already stand in decision 16 (Q5, Q3), 1.1's trigger paragraph, 3.2's registration line, the assistant bullet's item (4) and 3.3's `get_artifacts` target.
- Josh's Telegram rule: FOLD as S5. The assistant persona (3.2) gains a fifth requirement quoting Josh verbatim with the rule (after a Telegram send the terminal reply confirms the send in at most one line and never restates the content); the assistant's divergence line names it (the book is silent on it, criterion 10); 3.2.4 widens to the four items plus this rule by fixed phrases, same test `test_assistant_requires_the_four_comms_items`.

### R3 Send-back on the final hash (PD message `64e65c8e`, 2026-09-24 00:38 CDT, wake requested; three items, the rest accepted as written)
Verbatim (line breaks joined): "PD review of #22808 at 50d5cfff95 (sha256 01f40581...): SENT BACK with three items. The rest of the plan is accepted as written. Record this verbatim in the round file; the adversary re-attacks the new hash, then finalizes to me again. S1 (blocking, mechanical). Migration 450 is taken. #22740 (drop sessions.heuristic_title) is being built as 450 on epic-rust-gclient tonight (gobby#14436), and it lands before this plan expands. Renumber this plan's migration to 451 everywhere: 1.1's title, the file `451_add_runbook_roles.sql` and the MIGRATIONS entry; the test names `migration_451_*`; `latest_version` 451 in bundle.rs, schema_contract.rs and cli_contract.rs; decision 16's 'ride migration 450'; V1 step 2's 'applies migration 450'; Research context 'Latest existing migration is 449', which becomes 450 (#22740). The claim-snapshot trigger still rides the same migration, so V1 step 2 stays one cutover. S2 (factual). Research context says the no-postgres fallback in crates/gcore/src/grant/bundle.rs carries a stale `latest_version: 447`. That is false on HEAD: #22809 (8039cebc41) set it to 449 (bundle.rs:218) and added a default-features guard test (memory 24090e86). Say that step (4) moves that literal with the others, and drop the stale-literal sentence. S3 (clarity). 3.2's council blocks say 'so the spawn-side plan runs behave as before'. But adding `group:runbook` to planner, plan-enhancer-taskless and plan-adversary-taskless applies 3.1's runbook-no-code-edits to their spawned runs as well, because agent_scope matches `_agent_type` on either surface. State that this is intended. Also verify from the pipelines or the spawn prompts that a spawned planner, enhancer or adversary never writes a non-markdown path (for example coverage YAML through Write rather than through the plans CLI), and cite where. If one does, the carve-out must cover it. Also check against the prompt book: the adversary launch line says `--reasoning-effort high`, but the live council adversary gobby#14423 runs at xhigh. Keep whichever the book says and cite its line. The hand-off to the assistant for criterion 5 comes after the re-finalization."
Writer verification and moves on R3:
- S1: verified. HEAD's committed migrations end at 449 (`git ls-files` lists 447-449 only); #22740 (drop `sessions.heuristic_title`) is claimed and building, and the PD assigns it 450. FOLD: every 450 of this plan's migration becomes 451 (1.1 title; `451_add_runbook_roles.sql` in Targets, 1.1.1 and 1.1.5; the `EmbeddedMigration { version: 451` carrier step; `latest_version: 451` in step (4) and 1.1.3; the `migration_451_*` test names; decision 16 "ride migration 451 rather than a 452 of their own"; V1 step 2 "applies migration 451"); the 1.1 research context now says 450 is #22740's and lands first. The `resolve_probe_env (450-468)` in 2.1 is a source line range and stays.
- S2: verified. `crates/gcore/src/grant/bundle.rs` line 218 reads `latest_version: 449` on HEAD, set by #22809 in `8039cebc41` (2026-09-23 20:21 CDT); memory 24090e86 names the guard `grant::tests::expected_schema_identity_tracks_catalog_head`, which exercises the fallback only under gcore's default features. FOLD: the stale-literal sentence is dropped; the research context says step (4) moves the literal to 451 with the others and names the guard; V1 step 1 adds `cargo test -p gobby-core --lib grant::tests` without `--features postgres` (one clause; the memory asks for it on every migration).
- S3: verified. `_filter_by_agent_scope` (`src/gobby/workflows/engine/core.py` 838-855) keeps a scoped rule when `variables["_agent_type"]` is in `agent_scope`, with no surface check, so the group reaches spawned runs. Write paths checked: the spawned planner writes only the plan file (`planner.yaml` `plan` step `allowed_tools: "all"`, line 220; prompt 68-75 authors narrative only, never the manifest), admitted by the `planner` carve-out; the spawned enhancer has `Edit` and `Write` in `blocked_tools` (`plan-enhancer-taskless.yaml` 18-20, prompt 81-82); the spawned adversary never edits the artifact and returns section ids and the coverage attestation through `end_agent_run` (`plan-adversary-taskless.yaml` 44-45, 55-56, 104; lane subagents are forbidden edits, 60-64). Coverage YAML is written by the daemon's `regenerate_coverage_manifest` (`src/gobby/mcp_proxy/tools/plans/__init__.py` 224-255) and by `expansion-qa` (`expansion-qa.yaml` 31), which is outside the rule's `agent_scope`. FOLD: the council block says the spawn-surface reach is intended and why it blocks nothing they do; the 3.2 research context carries the citations; no carve-out change is needed.
- Launch line: the book names no provider or effort for the adversary (section 11, lines 645-681, and "The plan council", 519-535, carry no Grok or effort line; a case-insensitive grep for `grok` over the book finds nothing; `~/Desktop/gobby-team-resume-2026-09-22.md` has no adversary line either). The definition of record `plan-adversary-taskless.yaml` line 13 says `reasoning_effort: xhigh`, and the live council seat runs `Grok 4.7 (xhigh)` (composer border captured 2026-09-23). FOLD to `--reasoning-effort xhigh` with that citation in 2.1; a signed line from the PD or Josh overrides it.
Resolution: R3 folded in commit `898932d7ba1dccd5786d5573a508e24d82f1ad28` (2026-09-24 00:57 CDT, both plan files by pathspec with `git commit --only`); plan sha256 `0504de2aea9e4327d3ff9054dbfc22d51236669aa1b137e8b6f216cca55a1431`. The PD accepted both writer judgment calls (PD message, 01:0x CDT, verbatim): "Thanks; index clear, and my two merges are in. Both judgment calls stand: (1) the default-features `cargo test -p gobby-core --lib grant::tests` is right, since the fallback guard only runs there; (2) `--reasoning-effort xhigh` for the adversary matches the live seat and plan-adversary-taskless.yaml. I'll review when the adversary re-finalizes." The adversary re-finalized on this hash (Adversary findings, re-finalization entry); the PD's review is the next entry.

### PD review of the R3 hash and hand-off to the assistant (PD message, 2026-09-24 01:0x CDT): PASSES
Verbatim: "PD -> writer: R3 PD review PASSES at 898932d7ba (sha256 0504de2a). S1: 451 throughout; the only 450 mention left is #22740's. S2: bundle.rs:218 is 449, as stated. S3: the spawn-surface reach is documented with evidence from each definition. Handed to the assistant for Josh at 01:0x; expansion waits for him. Record the hand-off in the round file for #22808 criterion 5 (commit with `--only`). Then wait for Josh's verdict. The next council item (#22641) opens only on Josh's or my word."
Resolution: task criterion 5 is met: the PD reviewed the final hash and handed the plan to the assistant gobby#14069 for Josh's review. Expansion waits for Josh; the writer holds until his verdict arrives through the PD, and #22641 opens only on Josh's or the PD's word.
### R4 Post-hand-off amendment (PD message, 2026-09-24 03:58 CDT, delivered as pending P2P context without a wake; done after #22816 R3 as instructed): SENT BACK, two blocking items plus two folds; Josh has not yet approved expansion
Verbatim (paragraph breaks joined): "PD post-hand-off AMENDMENT for #22808 (runbooks.md at 898932d7ba). Do it AFTER #22816 R3; the same review of the migration-451 leaf found the #22816 gaps here too. Josh has not approved expansion yet, so this is a PD send-back. R1 BLOCKING: the 451 leaf has no step for the five signed grant goldens (tests/runtime_grants/golden/*.json). Regenerate them per test_golden_vectors.py:4-6 (recompute payload_checksum, re-sign with GOLDEN_SECRET; precedents 2b7cf60e79 and ec61574731). Add them to Targets with a scope-reason, as step (6) of the carrier procedure (lines 290-303), and add test_golden_vectors.py to V1. R2 BLOCKING: the gcore schema commands compile zero tests without `--features postgres` (default features [], `pub mod schema` gated at lib.rs:46; schema_contract.rs and catalog_manifest_freshness.rs are `#![cfg(feature = "postgres")]`). Fix line 221 (`cargo nextest run -p gobby-core -E 'test(schema)'`) and line 295 (the manifest regeneration: it silently does nothing, so the manifest would never be regenerated) by adding `--features postgres`. Sweep every other cargo command in the plan for the same trap. R3: line 296 `target/debug/gdaemon schema-identity --json` does not exist (unrecognized subcommand). It is `target/debug/gdaemon schema version --json`. R4 drift: lines 304-310. 450 landed on 0.5.0 (8de59a666f; bundle.rs:218 now reads 450 with the 450 GOLDEN_* values), so the leaf bumps 450 to 451. Fold as R4, commit with `commit --only`, and send the hash to adversary gobby#14449 for a re-check. The adversary re-finalizes to me, and I re-hand it to the assistant for Josh."
Writer verification and moves on R4 (04:1x CDT, against 0.5.0 HEAD `d92014bedc`):
- R1: verified. `tests/runtime_grants/golden/` holds five files (brokered_datastores, direct_datastores, old_client_new_grant, payload_skew_unknown_field, unavailable_datastores); `test_golden_vectors.py` lines 3-6 say to regenerate every file after `expected_schema_identity()` changes, recomputing `payload_checksum` and re-signing with `GOLDEN_SECRET` (`support.py` line 13); `NEGATIVE_GOLDENS` (line 40) holds the skew vector, whose `future_capability_probe` sits at the top level outside the signed payload; precedents `2b7cf60e79` (449) and `ec61574731` (450) each changed one line in each of the five files. FOLD: the five files join 1.1's Targets with scope-reasons ("re-signed at identity 451"; the skew vector's says its unknown field stays outside the signed payload); the carrier procedure gains step (6) (set `schema_identity`, recompute `payload_checksum`, re-sign with `sign_grant(..., GOLDEN_SECRET)`, only those fields change); new acceptance 1.1.6 names `test_grant_vectors_round_trip` and `test_config_revision_signed`; V1 step 1 runs `tests/runtime_grants/test_golden_vectors.py` under the test-hub DSN with `GOBBY_TEST_PROTECT=1`.
- R2: verified. `crates/gcore/Cargo.toml` line 15 `default = []`; `lib.rs` line 46 `#[cfg(feature = "postgres")]` on `pub mod schema`; `tests/schema_contract.rs` and `tests/catalog_manifest_freshness.rs` open with `#![cfg(feature = "postgres")]`. FOLD: `--features postgres` added to the Constraints command (line 221, with the reason) and to the manifest regeneration in step (2), which now names the test-hub DSN in place of `<scratch pg url>` and says what each omission does (no DSN: the test skips with an eprintln; no feature: no test compiles and the manifest is never rewritten); V1 step 1 says the same for the schema tests. Sweep of every other cargo command in the plan: `cargo nextest run -p gobby-client`, `-E 'test(command)'`, `cargo clippy -p gobby-client`, `cargo fmt -p gobby-client -- --check` and `cargo build --release -p gobby-client` target `crates/gclient`, whose `Cargo.toml` declares no `[features]` and whose sources carry no `cfg(feature`; `cargo build -p gobby-daemon` enables gcore's `postgres` in `crates/gdaemon/Cargo.toml` line 21; `cargo test -p gobby-core --lib grant::tests` stays default-features on purpose (the no-postgres guard, PD ruling on R3). No other command changes.
- R3: verified. `~/.gobby/bin/gdaemon schema --help` (04:1x CDT) lists `apply`, `plan`, `sweep-test-schemas`, `verify` and `version`; `schema-identity --json` is gcode's subcommand. FOLD: step (3) reads `target/debug/gdaemon schema version --json` and names the subcommand list.
- R4: verified. `8de59a666f` (03:21 CDT) merged `b9303f8183`; the migrations directory ends at `450_drop_session_heuristic_title.sql`; `bundle.rs` line 218 reads `latest_version: 450`. FOLD: the research context's drift paragraph says the newest committed migration on 0.5.0 is 450 (#22740, `b9303f8183`, merged in `8de59a666f`), that bundle.rs line 218 reads 450 beside the 450 `GOLDEN_LATEST_CHECKSUM` and `GOLDEN_ASSETS_ROOT_HASH` (lines 15-18), that step (4) moves the three from 450 to 451 together and that `schema_contract.rs` moves four (version, newest file name, latest checksum, assets root hash); the guard-test sentence and V1's default-features run are unchanged. No other line of the plan cites 449 as the head.
Resolution: R1-R4 folded in the R4 commit (both plan files by pathspec with `git commit --only`; the sha is in the Resulting artifact's next entry); the hash goes to adversary gobby#14449 for the re-check, the adversary re-finalizes to the PD, and the PD re-hands the plan to the assistant gobby#14069 for Josh.
### PD review of the R4 hash and re-hand-off to the assistant (PD message, 2026-09-24 04:2x CDT, delivered through the writer's coordination wait `09b75665`): PASSES
Verbatim: "PD: #22808 R4 at de1855317b (sha256 8e4ca52c) PASSED my review after adversary gobby#14449's re-finalization. Handed back to the assistant for Josh. Record the pass in the round-2 file if your flow logs it; no plan edit needed. Keep reporting the #22816 close verdict (review b2957e95)."
Resolution: task criterion 5 holds again: the PD reviewed the R4 hash and re-handed the plan to the assistant gobby#14069 for Josh's review. No plan edit. Expansion waits for Josh; his verdict arrives through the PD.

## Writer sweep after R1
Staged so a validating commit exists before the 21:00 cutover; each stage is one commit.
- S1 record: decisions 8 and 10 marked superseded in place, decisions 13-15 appended (Q1/Q6, Q2, Q3-Q5); V1 step 2 migration number; Constraints wording (sha only). Status: committed `eebfc0ad2db67f2b82168cf3517da46a5f18ed91`.
- S2 3.2 rewrite to the decision-13 roster with the five standing instructions and the shared tail verbatim, a divergence line per definition (criterion 10), the no-backend-named test (criterion 1), and D2 `original_acceptance_items` repointed. Status: committed `9d082c7db47a160ef6b980831e32aad604332a97` (bare validate clean; `git diff --check` clean, 21:0x CDT).
- S3 wiring: 3.1 `agent_scope` lists and the program-director rename of the delegate nudge (keyed on a non-`.md` write path so the PD's merges pass; rule-text change under PD approval); 2.1 second script, split of 2.1.5; 1.2 target and 1.2.7; V1 steps 3-6. Status: written with the Q5 wording (decision 16, 2.1 research context) and the criteria map; committed `e7d05fac306c75ba23574e4711675f1362b0b5ce` (bare validate clean; `git diff --check` clean, 21:2x CDT).
- S4 3.3 (#22713 carrier) after lookup L3; Q4 script assertion after 472b0cbd; Q5 wording after L4. Status: written as 1.1's trigger paragraph and 1.1.5, the new 3.3, the Non-goals edit, decision 16's Q3 sentence, the 3.2 reviewer reference and V1 step 8; committed with this entry (the Q4 assertion was dropped in S3, the Q5 wording landed in S3). The validator refused `_crud.py::get_task` as a target at 996 lines (`production-size-growth`), so 3.3 surfaces the field through the existing `gobby-tasks-artifacts-ops:get_artifacts` and the close reviewer gains that one allowed tool.
- S5 Josh's Telegram rule folded into the assistant persona, its divergence line and 3.2.4 (R2). Status: committed with this entry (bare validate clean; `git diff --check` clean, 21:3x CDT).

### Sweep design of record (writer, 20:5x CDT, checked with the advisor)
- S2 3.2 Targets: eight persona YAMLs (`program-director`, `assistant`, `lane-manager`, `researcher.yaml::*` edit, `reviewer`, `archivist`, `log-monitor`, `elicitor`) plus persona-block edits to `planner.yaml`, `plan-enhancer-taskless.yaml`, `plan-adversary-taskless.yaml` (already `surfaces: [spawn, persona]`; one-line personas at planner 18-19, enhancer 26-27, adversary 24-25; `rule_selectors` at planner 127, enhancer 91, adversary 166, researcher 69-73). Every definition embeds the five standing instructions (book 72-89) and the shared tail (91-113) verbatim; the test module carries the same text as a constant and asserts containment, because a test cannot read `~/Desktop`. One divergence line per definition, "none" allowed: lane-manager (book section 3 "Dispatcher" 186-214; ruling 22 naming; ruling 23: no spawns, stands down after restart 8, so the persona holds the seat for bounded runs the Program Director orders and otherwise reports), researcher (fourth council pane lookup duty), reviewer (ruling 23: verdicts only, the Program Director lands), planner (#22808 no-cap flow versus ruling 21's two rounds), adversary (carries the dropped plan-verifier checks). The assistant carries criterion 9's four requirements. Registration line in every persona (criterion 3): the first message names `GOBBY_PANE_REF` and the backend, gterm for a workspace pane because `_pane_source` resolves native unconditionally; `GOBBY_PANE_ID` unset means no runbook pane; an adopted pre-existing terminal is the one caveat. Criterion 1 test: no key named `backend` or `terminal_backend` anywhere in a definition tree and no backend-selection text in prompts (`AgentDefinitionBody` has no backend field, so the test guards prose). Criterion 11: lane-manager never calls `spawn_agent` for a persistent role and never passes `terminal_backend`. D2 `original_acceptance_items` repoints from 3.2.2 to the reviewer-definition item.
- S3: 3.1 `agent_scope` every role except `program-director`; carve-outs assistant `docs/` and `.gobby/plans/`, planner and elicitor `.gobby/plans/`; the nudge becomes `program-director-delegate-once` keyed on a `canonical_write_file_paths` entry not ending in `.md` so the Program Director's merges pass (W-item under PD rule-change approval). 2.1: `plan-council-v1.sh` joins Targets and 1.2 Targets/1.2.7; 2.1.5 splits into a control+monitors item and a council item per decision 14; launch lines per `~/Desktop/gobby-team-resume-2026-09-22.md`; V1 steps 3-6. Constraints gain one criteria 1-7 map: 1 test; 3 registration line; 5 by construction (`pane.split` refuses `terminal_failed`, the script exits 1 per 2.1.3 and V1 step 7); 2, 4, 6, 7 are #21565's per ruling 16; no script gterm assertion.
- S4: new 3.3 "Task amendment disclosure (#22713)" [category: code] (depends: 1.1). Carrier: `task_artifacts.claim_snapshot` text holding JSON {claimed_at, session_id, description, validation_criteria, labels}, added to 1.1's migration 450 and written by a PostgreSQL trigger on `tasks` AFTER INSERT and AFTER UPDATE OF `claimed_by_session_id` when the new claimant is non-null and differs (pattern `tasks_state_bucket_au`, baseline.sql 5338-5340). The trigger covers every claim path with no Python call sites: `_creation.py` insert with a claimant, `_transitions.py` 177-251, `_manager.py` `update_task` 348, `_stage_state_transitions.py` 638-650. `_artifacts.py` `_ARTIFACT_FIELDS`/`TaskArtifacts` gain the field; `get_task(brief=false)` (`mcp_proxy/tools/tasks/_crud.py` 489-527) adds `claim_snapshot` to the full record; `task-enforcement/disclose-task-amendment.yaml` fires `inject_context` on `after_tool` for `gobby-tasks` `update_task` when `task_id` (#N, path or UUID) is in `claimed_tasks` and any of the three fields is present; `task-close-reviewer.yaml` moves from Non-goals to Targets, reads the snapshot, computes the field diff itself (`get_task_diff` is the code diff) and bounces an undisclosed amendment. 3.3 is the follow-up #22713's own criterion names; the `needs-decision` label on #22713 is the documented gate workaround. Q5 wording folds into 2.1's research context as the position above.

## Lookup answers (gobby#14332, message `1e77d597`, 2026-09-23 20:36 CDT) and writer moves
- Q4 terminal backend: `WorkspaceOps._pane_source` (`src/gobby/terminals/workspace_ops.py:754-790`) resolves `native` unconditionally at 771 for every fresh pane; only `terminal_id` adoption (`_adoptable`, 792) can bring a pre-existing terminal of any backend into a pane. `terminals.backend` (`crates/gcore/assets/schema/baseline.sql:3929`, CHECK tmux|native) is the only backend column; `workspace_panes` has none (migration 440). `_identity_env` (228-242) exports no backend and `get_workspace` returns the `WorkspacePane` fields only. A missing host surfaces from `_fill` (823-873) as `WorkspaceOpError("terminal_failed", ...)`; `docs/guides/gterminal-development-guide.md:91-190` states there is no silent tmux fallback. Only `spawn_agent` resolves a backend (`resolve_terminal_backend`, `src/gobby/agents/spawn_models.py:26-38`; default `TerminalConfig.default_backend` = native). Move: decision 15's condition is met, so no script gterm assertion is added; S3 writes one criteria 1-7 map (1 as the 3.2 test, 3 as the registration line, 5 holds by construction through 2.1.3, and 2, 4, 6, 7 are #21565's per ruling 16).
- L3 #22713 carrier: no durable surface records `description`/`validation_criteria`/`labels` edits (`task_lifecycle_events`, baseline.sql:3742-3752, is state transitions with a text reason; `task_validation_history`, 3845-3858, is run outcomes). `task-close-reviewer.yaml` allows only `gobby-tasks:get_task` and `get_task_diff` (239-240), and `get_task(brief=false)` returns no artifacts. Rule-engine facts: `tool_input.server_name`/`tool_name` plus the promoted nested args (`src/gobby/workflows/engine/enforcement_checks.py:930-940`; pattern in `task-enforcement/block-reopen-task.yaml:10-19`); `claimed_tasks` variable `{uuid: '#N'}` written by `detect_task_claim`; effects set_variable, inject_context, observe, mcp_call, rewrite_input, load_skill, run_command, block (no `warn`). Move: 3.3 creates the carrier: `task_artifacts.claim_snapshot` (text holding JSON) in 1.1's migration 450, written inside `claim_task`/`claim_task_for_agent` (`src/gobby/storage/tasks/_transitions.py:177-251`) when ownership changes hands, surfaced by `get_task(brief=false)`; an `after_tool` `inject_context` rule delivers ruling B1's standing order at the edit; `task-close-reviewer.yaml` moves from Non-goals to Targets and bounces an undisclosed amendment. 3.3 is the follow-up #22713's own criterion asks for.
- L4 #22695: `apply_run` (`src/gobby/tasks/expansion/_apply.py:91-`) always mints children; its only existing-output check refuses a re-apply. Move (a position, not a question): 2.1 stays a deliverable; when expansion mints 2.1's task the Program Director closes #22695 as superseded by it and the lane slot passes to the minted task. The alternative, 2.1 as `kind: deferred` with `task_ref: "#22695"`, would drop 2.1's acceptance coverage (contract line 38 kinds; 291-352 `task_ref`). Named in the hash message.

### R5 Reopen from Josh's D2 review (PD message, 2026-09-24 09:2x CDT, delivered through the writer's coordination wait `159a2568`; #22808 reopened by the PD and re-claimed by the writer at 09:2x CDT): four directives, all folded
PD (verbatim, paragraph breaks joined): "PD to writer: #22808 (Runbooks plan council round) is REOPENED for revision R5, from Josh's D2 review. Claim it, then read the reopen reason: it has the full directive and my scope call. Summary: (1) :177 pane-placement non-goal stays. (2) Remove the :182-184 step_workflow non-goal. Restate it as as-is/to-be: step programs run only for spawned sessions today; the target is that every non-one-shot agent executes a step workflow. The roles ship as persona prompts in v1 and adopt step workflows from a sibling plan (interactive step-workflow execution). Don't grow this plan. (3) 'dispatcher loop' becomes 'lane manager', and sweep the other uses of 'dispatcher'. (4) Sweep the plan for any other code fact framed as a boundary. Commit with `commit --only` and send the hash to adversary gobby#14449 for re-finalization; it finalizes to me. Validate clean before handing off."
Reopen reason (appended to the #22808 description, verbatim, paragraph breaks joined): "[Reopened: Josh's D2 review correction (terminal, 2026-09-24 09:3x, via the assistant; memory 556ec801). Rule for plans: separate current behavior (as-is) from target design (to-be); a code fact may not stand as a non-goal or constraint when it contradicts the target model. Target model, verbatim: "Everything is an interactive session now unless it's a one-shot (like feature_low/mid/high). Everything is an agent running in a pane except one-shots. Therefore, every agent needs the ability to execute a step workflow." Revision R5 of .gobby/plans/runbooks.md: 1. runbooks.md:177 'No pane placement for spawn_agent' STAYS (Josh: "that's the reality today. when you spawn something it still runs in gclient"). 2. runbooks.md:182-184 'No step_workflow on the interactive roles...' is REMOVED as a non-goal. PD scope call: interactive-session step-workflow execution (lifting the is_spawned gate in build_persona_changes / step_workflow_complete) is an engine-wide capability, so it gets a SIBLING plan and does not grow this plan. In runbooks.md, state it as as-is/to-be: as-is, step programs run only for spawned sessions; to-be, every non-one-shot agent, the interactive roles included, executes a step workflow. The lane-manager and council roles ship as persona prompts in v1 and adopt step workflows when the sibling plan lands. Role definitions must not assume they stay persona-only. Do not cite memory 8351b9de's 'interactive personas never receive agent-step enforcement' as a boundary. 3. Naming (Golden Path ruling 22, memory 25966c9f): 'the dispatcher loop' at :184 becomes 'the lane manager'. 'Dispatcher' means only the retired gobby build dispatcher; sweep the plan for other uses. 4. Sweep the whole plan for other current-code facts framed as non-goals, constraints or boundaries that contradict the to-be model, and restate each as as-is/to-be. Flow: writer R5 commit (commit --only), adversary re-finalizes to the PD, PD review, then the assistant sends the plan to Josh as a Telegram attachment for D2. Expansion waits for Josh.]"
Writer verification and moves on R5 (09:3x CDT, against 0.5.0 HEAD `cfe241bc92`):
- (1) :177 stays. Verified against memory 556ec801 (Josh, verbatim: "that's the reality today. when you spawn something it still runs in gclient"): the non-goal excludes only spawn-time placement control (workspaces decision record #10). No change.
- (2) FOLD. The Non-goals bullet at 182-184 is removed. New decision 17 carries Josh's ruling verbatim, the as-is (`build_persona_changes` sets `step_workflow_complete` only when `is_spawned`, verified at `src/gobby/mcp_proxy/tools/apply_persona.py` line 113 [R6 annotation: first recorded as line 74, a misread; the file is unchanged between `cfe241bc92` and `9e47eb68e3`, and `is_spawned` is also the parameter at line 48 and the payload key at line 83] `if agent_body.step_workflow and is_spawned:`; `activate_default_agent` and 1.4's tool both call it with `is_spawned=False`, per 1.3's and 1.4's research context), the to-be (every non-one-shot agent, the interactive roles included, executes a step workflow), the PD's scope call (the `is_spawned` lift is engine-wide and belongs to a sibling plan; this plan does not grow into it), the v1 rule (the lane-manager and council roles ship as persona prompts and adopt step workflows when the sibling plan lands; no definition assumes persona-only) and the as-is/to-be planning rule. Memory 8351b9de is not cited. 3.2's "Rejected" list drops its step-workflow item and a "Deferred, not rejected" bullet replaces it. 1.4's "Never touches `agent_step_instances`" and acceptance 1.4.3 are scoped to v1 with a decision-17 pointer, because the sibling plan decides where an interactive session's step program is instantiated; the test names are unchanged.
- (3) FOLD. "the dispatcher loop stays a persona prompt" went with the removed bullet. Sweep of the other ten `dispatcher` matches: the Context sentence lists the lane manager and says the hand-built prompt file called it "dispatcher"; decisions 7, 8 and 10 keep their 2026-09-21 audit-trail text with a parenthetical naming the seat `lane-manager` (ruling 22); the program-director divergence reads "the lane manager no longer spawns lane workers on its own"; the assistant divergence "hands TTLs to the lane manager"; the elicitor "no lane-manager TTL"; 3.2's deferred bullet names the lane manager. Unchanged on purpose, because they mean the retired build dispatcher: the Context paragraph on `gobby build` stage-manifest dispatch and `src/gobby/dispatch/`, and the lane-manager definition's own sentence "named `lane-manager` because \"dispatcher\" is the build-stage dispatcher (ruling 22)".
- (4) Sweep of the Context, Decision Record, Non-goals, Constraints, 1.3, 1.4, 3.2, V1, D1 and D2 for current-code facts framed as boundaries against the to-be model: only the step-workflow items above. Left as they are: the terminal-backend facts (decision 16 Q4 and the Constraints entry) describe pane creation, which the to-be model does not touch; 1.4's refusal of spawned sessions is the tool's own design (a spawned session's definition was applied at spawn), not a step-program boundary; 3.2's `surfaces` and `prompts.agent` lines describe the existing definition files as-is; the Context's "role lived only in the prompt" describes the hand-built chart, which the plan replaces.
Resolution: R5 folded in the R5 commit (both plan files by pathspec with `git commit --only`; the sha is in the Resulting artifact's next entry). The hash goes to adversary gobby#14449, which re-finalizes to the PD; the PD reviews and the assistant sends the plan to Josh for D2. Expansion waits for Josh.

### R5 addendum: Josh's council roster directive (two PD messages, 2026-09-24 09:4x and 09:5x CDT, delivered as pending P2P context while the writer folded R5): folded as decision 18 and deliverable 3.4
PD (verbatim, line breaks joined): "PD: an addition to #22808 R5, JOSH DIRECTIVE, verbatim: "The runbooks plan should retire plan-enhancer-taskless and plan-adversary-taskless, or rename them. We need plan-writer, plan-enhancer, plan-adversary, and plan-mechanic." PD reading: - The council roster becomes exactly those four agent definitions. The two -taskless definitions are renamed (preferred if their bodies carry over) or retired, with every reference migrated: the Q1 roster, the orchestration/council scripts and any tests. - plan-mechanic owns the mechanical checks my Q6 ruling moved into the adversary's final report: plan validate, the coverage manifest, and re-verifying citations and literals against HEAD. The adversary keeps the substantive attack. This directive supersedes that part of Q6. If plan-writer or plan-mechanic has no existing definition to build on, say so in R5 and define it in the plan. Fold it all into R5 with the as-is/to-be items."
PD (verbatim): "PD, one more R5 note from Josh, verbatim: "mechanic could also be used for codebase research. that way we don't have four planning agents all doing codebase exploration for their parts. idea at least. we'll have time to optimize it after we get it working." Record it as to-be, deferred past v1; don't build it in v1. Target: plan-mechanic produces one shared evidence pack (file/symbol map, line citations, current behavior) that the writer and enhancer consume. PD constraint to record with it: the adversary may use the pack but keeps independent code spot-checks, so a shared wrong premise can't pass every role."
Writer verification (09:4x-09:5x CDT [R6 annotation: first written as 09:5x-10:1x, which is impossible because the R5 commit that carries this fold is 09:52:13 CDT], HEAD `cfe241bc92`): `plan-enhancer.yaml` (281 lines) and `plan-adversary.yaml` (464) already exist as gobby build's stage-native definitions with task-bound `claim` and `terminate` steps (`stages.yaml` 37-38 name `planner` and `plan-adversary`); `plan-enhancer-taskless.yaml` (208) and `plan-adversary-taskless.yaml` (296) are the `/gobby plan` spawn variants (`tests/agents/test_plan_enhancer_agents.py` 1-14 states the split); `plan-writer` and `plan-mechanic` have no definition (`plan-mechanic` is a folded skill name, `catalog.json` 1041, resolving to `references/plan/repair.md`); the taskless names are consumed by `dispatch/prompts.py` 356/358, `dispatch/spawn_artifacts.py` 43-45 and 508-509, three rule templates, two plan-skill references, two docs and sixteen test files; `sync_bundled_agents`'s orphan sweep soft-deletes rows whose bundled file is gone (`sync.py` 5-6, 135-136).
Writer moves, all FOLD: decision 18 (the directive verbatim, the HEAD facts, the roster, the mechanic's checks superseding ruling Q6's verifier part, the mechanic taking the fourth pane, and Josh's evidence-pack note verbatim as to-be with the PD constraint); decision 13's council part marked superseded (audit trail kept); decision 14's fourth pane is the mechanic; new deliverable 3.4 (category code: two new persona definitions, persona blocks on the existing pair, the retirement with every migrated reference as targets, a `Consumers unchanged` inventory for `PROMPT_BUILDERS`, four acceptance items); 3.2 trimmed to the eight chart personas with `researcher` losing the council lookup duty and the standing-text constants moved to `tests/agents/runbook_text.py` for reuse; 3.1's `agent_scope` and carve-out name the four council definitions; 2.1's council script titles and the mechanic's launch line; 1.2's `--role plan-writer`; Non-goal 1 carves out the two dispatch name-map deletions; P3 intro; V1 steps 3 and 5.
Writer positions, named with the hash (the PD may rule otherwise): (a) the taskless pair is retired, not renamed, because the target names are taken by the stage-native pair and renaming either pair reaches gobby build (Non-goal 1); (b) `plan-writer` is a new persona-only definition built on `planner`, which stays as the planning stage's `default_agent`; (c) the `/gobby plan` interactive enhancement and review rounds move into the council tab (`enhancement.md` and `review.md` line 6) rather than gaining a taskless mode on the stage-native pair, because that mode would merge two step workflows and change gobby build's reviewer; (d) the mechanic's launch line is the lookup pane's (`claude-opus-5`), since the book has no mechanic section; (e) the researcher leaves the council tab (four definitions, four panes) and keeps its chart seat.
Resolution: folded in the R5 commit with the as-is/to-be items; the hash goes to adversary gobby#14449 for re-finalization to the PD.

### PD review of the R5 hash and hand-off to the assistant (PD message, 2026-09-24 09:5x CDT, delivered through the writer's coordination wait `6cfd38d6`): PASSES, then SUPERSEDED by R6
Verbatim: "PD: #22808 R5 at 9e47eb68e3 (sha256 125b0c97) PASSED my review after adversary gobby#14449's re-finalization. Rulings on your positions: (a) through (e) all ACCEPTED as written. Handed to the assistant for Josh's D2 (Telegram attachment). One low item in the round file, audit trail only: the R5 verification cites apply_persona.py line 74 for `if agent_body.step_workflow and is_spawned:`; at HEAD that line is 113. Fix it when you record this pass. No plan edit is needed. Expansion waits for Josh."
Writer verification and correction (10:0x CDT): `src/gobby/mcp_proxy/tools/apply_persona.py` is unchanged between `cfe241bc92` and `9e47eb68e3` (`git log` on the path prints nothing), so the R5 entry's "line 74" was a misread: `if agent_body.step_workflow and is_spawned:` is line 113, and `is_spawned` is also the parameter at line 48 and the payload key at line 83. The R5 entry above is annotated in place, and so is the R5 addendum's verification range (its "09:5x-10:1x CDT" cannot precede the 09:52:13 CDT commit that carries the fold). The plan cites no line number.
Resolution: task criterion 5 was met on R5: the PD reviewed the hash, accepted positions (a)-(e) of the R5 addendum and handed the plan to the assistant gobby#14069 for Josh's D2. The PD then superseded the pass with R6 (next entry) on Josh's directives of 10:2x-11:04 CDT; the R5 positions stand accepted and are redone under decision 19 as R6 positions (a)-(e).
### R6 Retirement of `gobby build` assumed, launch through `spawn_agent` by both paths, sandboxed panes with runbook-declared restrictions, the program-director as the one unsandboxed role, per-role specification blocks, existing workflows kept, #22660 folded (nine PD messages, 2026-09-24 10:00-11:5x CDT; the first with wake, the rest as pending P2P or hook context after the writer's compactions): folded
PD message 1, retirement (message `a8a51d12-006a-4c56-a741-88e559cd9b62`, 10:00 CDT, wake; verbatim, paragraph breaks joined): "PD to writer: #22808 needs revision R6 (you still hold the claim; no reopen). This supersedes my R5 pass. JOSH DIRECTIVE (terminal, 10:2x via the assistant, verbatim): "assume we're retiring gobby build and stages (we won't actually until we have runbooks where we need them, but I'm the only user and I haven't touched gobby build in months). if we need old definitions we can grab them from git history." PD reading, fold all of it: 1. Compatibility with gobby build, the stage registry and dispatch is no longer a constraint. The actual retirement stays D2 (deferred), so v1 does not delete gobby build. Only the compatibility constraint goes. 2. 3.4: the roster is exactly plan-writer, plan-enhancer, plan-adversary and plan-mechanic, each shaped for the council. Drop 'keep the stage-native bodies': plan-enhancer.yaml and plan-adversary.yaml are rewritten as council definitions (persona, tools, rule selectors; any step workflow is decision 17's to-be). Drop 'planner untouched': planner.yaml becomes plan-writer (rename, then modify) unless you can show a live non-build consumer that needs planner. The -taskless pair is still deleted. Old bodies stay recoverable from git history, so nothing is kept for reference. 3. Keep the tree green, not the old behavior. Where stages.yaml, src/gobby/dispatch/ or tests still name a changed or removed definition, repoint or delete that reference the cheapest way that keeps sync, validation and the named test files passing. Preserving gobby build's planning-stage behavior is not required. List each touched consumer in 3.4's targets. 4. Non-goal 1 ('No changes to gobby build, src/gobby/dispatch/, the stage registry...') is restated as as-is/to-be: as-is, gobby build still exists; to-be, runbooks replace it (D2). Wherever that non-goal forced a workaround (the persona blocks on the stage-native pair, the 'Consumers unchanged' PROMPT_BUILDERS inventory, position (b)'s planner split, position (c)'s reasoning), redo it plainly. Sweep the whole plan for any other gobby-build-compatibility workaround. 5. My R5 sibling-plan note (a council-mode step program apart from the task-bound build one) is moot. The definitions are council-shaped now, so drop it if you recorded it. Flow as before: R6 commit with --only, validate clean, hash to adversary gobby#14449, adversary re-finalizes to me, my review, then the assistant sends it to Josh. Record this message verbatim in the round file. Also fix the apply_persona.py :74 to :113 cite while you are in there."
PD message 2, sandboxed panes (~10:3x CDT; verbatim): "PD to writer: one more R6 item. JOSH DIRECTIVE (terminal, via the assistant, verbatim): "The runbooks need to be able to launch clis in sandboxed panes, just like spawn_agent does." Fold it as a v1 requirement. Every runbook pane launch, meaning gclient command mode and the one-shot launch command, starts its CLI under Gobby's managed SRT sandbox the same way spawn_agent does (memory 827ab8eb: managed SRT for every supported coding CLI; provider-native sandboxes are out of contract). Reuse the spawn path's sandbox resolution (sandbox_resolvers.py and the launch-contract flags) instead of building a second one. State it as as-is/to-be: as-is, only spawn_agent launches sandboxed; to-be, runbook panes do too. It must not stay a non-goal, and it needs acceptance items with a named test that a runbook-launched CLI's argv or environment carries the same sandbox wrapping as a spawn_agent launch of that provider. If a role must run unsandboxed (the assistant's docs edits, for example), make that a per-role runbook setting with the reason, not a blanket exemption. Record this verbatim in the round file with the gobby build directive."
PD message 3, restrictions in the runbook (~10:4x CDT; verbatim): "PD to writer: an addendum to the R6 sandbox item. JOSH (verbatim): "sandbox restrictions need to be part of the runbooks". The runbook declares each pane's or role's sandbox restrictions next to its role binding: filesystem read and write deny roots, and network and package-registry allowances. Launch applies them through the existing SRT policy surface (memories f08d0c93, fe8aa2a8 and 4a00633f). Don't invent a new policy format. Replace my earlier line about a per-role unsandboxed setting: exemptions and loosening are part of that same declared restriction set. The acceptance items need a named test that a restriction declared in a runbook reaches the launched CLI's SRT policy. Also a test that a runbook with no restriction block gets the same default policy as spawn_agent. Record this verbatim in the round file."
PD message 4, the program-director exception (~10:5x CDT; verbatim, line breaks joined): "PD to writer: an R6 decision from Josh (in my pane, 10:5x, verbatim): "for now, you'll be the un-sandboxed agent. your the only one that can write code in 0.5.0 (every other agent makes changes inside a worktree when the runbooks are in place)." Fold it with the sandbox items: 1. program-director is the ONE role that launches unsandboxed. Its runbook restriction block says so, with the reason: daemon restart, cutover, binary promotion into ~/.gobby/bin, pushes, and read-only hub psql. A sandbox is inherited by child processes, so a daemon restarted from a sandboxed pane would start inside it or fail. Every other role launches under the managed SRT sandbox as the earlier items say. 2. The program-director is instead held in by role-scoped hook rules (agent_scope program-director) in 3.1's runbook group. Name the rules and test them: a claimed task before commits, explicit-path commits only (no `git add -A`, no stash), no SQL writes against the hub, restarts and cutovers only through the gobby CLI, push only to origin, and no directory deletion. Reuse existing rules where they exist and say which. 3. Only the program-director writes code on 0.5.0 in the main checkout. Every other role that changes code works in its own worktree: the runbook binds each such pane to a worktree, and the launch creates or reuses it through the existing worktree path. A no-edit role (monitor, researcher, assistant docs-only) needs none. Add an acceptance item that a lane-role pane launched from a runbook starts with its cwd in its worktree, never in the main checkout. 4. To-be, recorded but not designed, because Josh is still shaping it: typed privileged commands (roughly `gobby ops restart|cutover|promote|push`) that a sandboxed caller can trigger and that run outside the sandbox. Once they exist, the program-director can be sandboxed too. Record it as an open to-be item with no deliverable. Record this verbatim in the round file with the other R6 directives."
PD message 5, per-role specification (~10:5x CDT; verbatim, line breaks joined): "PD to writer: another R6 directive from Josh (in my pane, verbatim): "and the runbook plan needs to include all of the definitions for each role, what they can do, how their sandbox works, rules they use, etc." Fold it as a complete per-role specification in the plan, covering every role a runbook can launch: the chart roles in 3.2 and the four council roles in 3.4. Each role gets one block with the same fields: - definition name, surfaces and launch line (provider, model, effort) - persona: purpose and what it owns - what it can do: allowed and blocked MCP tools, native tools, and whether it may edit code (only the program-director on 0.5.0; others in their worktree) - sandbox: its restriction block (filesystem read/write roots, deny roots, network and package-registry allowances) through the SRT policy surface; or unsandboxed with the reason (program-director only) - rules: every rule group and named rule that applies, with its agent_scope, existing and new - skills it loads, and the core-skill bootstrap on or off - worktree binding (yes/no, and how it is created or reused) - messaging: who it reports to and wakes, and what it may send to Josh - step workflow: as-is persona prompt, to-be step workflow (decision 17) Nothing about a role may live only in prompt prose; if the plan relies on it, it is a field. Also fold Josh's standing role preferences into the roles they govern. Assistant: memory 39b1c075 (don't repeat what you sent him) and a620b843 (clickable decisions, no nagging, plans as Telegram attachments). Program-director: its hook-rule set from my last message. A validation acceptance item should prove every shipped definition matches its block (tools, rule scope, sandbox, skills). Record this verbatim in the round file with the other R6 directives."
PD message 6, scope ruling (~11:0x CDT; verbatim, line breaks joined): "PD to writer: an R6 scope ruling from Josh (in my pane, verbatim): "I don't want to design the new workflows yet, I just want to be able to launch panes through spawn_agent or through the cli." Fold it: 1. NO step-workflow design in this plan. Decision 17 stays as-is/to-be, with interactive step workflows in a sibling plan, and each role block's step-workflow field reads persona prompt in v1. Do not add looping or cyclic step programs. 2. The two launch paths are in scope for v1: a runbook pane launches either through spawn_agent or through the CLI (gclient command mode and the gobby CLI), and both give the same result: role, sandbox policy from the runbook, worktree binding and placement. That makes spawn_agent pane placement (target workspace, tab, and a split beside a named pane) a v1 capability. Restate :177's 'No pane placement for spawn_agent' as as-is/to-be, with this plan delivering the to-be. Both paths share one launch implementation (the spawn path's sandbox resolution and launch contract), with acceptance items proving a pane launched each way lands in the requested tab or split and carries the same role, sandbox and worktree. 3. The earlier R6 directives stand: council-shaped definitions with gobby build compatibility dropped, sandboxed panes, runbook-declared restrictions, the program-director as the one unsandboxed role under hook rules, worktrees for code-changing roles, and the full per-role spec blocks. Record this verbatim in the round file."
PD message 7, addendum (right after message 6; verbatim): "PD to writer: an addendum to the last ruling. JOSH (verbatim): "and have existing workflows work since 1 is true". Here '1' is my point that a pane launched through spawn_agent is a spawned session, so the is_spawned gate already lets its step workflow run. Fold it: a runbook pane launched either way, through spawn_agent or through the CLI, runs its definition's EXISTING step workflow exactly as a spawn_agent launch does today. The CLI path goes through the same spawn launch, so the session counts as spawned. Design no new workflows. A role whose definition has no step workflow stays persona-only. Add an acceptance item: a definition that carries a step workflow, launched from a runbook by each path, gets step_workflow_complete and its step instance just as spawn_agent gives it. Record this verbatim in the round file."
PD message 8, #22660 (~11:5x CDT; verbatim, paragraph breaks joined): "PD, for #22808 R6. Josh's decision (11:04 CT, decisions page): fold #22660 (Persistent role agents hand off with clear=true between tasks, after recovering operating instructions on clear) into the runbooks plan and settle it there. Add to R6's per-role spec blocks: (1) How a persistent role recovers its full operating instructions after set_handoff(clear_session=true): role prompt, reporting lines, and message formats such as EVENT= and VERDICT=. The natural answer given R6's direction is the role's definition, re-applied at clear-successor SessionStart. The options in #22660 are: the handoff carries the prompt, the definition is re-applied at clear-successor SessionStart, or a runbook file the successor reads. Pick one and justify it against the clear-successor delivery contract (memories 81ab8aca, 27fe9c9f). (2) Which roles clear between tasks, and when. At minimum, cover the researcher and the reviewer/landing roles. (3) An acceptance item: a live check that a clear-successor researcher and reviewer resume correctly with their instructions intact. Record #22660 as covered in the round file. Don't close #22660 yourself. It stays escalated until Josh approves R6, and I close it then, citing the plan section. Read #22660 with get_task for the full idea text."
PD message 9 (broadcast, no action recorded in the plan): 0.5.0 pushed to origin (`1e21ebb72d..9e47eb68e3`); the heavy-job hold lifted, one heavy job at a time machine-wide.
Writer verification (10:2x-11:5x CDT against 0.5.0 HEAD `9e47eb68e3`, re-checked at `f7b9ccc54c`; every cite below is a line at that HEAD):
- Retirement consumers of the `planner` name and the taskless pair: `registry/stages.yaml` 37 (`default_agent: planner`; 38 keeps `plan-adversary`), `dispatch/_rule_actions.py` 40, `dispatch/prompts.py` 71-76 and 360 (356/358 the taskless alias keys), `dispatch/spawn_artifacts.py` 43-45 and 508-509, `tasks/expansion/_plan_gate.py` 30-31, `spawn_agent/_code_index.py` 7, the rules `review-learning/inject-planner-lessons.yaml` 10, `memory-lifecycle/guard-plan-memory-writes.yaml` 13-19, `plan-mode/reset-plan-mode-on-session-start.yaml` 11 and the two taskless lesson rules, `references/plan/enhancement.md` and `review.md` line 6, `docs/guides/plans-and-plan-mode.md` 34, `docs/contracts/plan-coverage.md`, `RETIRED_AGENTS` (`test_retired_bundled_definitions.py` 50-56) and the tests listed in 3.4's targets with their lines. No non-build consumer needs the `planner` name; `web/` has none; `crates/` has only a sidebar sample string. `sync_bundled_agents`'s orphan sweep soft-deletes rows whose template is gone (`agents/sync.py` 135-136).
- Definition files: `planner.yaml` 257 lines (`surfaces: [spawn, persona]` 9, `provider: codex` 10, `step_workflow` 133), `plan-enhancer.yaml` 281 (`step_workflow` 124 with `claim` and `terminate`), `plan-adversary.yaml` 464 (`step_workflow` 270; `rule_selectors` exclude `tag:task-skill-gates`), the taskless pair 208 and 296, `researcher.yaml` 160 (its step workflow is the discovery one-shot: `claim` allows only `claim_task`/`get_task` and transitions on `task_claimed`, so a taskless seat sits in `claim` forever). `AgentDefinitionBody` (`agent_models.py` 78-219): `surfaces` 102-103, `require_surface_prompt_blocks` 164-174 (persona needs `prompts.persona`, spawn needs `prompts.agent`), `provider` defaults to `inherit` (108); no rule ties `step_workflow` to a surface.
- Spawn path: `spawn_agent_impl` requires `prompts.agent` (`_implementation.py` 147, 156), merges the sandbox at 397 (`apply_write_grant(agent_sandbox_config(daemon_config), write_grant)`), requires a parent (400-401) and checks depth (403; `session.py` 118-143); timeout None with definition timeout 0 means none (324-328). `gobby agents spawn` posts the same tool at `/api/mcp/gobby-agents/tools/spawn_agent` with the local token (`cli/agents.py` 176-307; `--session` required at 180, `--timeout` default 120 at 198). `_resolve_spawn_project_context` (`_factory.py` 182-235): an explicit `project_path` wins. The system session as parent is the scheduler's precedent (`scheduler/executor.py` 304; `system_session_id` `_constants.py` 77-80). `build_cli_command` (`command_builder.py` 12-219): the default `agent` mode is the TUI with an initial prompt; only `interactive` adds the web-chat `--output-format stream-json`.
- Pane path: `tab_create` (`workspace_ops.py` 372-402) and `pane_split` (460-490) adopt an existing terminal through `_pane_source` (754-790) and `_adoptable` (792-807); `_fill` (823-873) reserves the pane row before `spawn_web_terminal` with `_identity_env` (228-242), so a placed launch must reserve the row first or the CLI starts without `GOBBY_PANE_REF`; `pane_split` already takes `worktree_id`. `resolve_actor_scope` (`actor_scope.py` 64-90) refuses every caller with an `agent_run_id`.
- Lifecycle: `IdleCheckHandler` monitors every active run with a live terminal (`idle_check_handler.py` 109-135, 166-190); `_idle_after_delivered_result` (`lifecycle_monitor.py` 717-728) spares only taskless runs; stuck (603-697), completed-task (792-797) and periodic-enter (564-566) checks run every 30 s; the stale sweep touches only runs with no pid and no live terminal; runs join terminals by `agent_runs.terminal_id`; `_session_end.py` 62-83 ends a native seat's predecessor as `paused` on `/clear`, so the run survives; `agent_health` times out only runs with `timeout_seconds`; `_bind_clear_successor` (`materialize.py` 152-) copies task claims only.
- Sandbox: `agent_sandbox_config` (`sandbox.py` 160-167) from `config.agent_sandbox` (`daemon_sandbox.py` 10-64: enabled, backend, mode, allow_network, extra read/write/deny paths, domains, git network, package registries, unix sockets); `prepare_sandbox_launch` (`srt_runtime.py` 472-669) refuses `allow_network: true` under SRT and renders `assets/settings.json` under `~/.gobby/runtime/managed-executions/<run-id>/`; `SandboxLaunch.wrap` (86-117); `spawn_executor.wrap_provider_command` wraps once (85-87); `_unsupported_sandbox_request_error` (`spawn_executor_support.py` 222-243); `_record_actual_sandbox_enforcement` (245-262) writes `sessions.sandbox_enabled` and `sandbox_policy_hash` (baseline 3505-3506); `sensitive_write_roots` (`sandbox_policy.py` 302) documents that denyWrite beats allowWrite and deny roots are emitted literal and canonical, so `extra_deny_write_paths: ["."]` denies every write under the checkout; `Path(raw_path).expanduser()` (`sandbox.py` 399) expands `~/Desktop`; resolvers for Claude, Codex, Qwen, Grok and Agy (`sandbox_resolvers.py` 447-471); `reap_terminal_sandbox_run` (`sandbox_reaper.py` 316-318).
- Prompt delivery and rules: `_inject_agent_instructions_if_needed` (`_agent.py` 254-) delivers `prompts.agent` to a spawned session and `prompts.persona` to an interactive one including a `/clear` successor, once per context, so one anchored text under both keys arrives once. `_audience_matches` (`engine/core.py` 867-886) treats every spawned session as autonomous, so `context-handoff/block-autonomous-clear-session.yaml` (tag `default`, priority 12) would block `/clear` in every launched seat. `_filter_by_active_rules` (888-907) keeps the rules the active definition's selectors match (`tag:`, `group:`, `name:`; exclude beats include). The `worker-safety` rules are gated on `variables.get('is_spawned_agent')` and tagged `[worker-safety, enforcement, gobby]`; their interactive twins carry tag `default`. Existing rules for the program-director set: `require-task-before-commit` (`task-enforcement/`, tag `default`), `no-git-stash` (`worker-safety/no-destructive-git.yaml`), `no-force-push`, `no-recursive-rm` (`no-destructive-shell.yaml` 4); `no-daemon-management` blocks `gobby start|stop|restart` and its `-http` twin blocks `curl` to `/api/admin/restart|shutdown`. No bundled rule guards hub SQL writes, `git add -A`/`commit -a`, push remotes or directory deletion.
- gclient: `crates/gclient/src/daemon/rest.rs` (317 lines) holds the REST helpers; the mock daemon records each request body (`tests/mock_daemon/mod.rs` 29) and answers by `enqueue(method, path_prefix, status, body)` (175). `uv run --no-sync` exists.
- Launch lines: the prompt book and the resume file name Claude Fable 5.1 (1M) for the Program Director (`claude-fable-5-1[1m]`, no effort flag), Claude Opus 5 (1M) for the assistant and the research seat, Codex `gpt-5.6-sol` xhigh for the lane-manager and the reviewer, Codex `gpt-5.6-terra` medium for the archivist, Codex `gpt-5.6-luna` medium for the log-monitor, and no provider for the elicitor or the council; the live adversary seat is `grok-4.7` xhigh.
- Sizes: `_implementation.py` 964, `lifecycle_monitor.py` 956, `workspace_ops.py` 973, `spawn_executor.py` 962, `cli/agents.py` 736, `idle_check_handler.py` 566, `agent_models.py` 219, `actor_scope.py` 90.
- Message 1 item 5: neither the plan nor this file recorded a council-mode step-program sibling note; the only sibling note is decision 17's `is_spawned` lift, which is a different note and stays. Nothing to drop.
Writer moves, all FOLD:
- Message 1: decision 19 (Josh verbatim; as-is `gobby build` exists, to-be runbooks replace it in D2; the compatibility constraint goes; each touched consumer is a target). Non-goal 1 restated as as-is/to-be; the Context direction line; decision 18 amended with a note; 3.2's rejected epic-reviewer item reworded; D2's reason cites decision 19. 3.4 rewritten: `planner.yaml` is `git mv`'d to `plan-writer.yaml` and rewritten, `plan-enhancer.yaml` and `plan-adversary.yaml` are rewritten as council definitions, the taskless pair is deleted, `RETIRED_AGENTS` gains the three names, and every consumer (the stage registry, two dispatch maps, the expansion gate, the code-index list, five rules, two references, two docs and the named tests) is a target with a line-cited scope-reason; the R5 `PROMPT_BUILDERS` inventory is gone because `prompts.py` is a target; acceptance 3.4.1-3.4.5. The `apply_persona.py` cite is fixed in this file.
- Messages 2, 3, 4, 6, 7: decision 20 (Josh verbatim five times; as-is/to-be for spawn placement, which retires the :177 non-goal, and for sandboxing; the one unsandboxed role with its reason; worktrees for code-changing roles; `gobby ops` as a to-be item with no deliverable; no new workflows; existing step workflows run by both paths). New deliverable 1.5 (`spawn_agent` gains `placement` and `sandbox`; `AgentDefinitionBody.sandbox`; both paths go through `spawn_agent_impl`; the pane row is reserved before the spawn; native backend; identity env; parent = the caller session or the system session; the tab's checkout pinned as `project_path`; no timeout; `notify_parent_on_completion` false; pane-bound runs admitted as terminal actors and exempt from idle, stuck, completed-task and periodic-enter handling; pane close reaps the sandbox run; 1.5.1-1.5.11; the three size-guard splits). 1.4 is a retired slot; 1.2 shrinks to the columns and the derived roster fields; 1.3 keeps the successor continuity and gains 1.3.5; 2.1 gains the `launch` verb over the tool endpoint, the scripts become one launch line per seat with the program-director's `--unsandboxed` reason, and 2.1.10; decision 6 amended (`GOBBY_PANE_REF`); decisions 1, 4, 5, 11 and 12 amended in place; the Constraints public-API line amended; 3.1 gains the five program-director guards, the four reused rules by name, the corrected audience note and 3.1.4-3.1.5; each role block carries its sandbox block; V1 steps 3, 5, 7 and 9.
- Message 5: decision 21 (Josh verbatim; the ten fields, continuity included). 3.2 holds eight blocks and 3.4 four, every field filled; 3.2.9 and 3.4.5 prove each shipped definition matches its block through the `ROLE_BLOCKS` table in `tests/agents/runbook_text.py`; the assistant block folds memories 39b1c075 and a620b843 (3.2.4); the program-director block carries its rule set (3.1.5).
- Message 8: decision 21's continuity field; 1.3 re-applies the definition at the clear successor's SessionStart (the persona surface is delivered once to a non-spawned session; the handoff carries state only; `_bind_clear_successor` also copies `sessions.sandbox_enabled` and `sandbox_policy_hash`), chosen over a handoff that carries the prompt (it would duplicate the definition and drift) and over a runbook file the successor reads (a second source of role text); the clear cadence per role (researcher after each report, reviewer after each verdict, plan-mechanic after each final-hash report, the writer, enhancer and adversary between plans; the program-director, assistant, lane-manager, log-monitor, archivist and elicitor compact only); 3.2.10 and V1 step 6 are the live check. #22660 is covered by decision 21 and 3.2.10 and is NOT closed by the writer; it stays escalated for the PD to close on Josh's approval.
Writer positions, numbered so the PD can reverse any one (each names the acceptance items it removed, if any):
- (a) The taskless pair is deleted (decision 19 leaves no reason to keep it; R5's Non-goal-1 reasoning is gone).
- (b) `plan-writer.yaml` is `planner.yaml` renamed with `git mv`, then rewritten; `planner` survives nowhere (every consumer repoints to `plan-writer`).
- (c) The `/gobby plan` enhancement and review rounds move into the council tab plainly: the stage-native pair becomes the council pair, with no taskless mode.
- (d) The mechanic's launch line is the lookup pane's Claude Opus 5 line, and it validates with `uv run --no-sync gobby plans validate` because its deny root covers `.venv`.
- (e) The researcher leaves the council tab (four definitions, four panes) and keeps its chart seat.
- (f) `researcher.yaml` is rewritten as the chart seat without its discovery step workflow (the PD named it as the addendum proof; a taskless seat would sit in its `claim` step forever); the research stage keeps the name; the addendum proof is a stepful fixture in 1.5.8 and a live launch of `architect` in V1 step 9.
- (g) 1.4 (`apply_agent_definition`), `pane.set_role`, `--role`/`--runbook` on `new-tab` and `split`, `gobby panes set-role` and the `pane.role_set` event are dropped: the launch is the only writer of a role, and a role change is a relaunch. Removed: 1.2.2, 1.2.3, 1.2.4, 1.2.7, 1.4.1-1.4.5, 2.1.4, 2.1.7, the old V1 step 6 and V1 step 5's relaunch-by-hand.
- (h) gclient's `launch` posts the HTTP tool endpoint with the local token; no new daemon op; decision 12, the Constraints public-API line and 2.1's rejected REST item are amended to say so.
- (i) Pane-bound runs are admitted as terminal actors and exempt from the idle, stuck, completed-task and periodic-enter monitors, by one predicate keyed on `agent_runs.terminal_id` joining `workspace_panes.terminal_id`.
- (j) Every definition declares both surfaces with one anchored text, so the spawned seat and its `/clear` successor read the same instructions.
- (k) The 2026-09-21 sentences of decisions 1, 4, 5, 11 and 12 that the launch supersedes are amended in place with a dated note.
- (l) The retirement fold lists every consumer as a target; fixture-only occurrences of a name in tests are not consumers and stay.
- (m) The number 1.4 is retired (a supersession note keeps the slot); the launch is 1.5; work order 1.1, 1.2, 1.5, 1.3, then 2.1, then 3.1, 3.2, 3.3, 3.4.
- (n) Criterion 11 is restated: the pane launch is the pane creation; the lane-manager sentence reads "Never launch a persistent or semi-persistent role yourself: those seats come up only through the pane launch (1.5) that the assistant runs, never through a headless `spawn_agent` and never with `terminal_backend`."
- (o) The restriction block lives on the definition (`AgentDefinitionBody.sandbox`, SandboxConfig field names plus `enabled` and `reason`) and the launch line may narrow it; the alternative, a script-line-only block, would launch an unrestricted seat whenever a runbook forgot it.
- (p) The elicitor's launch line is the research seat's (the book names no provider for it).
- (q) The program-director selects its four reused rules by `name:` and never `tag:worker-safety` as a whole, because `no-push` and `no-daemon-management` would block the pushes and restarts the seat exists for.
- (r) Roles that clear keep `tag:default`, so the interactive twins hold in a `/clear` successor where the spawned-gated rules lapse; the five guards carry no spawned condition and hold in both.
- (s) Provider, model and effort stay on the launch line (definitions keep `inherit`), where Josh edits them today.
- (t) `AgentDefinitionBody.sandbox` on the program-director is `enabled: false` with its reason; the launch records the reason in the `_launch_sandbox_reason` variable and in its reply, since the session row stores only `sandbox_enabled` and the policy hash.
Resolution: R6 folded in the R6 commit (both plan files by pathspec with `git commit --only`; the sha is in the Resulting artifact's next entry). The hash goes to adversary gobby#14449, which re-finalizes to the PD; the PD reviews and the assistant sends the plan to Josh. Expansion waits for Josh; #22660 stays open for the PD.

### R7 Adversary A1 folded: the sandbox exemption is definition-only
A1 (blocking, ~12:3x CDT, quoted under Adversary findings): the 1.5 tool contract let either the definition block or the call block carry `enabled: false` with a reason, so a launch of any role could switch its sandbox off, against decision 20's one unsandboxed role.
Moves: decision 20 now says the exemption is declared only in a definition's block and no launch line can switch a sandbox off. The 1.5 tool contract moves `enabled` into the refused-in-call set beside `backend`, `mode` and `allow_network`, and the merge rule reads "`enabled: false` comes only from the definition's block, where the validator requires a `reason`". 1.5.6 is restated as three clauses (a call block with an `enabled` key is refused whatever its value; a definition block with `enabled: false` and no `reason` fails `AgentDefinitionBody` validation; a launch of such a definition records `sandbox_enabled` false, `_launch_sandbox_reason` and the reply's `sandbox.enforced`) with tests `test_call_block_enabled_key_is_refused` and `test_unsandboxed_definition_requires_reason_and_records_it` replacing `test_unsandboxed_launch_requires_reason_and_records_it`. `--unsandboxed REASON` leaves the CLI (the 1.5 CLI paragraph, the 2.1 verb table, the exit-code list, 2.1.10 and the scripts paragraph): an `enabled` key in `--sandbox` is the usage error, the program-director launch line in `orchestration-v1.sh` carries no sandbox flag, and the 3.2 program-director block says the launch line carries no flag and 1.5.6 records the reason from the block.
Positions for the PD (additions to (a)-(t), which stand):
- (u) The confinement is to definition blocks with the reason required by the definition validator, and the validator does not hard-code the role name: decision 20 names the one role, the bundled definitions are the reviewed surface (3.2 lists the program-director as the only block with `enabled: false`), and a per-name check would be a second mechanism for the same policy. The adversary asked for confinement to the program-director definition; the writer reads that as the definition side against the call side.
- (v) `--unsandboxed` is dropped rather than kept as an acknowledgement flag: a flag that must match the definition adds a second source for one fact, and Josh reads the exemption and its reason in the 3.2 spec block and in the launch reply (`sandbox.enforced` false with the reason).
Resolution: R7 folded and committed with this file by pathspec (`git commit --only`; the sha is in the Resulting artifact's next entry). The hash goes to adversary gobby#14449 for re-finalization to the PD.

### R8 Send-back S1 on the R7 hash: the sandbox exemption is sync-written only and the exempt row is sync-owned (PD message `3aa124f1-3b18-46e3-9e5d-0cbd403de440`, 12:51 CDT, wake)
Verbatim (paragraph breaks and list bullets joined): "PD review of #22808 R7 a68a7002f9 (sha256 861022b7, verified): SENT BACK on one security finding, S1. The rest of the A1 fold is clean: call blocks refuse `enabled`, `--unsandboxed` is gone from every line, 1.5.6 names both tests, and I accept (u) and (v). S1 (blocking; Josh's standing concern is an off switch agents can use to escape a sandbox). R7 makes the definition's `sandbox` block the only switch, but the plan never restricts who can WRITE that block. Today a spawn's sandbox comes from config-store defaults only (spawn_agent/_implementation.py:393), so no route exists. Once `enabled: false` is honored from a definition, these live write paths become escape routes: - `gobby-workflows:create_agent_definition` (mcp_proxy/tools/workflows/_agents.py:148) validates any dict through AgentDefinitionBody and stores it with source="installed", tags ["user"]. A sandboxed lane can create `x` with `sandbox: {enabled: false, reason: "..."}` and then call spawn_agent(agent="x"). - HTTP `update_definition` (servers/routes/agents.py:436) already maps `sandbox_config` onto body["sandbox"] for ANY existing row, the program-director's or a lane role's included, and `sandbox` is in its preserved_extra_fields. So `sandbox` is already a stored body key and the plan's 'new optional AgentDefinitionBody.sandbox' has to own that route. - Delete-then-recreate under a bundled name. `source` doesn't separate bundled rows from user-created ones, since both are "installed". Fold: 1. Every definition write path except the bundled template sync from the main checkout refuses a `sandbox` block carrying `enabled`. Enumerate them: the MCP create, the HTTP create and update routes, the workflows import path (workflows/imports.py) and any CLI that reaches them. Then the exemption can only arrive through a reviewed merge into src/gobby/install/shared (the worktree-daemon guard already stops a branch publishing templates). 2. The launch honors `enabled: false` only from a row that sync wrote. Name the provenance marker you use; `source` alone can't carry it. 3. Add acceptance items with discriminating tests: create_agent_definition refuses sandbox.enabled; the HTTP update refuses sandbox_config.enabled; a user-tagged row carrying enabled:false (inserted by the test directly) still launches sandboxed. Keep it to that. No new tool and no role-name check. The adversary re-attacks the fold hash, then re-finalizes to me. Your reply wait d4ff321b can lapse; I'm not waiting on a reply, only on the fold."
Verification (HEAD 6039a2d4a1 plus the R7 commit): `spawn_agent_impl` takes its sandbox from `agent_sandbox_config(daemon_config)` under `apply_write_grant` (the PD's line 393 cite holds); `create_agent_definition` validates through `AgentDefinitionBody` and writes `upsert_with_steps(source="installed", tags=["user"])`; the HTTP `update_definition` maps `sandbox_config` onto `body_dict["sandbox"]`, keeps `sandbox` in `preserved_extra_fields`, and writes `manager.update(definition_json=...)`; the HTTP `create_definition` builds the body field by field and never forwards `sandbox_config`; `import_definition` copies a bundled YAML with `upsert_with_steps(create_only=True)`; `_upsert_agent` in the workflows import module writes `manager.update` or `upsert_with_steps`; `duplicate` calls `upsert_with_steps(source="custom")` and has no caller in `src/`; the storage manager already separates sync writes (`upsert_from_sync`, `update_from_sync` with `from_sync=True` into `_write_update`, which locks the live row first) from the rest; `sync_bundled_agents` decides ownership with `_is_sync_managed_bundled_agent` (global row, `source` installed, tag `gobby`), stamps `tags=["gobby"]`, re-writes managed rows whose body differs from the template, refuses to touch a shadowing unmanaged row, sweeps `gobby`-tagged rows with no template, and creates new rows through `upsert_with_steps` (its one non-sync write); `get_bundled_agents_path()` is `get_install_dir()/shared/workflows/agents`; `resolve_agent_with_row` already returns the row beside the body; the HTTP request models accept `tags` on create and update, so the tag alone is forgeable; `gobby agents` has no create or update command; the HTTP `restore_definition` maps `ValueError` to 404; `sessions.sandbox_enabled` defaults to false (baseline.sql 3505), so the system session the CLI paths parent on is never sandboxed; the sync's own `restore` flag is its re-enable path, not `manager.restore`, and `upsert_from_sync` inserts a new row where a soft-deleted one sits.
Moves: decision 20 gains the write-path sentence. 1.5 gains the "Definition write paths (PD S1)" paragraph, eight targets (the manager's `create`, `upsert_with_steps`, `_write_update`, `restore` and `_find_live`, the sync predicate and `sync_bundled_agents`, and the MCP `create_agent_definition`), four extended test files, twenty-eight consumer lines, the provenance and caller clauses in the tool contract and in order steps (1) and (3), the 1.5.11 docs wording, and acceptance items 1.5.12 (write-path refusals and the exempt row's immutability; four tests), 1.5.13 (an unmanaged row launches sandboxed; two tests) and 1.5.14 (a sandboxed caller's launch stays sandboxed; one test). The 3.2 program-director block cites them. V1 step 1 counts seven extended test files and step 2 names the post-cutover sync as what makes the marker sound.
Positions for the PD (additions to (a)-(v), which stand):
- (w) The provenance marker is the sync-ownership predicate that already exists (`_is_sync_managed_bundled_agent`: global row, `source` installed, tag `gobby`), made public, with no new column or migration. It is sound because the storage guard makes `sandbox.enabled` unwritable outside the sync entry points and the first start after the cutover re-writes or sweeps every `gobby`-tagged row, so a tag set through the HTTP `tags` field buys nothing. A launch-time equality check against the bundled YAML was considered and dropped as a second mechanism for the same fact, one that would also fail closed on an unsynced template edit.
- (x) The guard is one function at the storage seam rather than a check per route, because every enumerated path ends in `AgentDefinitionManager` and the sync-only entry points already exist. It refuses an incoming body that carries the key and any non-sync write to a row whose stored body (the row locked in the same transaction) carries it, so the program-director row is immutable outside the sync: rename, description edit, rule and variable patch, restore and re-tag all refuse, and the role changes the way the key already does, through its template and a restart. A first cut required only that the key stay identical, which let description edits and patches through; it was dropped because a renamed (the advisor's find) or re-prompted program-director row would keep its exemption until the next start's sweep, and because the restore of a soft-deleted exempt row (the writer's find) needed its own clause. Whole-row immutability is one rule where that was three. The sync's own new-row branch moves to `upsert_from_sync` to stay on the sync side of the guard.
- (y) `spawn_agent_impl` gains `definition_row` with default None and the factory switches to the existing `resolve_agent_with_row`; the direct callers in `ask/` and `feedback/` keep passing a body only and can never honor an exemption.
- (z) HTTP `import_definition` of the program-director YAML and `duplicate` of that row are refused by the same guard; both would be customization copies that launch sandboxed anyway, and a refusal is clearer than a silently ignored exemption. `create_definition` over HTTP does not forward `sandbox_config` today; that pre-existing gap is recorded, not widened.
- (aa) The caller check is a writer addition beyond the PD's three S1 items, flagged as such. A sync-owned exempt row is launchable by any session that can call `spawn_agent`, so without it a sandboxed lane would get an unsandboxed program-director child that takes its prompt, an escape by proxy that no write-path guard closes. The least mechanism is one read of the `parent_session_id` row's `sandbox_enabled` at the seam that already merges the blocks, on the precedent of `authorize_write_grant`, where a managed caller may only narrow what it holds. No role-name check and no new tool. 1.5.14 tests both branches.
Resolution: R8 folded and committed with this file by pathspec (`git commit --only`; the sha is in the Resulting artifact's next entry). Under the PD's council mechanics (message ~13:2x CDT, after Josh's 13:12 and 13:17 CT directives relayed by the assistant): the hash goes to enhancer gobby#14422 and adversary gobby#14449 in one message each, which starts the 60-minute timer; each reviews in parallel with numbered items and file:line cites sent to the other two; the council debates by message, always copying the other two, until each member states `AGREE <commit> <sha256>` on one revision, every writer fold being a new commit whose hash is sent; on consensus or timer expiry the adversary runs validate and the coverage manifest refresh, commits the manifest and sends the PD one final report; the PD presents to Josh or sends back, and S1 was the first of at most two send-backs.

## Resulting artifact
Pre-round fold commit `d42521cc0981f36430ff1f2505fcd1704b572498`; plan sha256 `aa897d8ce9cdab9740e6aa381723fac45017564283b8900c923a438c464ed150`; bare validate clean; `git diff --check` clean (2026-09-23 20:1x CDT). The enhancer hash is the one published after the sweep, not this one.
S1 commit `eebfc0ad2db67f2b82168cf3517da46a5f18ed91` (decisions 13-15 recorded; bare validate clean; `git diff --check` clean, 20:3x CDT).
S2 commit `9d082c7db47a160ef6b980831e32aad604332a97`, S3 commit `e7d05fac306c75ba23574e4711675f1362b0b5ce`, S4 commit `e413116cb9302b1169e19b97f91719bec642e7e5`; sweep hash (sent to the PD in message `26982800`, 21:1x CDT): plan sha256 `8987dd5ebb02f3cd6c5a6563b202f5488da9834df66424f1ea4ed68c3692342c`; bare validate clean (Phases 3); `git diff --check` clean. Its five positions were accepted in R2.
S5 (this entry): plan sha256 `c8a9238f6ad8e2fcf9ebcc579b6cc8797b2ba876e59302cc194169bfdb843bad`; bare validate clean; `git diff --check` clean (21:3x CDT). This is the hash the enhancer starts on unless the PD rules otherwise.
E2 (this entry; cr-1 and cr-2 folded): plan sha256 `d9d65815736a1cd0b384860228d34241ec36190ee96b5dba1ff7be114e03afcf`; bare validate clean; `git diff --check` clean (23:0x CDT). Enhancer consensus on this hash at 23:0x CDT; it is the hash the adversary attacks.
AR1 (this entry; A1-A7 folded): plan sha256 `e4baf6640abfd856669c7ead1dd8053af55602f338a4fabd9c2eeff7e6876289`; bare validate clean; `git diff --check` clean (23:4x CDT). Sent to the adversary for re-attack.
AR2 (this entry; A8 folded): plan sha256 `ab982751bb9b3452359977b068bf155154f70a748858a14a71929570109ae56f`; bare validate clean; `git diff --check` clean (23:5x CDT). Sent to the adversary for re-attack.
AR3 (this entry; A9 folded): plan sha256 `01f40581bba2492f479726f7a5caa3f2923a979135068213ca9607e513e7bbbb`; bare validate clean; `git diff --check` clean (23:52 CDT). Sent to the adversary for re-attack.
AR3 (superseded by R3): the adversary finalized on the AR3 hash (2026-09-23 23:5x CDT; message quoted under Adversary findings). Final plan commit `50d5cfff959cbee14df0b03a34902221a95cc35e`; plan sha256 `01f40581bba2492f479726f7a5caa3f2923a979135068213ca9607e513e7bbbb`; writer re-check on resume (23:57 CDT): bare validate clean (Phases 3), `git diff --check` clean. Task criteria 1-4 were met at this hash; the PD sent it back in R3.
R3 (the PD's S1-S3 and the launch-line check folded): commit `898932d7ba1dccd5786d5573a508e24d82f1ad28` (00:57 CDT); plan sha256 `0504de2aea9e4327d3ff9054dbfc22d51236669aa1b137e8b6f216cca55a1431`; bare validate clean; `git diff --check` clean (00:48 CDT, re-run 01:0x CDT). The adversary re-attacked this hash and re-finalized to the PD (01:0x CDT).
R3-final (superseded by R4): plan commit `898932d7ba1dccd5786d5573a508e24d82f1ad28`; plan sha256 `0504de2aea9e4327d3ff9054dbfc22d51236669aa1b137e8b6f216cca55a1431`. The adversary re-finalized to the PD on it (01:0x CDT, verbatim under Adversary findings); the PD's review passed and it handed the plan to the assistant gobby#14069 for Josh at 01:0x CDT (verbatim under Program Director rulings). Task criteria 1-5 were met at this hash; the PD amended it in R4 after the hand-off (Program Director rulings, R4). Round-file commits after the plan commit touch only this file.
R4 (PD R1-R4 folded): plan sha256 `8e4ca52c0887e21dd9f9f5cd781dab9b8931400f52364b3e65b15c4c610999a7`; bare validate clean (Phases 3); `git diff --check` clean (04:17 CDT); commit `de1855317b5e6e293233363085c21946b0d908ca` (04:19 CDT, both files by pathspec). The adversary re-finalized to the PD on this hash (04:2x CDT).
R4-final (superseded by R5): plan commit `de1855317b5e6e293233363085c21946b0d908ca`; plan sha256 `8e4ca52c0887e21dd9f9f5cd781dab9b8931400f52364b3e65b15c4c610999a7`. Migration 451 with the goldens, the postgres feature, the test-hub DSN and `gdaemon schema version --json` in the carrier procedure; every citation re-verified against 0.5.0 HEAD at 450. The PD's review passed and it re-handed the plan to the assistant gobby#14069 for Josh at 04:2x CDT (verbatim under Program Director rulings). Task criteria 1-5 held at this hash; Josh's D2 review sent it back (Program Director rulings, R5).
R5 (Josh's D2 correction, PD directives 1-4 and Josh's roster directive folded as decision 18 and 3.4): plan sha256 `125b0c97171bcec4841cbb18f7b64e9dfaf1475a990da4d3e0c5c352e4889c2c`; bare validate clean (Phases 3); `git diff --check` clean (09:4x CDT); commit `9e47eb68e3255e45ce75d67c2bb31edea4a1c0d0` (09:52 CDT, both files by pathspec). The adversary re-finalized to the PD on this hash (09:5x CDT).
R5-final (superseded by R6): plan commit `9e47eb68e3255e45ce75d67c2bb31edea4a1c0d0`; plan sha256 `125b0c97171bcec4841cbb18f7b64e9dfaf1475a990da4d3e0c5c352e4889c2c`. The PD's review passed with positions (a)-(e) accepted and it handed the plan to the assistant gobby#14069 for Josh's D2 at 09:5x CDT (verbatim under Program Director rulings); the PD then superseded the pass with R6 on Josh's directives of 10:2x-11:04 CDT. Task criteria 1-5 held at this hash.
R6 (decisions 19-21, deliverable 1.5 and the nine PD messages folded): plan sha256 `1a389022f3c74dfde76b08774ca85f03a19cf8d24e6b62b69ef14ba2978c529e`; bare validate clean (Phases 3, no warnings); `git diff --check` clean (12:2x CDT). Committed with this file by pathspec as `361d656a97620fe672b6cfb4a76640465d668fbe` (12:24 CDT).
R6-final (superseded by R7): plan commit `361d656a97620fe672b6cfb4a76640465d668fbe`; plan sha256 `1a389022f3c74dfde76b08774ca85f03a19cf8d24e6b62b69ef14ba2978c529e`. The adversary confirmed the hash, validate and diff-check and blocked on A1 (~12:3x CDT).
R7 (A1 folded): plan sha256 `861022b78aa2618ab21c50d1c75535592f3d8e28ca10803e21e80dbf24a3fccb`; bare validate clean (Phases 3, no warnings); `git diff --check` clean (12:4x CDT). Committed with this file by pathspec as `a68a7002f99466f5e447c7f1a7af2326eecb63c0` (12:44 CDT).
R7-final (superseded by R8): plan commit `a68a7002f99466f5e447c7f1a7af2326eecb63c0`; plan sha256 `861022b78aa2618ab21c50d1c75535592f3d8e28ca10803e21e80dbf24a3fccb`. The adversary re-finalized on it (~12:5x CDT; quoted under Adversary findings); the PD sent it back on S1 (12:51 CDT).
R8 (S1 folded): plan sha256 `bd637209e7915c0ff7710129e97d279e392049aea4843b50ec5f8cd54fb941fb`; bare validate clean (Phases 3, no warnings); `git diff --check` clean (13:35 CDT). Committed with this file by pathspec; the commit sha is recorded in the next entry.
Pending: the enhancer's and the adversary's review of the R8 hash, the council's AGREE lines, and the adversary's manifest refresh and final report to the PD (criterion 4), the PD's review and hand-off to the assistant for Josh (criterion 5), Josh's verdict, and the PD's close of #22660 citing decision 21 and 3.2.10 once Josh approves.

### Writer refinements while writing S2 and S3 (named to the Program Director with the hash)
- Registration source (criterion 1 versus 3): the design of record had the persona say "gterm for a workspace pane". Criterion 1's sentence "A definition that names one fails validation" is literal, so no definition names a backend; instead 1.2's pane view gains `backend` from the terminals row the `session_ref` JOIN already reads, and the registration line repeats what `get_workspace` shows. Cost: one derived field. The adopted-terminal case then needs no caveat text.
- Standing text: the book's five instructions (72-89) lack criterion 8's fifth item (conditional sign-off), which lives in #22691's description item 5. Every persona carries the book text verbatim plus that rule; the test constants are `STANDING_INSTRUCTIONS`, `SHARED_TAIL` and `CONDITIONAL_SIGN_OFF`.
- Criterion 9 item 4 says the communications coordinator must "perform daemon restarts without pre-approval"; book section 1 says the assistant never restarts and section 2 gives the restart sequence to the Program Director. The assistant definition requires the alerting rule (alert before, alert after with the outcome, no pre-approval) and the restart stays the Program Director's. If the Program Director wants the assistant to perform restarts, that is one line in the assistant persona.
- Q5 wording landed in S3 rather than S4 (decision 16 and 2.1's research context), because 2.1 was open for the second script.
- The council tab is named after the plan's basename (decision 14 "named after the plan"); pane titles `plan-writer`, `enhancer`, `adversary`, `researcher`.
- 3.1's nudge fires on a `canonical_write_file_paths` entry not ending in `.md` and never on a pathless repo mutation such as `git merge`, so the Program Director's landing work passes (design of record S3).
- Launch lines: the grok CLI takes `-m <model> --reasoning-effort <effort> --always-approve` (checked with `grok --help`, 2026-09-23); the Claude and Codex lines follow the team-resume file's spellings.

### Writer positions for S4 (named to the Program Director with the hash)
- Carrier placement: the `claim_snapshot` column and its trigger pair ride migration 450 inside 1.1 rather than a migration 451 of their own, so V1 step 2 stays one cutover and 3.3 stays Python, YAML and docs. A separate 451 is the alternative if the Program Director wants 1.1 to carry only workspace columns; it costs nothing at cutover (both apply in the same window) and one more carrier refresh.
- The trigger is the carrier because it covers the four claim paths with no Python call site (`_creation.py`, `_transitions.py` 177-251, `_manager.py` 348, `_stage_state_transitions.py` 638-650); `clear_artifacts` has no caller outside the package export, so the snapshot lives with the task row.
- `claim_snapshot` is read-only from Python: it stays out of `_ARTIFACT_FIELDS` and out of `_ARTIFACT_MUTATION_FIELDS`, so `set_artifact` refuses it at both layers; `_artifact_payload` adds it explicitly.
- The close reviewer computes the field diff itself from the snapshot; the injected order at the edit is the courtesy half of ruling B1 and the reviewer's diff is the enforcement half. A path-form `task_id` on `update_task` is not matched by the rule; the reviewer catches it anyway.
