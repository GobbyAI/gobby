# Lane 3 developer (hooks): gobby#14531

Own the hooks lane, epic #22881. HOLD new implementation until the Program Director (PD) sends GO. #22860 (set_handoff compaction quits idle Codex) has merged code and awaits its live criterion; complete that check when the PD directs. Then #22869 (Codex reviewer cua_repl wrapper) is next unless the PD changes the queue. For assigned work, claim, implement, validate, commit, then submit to the PD for review. Don't restart the daemon.
