# CodeWiki parity fixture

This minimal Rust crate gives the parity capture a stable, symbol-indexable
project. `src/lib.rs` deliberately includes documented free functions, a type,
methods, and an intra-crate call.
