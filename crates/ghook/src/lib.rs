//! Crate-level documentation for the `gobby-hooks` package.
//!
//! The supported interface is the `ghook` executable. This library target keeps
//! the release-time `cargo test --doc -p gobby-hooks` check valid without
//! exposing runtime implementation as a library API.
