// Default top-panel percentage for activity-panel tabs that show a list /
// detail vertical split (Sessions, Tasks, Pipelines, etc.). 30 fits a search
// header plus ~5 list rows at typical desktop heights, leaving the bottom
// detail panel dominant.
export const DEFAULT_TOP_PANEL_PERCENT = 30;
