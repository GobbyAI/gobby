> You are continuing a session under the `impeccable` skill; the design-context protocol and anti-pattern rules already apply.

# Craft (deprecated alias)

`craft` is a deprecated alias for ordinary new visual work. Apply `SKILL.md`'s
`## Sub-command Dispatch` rules exactly. If `.impeccable.md` is missing, run the
inline `teach` mode first. Then call
`get_skill_file(name="impeccable", path="references/new-work.md")` on
`gobby-skills` and follow the returned flow.

Do not ask users to invoke `craft`; natural build and redesign requests use the
same dispatch path.
