tags: [memory-lifecycle, memory, lifecycle, title, gobby, default]

rules:
  title-on-prompt:
    description: "Synthesize session title from user prompt"
    event: before_agent
    enabled: true
    priority: 12
    when: "not variables.get('session_title_set') and len((event.data.get('prompt') or '').split()) >= 10"
    effects:
      - type: mcp_call
        server: gobby-sessions
        tool: synthesize_title_from_prompt
        background: true
