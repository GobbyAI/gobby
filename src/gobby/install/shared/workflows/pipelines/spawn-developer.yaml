name: spawn-developer
type: pipeline
description: |
  Thin dispatch pipeline: spawns a developer agent for a given task.
  Dispatch automation can invoke this via run_pipeline for tracked dispatch,
  or call spawn_agent directly for untracked dispatch.

version: "1.0"
enabled: true

inputs:
  task_id:
    type: string
    required: true
    description: "Task ID to assign to the developer agent"
  provider:
    type: string
    default: "codex"
    description: "LLM provider (claude, gemini, qwen, codex, droid, etc.)"
  model:
    type: string
    default: ""
    description: "Model override (empty = provider default)"
  agent:
    type: string
    default: "backend-developer"
    description: "Enabled developer agent slug to dispatch"
  worktree_id:
    type: string
    default: ""
    description: "Worktree ID for isolation (empty = auto-create)"

steps:
  - id: spawn
    mcp:
      server: gobby-agents
      tool: spawn_agent
      arguments:
        prompt: "Work on task ${{ inputs.task_id }}. Claim it, implement, test, commit, and submit for review."
        agent: "${{ inputs.agent }}"
        task_id: "${{ inputs.task_id }}"
        provider: "${{ inputs.provider }}"
        model: "${{ inputs.model }}"
        worktree_id: "${{ inputs.worktree_id }}"
        parent_session_id: "${{ session_id }}"
