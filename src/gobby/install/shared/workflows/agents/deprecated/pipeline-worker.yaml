name: pipeline-worker
description: "[DEPRECATED] Replaced by direct pipeline execution and state-driven dispatch."
version: "2.1"
enabled: false
deprecated: true
deprecated_reason: "No surviving bundled pipeline or code path dispatches pipeline-worker; pipeline execution is now direct or state-driven."
instructions: |
  LIFECYCLE MODEL:
  - Rules are authored against semantic workflow events such as `turn_start` and `turn_end`.
  - Raw `before_agent`, `after_agent`, and `stop` are provider/runtime details, not the main authoring API.

  This deprecated definition must not be dispatched.
