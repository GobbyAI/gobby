[Unit]
Description=Gobby Daemon - Local-first AI coding assistant platform
After=network.target

[Service]
Type=simple
ExecStart={{ python_executable }} -m gobby.runner{% if verbose %} --verbose{% endif %}

WorkingDirectory={{ working_directory }}
Environment=HOME={{ home_dir }}
Environment=PATH={{ path_env }}
Environment=GOBBY_SERVICE_LAUNCH=1
Environment=GOBBY_SERVICE_NONCE={{ gobby_home if gobby_home else home_dir + "/.gobby" }}/gobby.pid.service-nonce
{% if gobby_home %}
Environment=GOBBY_HOME={{ gobby_home }}
{% endif %}

Restart=on-failure
RestartSec=10
StartLimitIntervalSec=60
StartLimitBurst=3

StandardOutput=append:{{ runtime_log_file }}
StandardError=append:{{ runtime_log_file }}

NoNewPrivileges=yes
PrivateTmp=yes
ProtectSystem=strict
ReadWritePaths=%h/.gobby

[Install]
WantedBy=default.target
